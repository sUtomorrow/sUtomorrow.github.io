<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[推荐系统学习笔记]]></title>
    <url>%2F2020%2F08%2F11%2F%E6%8E%A8%E8%8D%90%E7%B3%BB%E7%BB%9F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[推荐系统简介基于用户的推荐系统计算用户信息之间的相似性，对于和用户A相似的用户B，将用户A喜欢的物品推荐给用户B。 基于内容的推荐系统计算物品之间的相似性，与某个物品A相似的物品B，将被推荐给喜欢物品A的用户。 基于协同过滤（Collaborative Filtering）的推荐系统协同过滤是指用户行为，需要的数据表现为一个用户对物品的评分矩阵（或者点击、喜爱程度矩阵），协同过滤推荐有两种方式，一种是User-CF，对于喜欢的物品相似的用户A和用户B,将他们喜欢的其他东西推荐给对方。一种是Item-CF，如果一系列的物品都被同一个用户喜欢，则这些物品判定为相似，可以推荐给喜欢这类物品的其他用户。 混合推荐实际系统中，一般是多种推荐方式一起使用，但是混合方式很多，例如加权混合、切换混合（在不同情况下，使用不同的推荐系统）、分区混合（不同方式推荐的物品放在不同位置显示）、分层混合（不同的推荐方式串行）。 推荐系统的评测方式推荐系统的实验方法 离线实验，将已有数据作为划分为训练数据和测试数据，在训练数据上训练推荐算法，在测试数据上对测试算法进行测评。 用户调查，记录用户在推荐系统上的行为。 在线实验，AB测试：将用户随机分成两部分，分别使用不同的推荐系统，查看两部分用户的行为。 评测指标预测准确度 评分预测的准确度，均方根误差（RMSE），或者平均绝对值误差（MAE）。 Top-N推荐，精确率（precision=TP/(FP + TP)），召回率（Recall = TP / (TP+FN)）用户满意度覆盖率关注冷门物品的推荐。多样性每次推荐的物品的多样性。惊喜度信任度实时性健壮性商业目标 基于UGC(用户生成标签)的推荐用户对物品的标签可以用三元组表示，（用户，物品，标签），表示为（u, i, b），用户u对物品i的兴趣公式可以表达为$p(u, i) = \sum\limits_b n_{u,b}n_{b,i}$，其中，$n_{u,b}$表示用户$u$打过$b$标签的次数，$n_{b, i}$表示物品$i$被打标签$b$的次数。 TF-IDF在UGC中，如果有热门标签，或者热门物品，那么UGC总是倾向于推荐热门物品或者有热门标签的物品，因此需要对热门程度做一个惩罚，例如在文章的关键字提取中，不能仅仅用词语出现的频率大小来决定关键字，因此提出了TF-IDF方法，通过计算TFIDF值的大小来决定文章的关键字，计算方式如下。 $TFIDF = TF \times IDF$，这个值越大，那么当前词就越可能是当前文档的关键词。 词频(Term Frenquency, TF)表示某个词语在改文件中的出现频率，$TF_{i,j} = \frac{n_{i, j}}{n_j}$，$n_{i, j}$是在文档$j$中，词语$i$出现的次数，$n_j$表示在文档$j$中的总词数。 逆向文件频率（Inverse Document Frequency, IDF）表示一个词语的重要性，可以用文档数目除以包含该文档的总数目，然后取对数，$IDF_i = \log(\frac{N+1}{N_i + 1})$，其中$N$表示所有文档的数量，$N_i$表示包括词语i的文档数量。 TF-IDF对UGC推荐的改进在原始的UGC推荐中，用户$u$对于物品$i$的感兴趣程度由$p(u, i) = \sum\limits_b n_{u,b}n_{b,i}$计算。为了避免热门标签和热门物品的影响，我们将$n_{u,b}$替换为$\frac{n_{u,b}}{\log(1 + n^{u}_b)}$，将$n_{b,i}$替换为$\frac{n_{b,i}}{\log(1 + n^{u}_i)}$，其中$n^{u}_b$表示有多少个用户打过$b$标签，$n^{u}_i$表示有多少个用户对物品$i$打过标签。 \begin{aligned} p(u, i) = \sum\limits_b\frac{n_{u,b}}{\log(1 + n^{u}_b)}\frac{n_{b,i}}{\log(1 + n^{u}_i)} \end{aligned}未完待续。。。]]></content>
  </entry>
  <entry>
    <title><![CDATA[常见概率分布]]></title>
    <url>%2F2020%2F08%2F11%2F%E5%B8%B8%E8%A7%81%E6%A6%82%E7%8E%87%E5%88%86%E5%B8%83%2F</url>
    <content type="text"><![CDATA[概率相关的一些概念简介随机变量随机变量表示值不确定的变量，对于一个随机变量，可以从两个方面描述，第一是其取值范围，可以是连续区间或者离散的集合（连续型随机变量和离散型随机变量），第二是其概率函数，即用函数来表示不同取值的概率。 离散型随机变量的概率函数（概率质量函数）对于离散型随机变量，其概率函数将变量的取值映射为对应的概率，取值为$x$的概率表示为$p(x)$ 连续型随机变量的概率函数（概率密度函数）对于连续型随机变量，其概率函数被称为概率密度函数，表示的不是概率，而是概率在某个点位置的密集程度，不等于概率，因此其值可能大于1。在$x$处的概率密度表示为$p(x)$ 概率分布对于离散型变量，其概率分布可以表示为“离散型随机变量的值和概率的分布列表”，即表示为一个表格，也简称为“概率分布列”，需要列出所有情况和所有情况对应的概率。 对于连续型随机变量，其没有概率分布的概念，因为无法用一个概率分布列写出来，只能用概率密度函数对其进行描述。 概率分布函数不同于概率函数，概率分布函数描述的是一个范围内的概率，例如$F(x_k) = P(x \le x_k)$，对于离散型随机变量，其概率分布函数可以进一步写为$F(x_k) = P(x \le x_k) = \sum\limits_{x \le x_k} p(x)$，对于连续型随机变量，其概率分布函数可以进一步写为$F(x_k) = p(x \le x_k) = \int_{-\infty}^{x_k} p(x)dx$ 关于概率的一些概念简介到此结束，下面来看一些具体的概率分布。 连续型随机变量的分布均匀分布（Uniform Distribution）取值范围在连续区间$[a, b]$上的连续变量，每个值的概率都相等，其参数为$a, b$，均匀分布用$U$表示。 \begin{aligned} x &\sim U(a, b)\\ p(x|a,b) &= \frac{1}{b - a}\\ E(x) &= \frac{b + a}{2}\\ Var(x) &= \frac{(b - a)^2}{12} \end{aligned}高斯分布（Gaussian Dsitribution）中心极限定理说明：大量独立同分布的随机变量，其均值适当标准化之后会收敛到高斯分布。 服从高斯分布的随机变量取值范围为$(-\infty, \infty)$。 对于一元高斯分布，可以用两个参数来描述：$\mu$、$\sigma^2$，分别表示高斯分布的均值和方差。 \begin{aligned} x &\sim N(\mu, \sigma^2)\\ p(x) &= \frac{1}{\sqrt{2\pi} \sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}}\\ E(x) &= \mu\\ Var(x) &= \sigma^2\\ \end{aligned}对于多元高斯分布，如果其随机变量是$m$维向量，那么可以用均值向量$\mu$和协方差矩阵$\Sigma$来描述。 \begin{aligned} x &\sim N(\mu, \Sigma)\\ p(x) &= \frac{1}{(2\pi)^\frac{m}{2} |\Sigma|^\frac{1}{2}} e^{-\frac{(x-\mu)^T \Sigma^{-1}(x-\mu)}{2}}\\ E(x) &= \mu\\ Var(x) &= \Sigma = E((x - \mu) (x - \mu)^T)\\ \end{aligned}离散型随机变量的分布伯努利分布（Bernoulli Distribution）取值范围在离散集合$\{0, 1\}$上的离散型随机变量，取值1的概率为$\mu$，取值0的概率为$1 - \mu$。 \begin{aligned} x &\sim Bernoulli(\mu)\\ p(x | \mu) &= \mu^x(1-\mu)^{1-x}\\ E(x) &= \mu\\ Var(x) &= \mu(1 - \mu) \end{aligned}分类分布（Categorical Distribution）也叫范畴分布或者Multinoulli Distribution，这个分布是伯努利分布的一个推广，伯努利分布表示一次伯努利实验，得到两种状态，而分类分布表示一次实验，可以得到$C$种不同的状态，其变量可以表示为一个$C$维向量$x \in \{0, 1\}^C$其中每个维度的取值只可能是0或者1，并且满足有且只有一个位置取得1，即$\mathbf{1}^T x = \sum\limits_{c = 1}^C x_c = 1$，其参数包括不同状态的概率，表示为$\mu_c$。 在分类分布中，我们一般不关心其均值和方差。 \begin{aligned} x \in \{0, 1\}^C,\ \mathbf{1}^T x &= 1,\ x \sim multinoulli(\mu)\\ p(x | \mu) &= x^T \mu \\ E(x_c) &= \mu_c\\ Var(x_c) &= \mu_c(1 - \mu_c) \end{aligned}二项分布（Binomial Distribution）用于表示N次对$Bernoulli(\mu)$进行独立同分布采样之后，有m次得到1的概率分布，取值范围为$\{0, 1, 2, … , m\}$，如果$N = 1$，那么二项分布就是伯努利分布，二项分布用$Binomial(N, \mu)$表示。 \begin{aligned} x &\sim Binomial(N, \mu)\\ p(x = m) &= C_N^m \mu^m(1-\mu)^{N-m}\\ E(x) &= N\mu\\ Var(x) &= N\mu(1 - \mu) \end{aligned}多项分布（Nultinomial Distribution）类似于分类分布在伯努利分布上的扩展，如果在二项分布上将状态扩展至C个，那么N次采样之后，每个状态分别出现$m_c$次的概率分布即为多项分布，$\mathbf{1}^T m = N$，其参数包括$N$和$\mu$，表示采样次数和每次采样时，不同状态出现的概率，$\mathbf{1}^T \mu = 1$ \begin{aligned} m &\sim Nultinomial(N, \mu)\\ p(m) &= \frac{N!}{\prod\limits_{c=1}^C (m_c!)}\prod\limits_{c=1}^C\mu_c^{m_c}\\ E(m_c) &= N\mu_c\\ Var(m_c) &= N\mu_c(1 - \mu_c) \end{aligned}泊松分布（Poisson Distribution）一个随机事件发生的概率不随时间变化，而且单位时间内发生的平均次数记为$\lambda$，那么在单位时间内该事件发生的次数（取值可以为所有非负整数）服从参数为$\lambda$的泊松分布。 当二项分布中的$N$较大（例如大于20），而$\mu$较小，例如小于0.05时，可以用Poisson分布来近似二项分布。 \begin{aligned} x &\sim Poisson(\lambda)\\ p(x) &= \frac{\lambda^x e^{-\lambda}}{x!}\\ E(x) &= \lambda\\ Var(x) &= \lambda \end{aligned}几何分布每一次实验，事件发生概率为$\mu$，那么重复实验直到事件第一次发生，需要重复的实验次数服从几何分布。 \begin{aligned} p(x) &= \mu (1 - \mu)^{x-1}\\ E(x) &= \frac{1}{\mu}\\ Var(x) &= \frac{1-\mu}{\mu^2} \end{aligned}]]></content>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[attention机制总结]]></title>
    <url>%2F2020%2F06%2F09%2Fattention%E6%9C%BA%E5%88%B6%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[Attention机制最初是在自然语言处理领域取得了巨大成功，现在CNN中也在越来越多的使用Attention机制，这里就CNN中使用的Soft Attention（其中Attention weight是模型产生，可微可学习）做一些总结。 对Attention的理解Attention使得神经网络（NN）可以更多的关注当前任务相关的输入（不一定是原始输入，也可能是提取出的特征），同时忽略和当前任务不相关的输入，本质上是希望对不同的信息计算得到不同的权重，以代表不同信息的重要性程度，并通过加权和的方式聚合这些信息。 更抽象来说，在给定一个query元素的情况下，Attention机制的作用是：根据query元素将不同的key元素按照相关程度（或者重要程度）排序，在产生一个输出时，让模型可以选择哪些输入更加重要。这里query元素和key元素暂时没有合适的定义，不过可以通过两个例子来理解，例如在语言翻译问题中，这里的query element可以理解为输出语句中的某个单词，key element则是输入的语句中的某个单词，对于某个翻译得到的单词，不是每个输入单词都和其相关（或者说不是每个输入单词和这个翻译得到的单词的相关程度都一样），又例如在CNN中，query和key都可以理解为图像上某个像素或者某个区域。 在论文《An Empirical Study of Spatial Attention Mechanisms in Deep Networks》中提出了广义注意力机制的表达式（Generalized attention formulation），如下所示，其中$y_q$表示最终得到的Attention feature（通过Attention机制聚合得到的特征或者信息），$q$表示query元素的位置，$k$表示key元素的位置，$M$表示Attention Head的个数（不同的Attention Head可以理解为不同的Attention计算方式），$z_q$表示位置为$q$的query元素的内容，$x_k$表示位置为$k$的key元素的内容，$A_m$表示计算Attention weight的函数，$W_m$和$W’_m$表示两个可学习参数，这两个参数也可以去掉（即设置为固定值），$\Omega_q$表示query元素可以涉及到的key元素的范围（例如翻译任务中，一般这个范围是整个输入语句的所有单词） y_q = \sum\limits_{m=1}^M W_m[\sum\limits_{k\in \Omega_q}A_m(q, k, z_q, x_k)\odot W'_m x_k]上面的公式中$W’_m x_k$可以理解为首先对key元素做一个投影变换，得到合适的特征，然后$A_m$函数针对范围$\Omega_q$中的每个key元素$x_k$计算出其Attention weight（这个attention weight一般和q, k, z_q, x_k这几个值有关，但有些计算方式不会全部使用），按照Attention weight对key元素的投影进行一个加权求和，最后再进行一次投影变换，得到合适的Attention 特征，这就完成了一种Attention的过程，但是有些时候可以多种Attention一起使用，因此可以$M$种Attention feature求和得到最终的Attention feature。 根据上面的广义注意力机制表达式，设计一个注意力机制时，需要确定的内容就是一下几点： $\Omega_q$，一个query元素可以涉及到的key元素的范围。 $A_m$，计算Attention weight的函数。 $W_m$、$W’_m$，是否需要进行特征投影变换。 Self-AttentionSelf-Attention是指query元素和key元素来自于同一个集合的Attention，CNN中的Attention基本上都是Self-Attention，例如《Non-local Neural Networks》、《CBAM: Convolutional Block Attention Module》这些论文中使用的Attention，其query和key元素都是特征图上的像素，属于同一个集合，因此都属于Self-Attention的范围。 在论文《An Empirical Study of Spatial Attention Mechanisms in Deep Networks》中提到了几种Self-Attention的计算方式，如下图所示，这里有四种Attention weight的计算方式： query and key content表示仅仅使用$z_q$和$x_k$来计算Attention weight，一般其Attention weight正比于query content和 key content之间的相似性。 query content and relative position表示使用$z_q$和$k,q$来计算Attention weight。 key content only表示仅使用$x_k$来计算Attention weight。 relative position only表示仅使用$k,q$来计算Attention weight。 不同的Attention机制在广义Attention表达式上的表示Transformer attention在论文《Transformer-xl: Attentive lan-guage models beyond a fixed-length context》中使用了4个Attention Head，包含了上面提到的Self-Attention的四种计算方式，论文《An Empirical Study of Spatial Attention Mechanisms in Deep Networks》中对其进行了总结，如下： query and key content方式计算得到的Attention weight可以表示为$z_q^TU^T_mV^C_mx_k$，其中$U_m$和$V^C_m$表示可学习的嵌入矩阵，用于将query content和key content转换到合适的空间方便计算，或者缩减维度以减少计算量。 query content and relative position方式计算得到的Attention weight可以表示为$z_q^TU^T_mV^R_mR_{k-q}$，这里的$R_{k-q}$表示相对位置$k-q$的一种编码，$V^R_m$同样是一个可学习的嵌入矩阵。 key content only方式计算得到的Attention weight可以表示为$u_m^TV_m^Cx_k$，其中$u_m$是个可学习的向量，用于捕获对当前任务贡献突出的key content的编码。 relative position only方式计算得到的Attention weight可以表示为$v^T_mV^R_mR_{k-q}$，这里的$v_m$也是个可学习的向量，用于key元素和query元素的全局位置偏差。 Regular and deformable convolution常规卷积和可变形卷积都可以看做一种注意力机制。 在常规卷积中，Attention weight的计算函数$A_m$属于上面提到的relative position only方式，可以表达为如下形式，这里的$Q$表示卷积核覆盖的区域。这样的$A_m$实际上是一种基于query和key元素相对位置的函数，计算Attention weight的过程不需要$z_q$和$x_k$的值，计算出来的Attention weight只是指定哪些位置和query相关。 A_m(k, q) = \begin{cases} 1 & if \ k \in Q\\ 0 & otherwise \end{cases}另外，常规卷积相当于只有一个Attention head，因此$M=1$，$\Omega_q$可以是整个特征图上的所有像素，的卷积核参数可以看成是$W_0$，常规卷积没有$W’_0$，key元素则是输入的特征图上的像素点$x_k$，因此常规卷积可以看做Attention的一种特例。 对于可变形卷积，其$A_m$函数可以表达如下，其中$w_m^T$表示用于计算偏移的$1\times 1$卷积的参数，$p_m$表示正常卷积相对于卷积核中心的偏移，$q + p_m + w_m^T x_q$实际上就是可变形卷积首先计算得到的卷积核偏移值，而$G$是插值函数，表示在插值过程中，对于需要插值的位置$q + p_m + w_m^T x_q$计算得到的位置$k$的权重，因此可变形卷积的插值权重可以理解为Attention weight，所以可变形卷积核常规卷积一样，也可以看做是一种Attention。 A_m(q,k,x_q) = G(k, q + p_m + w_m^T x_q)CNN中的一些AttentionNon-local Neural Networks在论文《Non-local Neural Networks》中，提出了一种称为Non-local Neural Networks的结构，通过将某一位置的响应计算为所有位置特征的加权和的方式，用于捕获远程依赖，其表达式如下，其中$i$是响应的位置，$j$则遍历所有输入的位置，（这里的位置可以包含空间和时间，因此Non-local Neural Networks可以用于处理视频），$x_i$表示响应位置的输入值，$x_j$则可以遍历所有输入元素的值，$f(x_i, x_j)$计算得到一个标量，用于表示$x_i, x_j$之间的关系，例如相关程度，$g(x_j)$则表示对$x_j$的一种表示，例如一种投影，$C(x)$表示归一化系数，$y_i$是最终得到的输出。 y_i = \frac{1}{C(x)}\sum\limits_{\forall j}f(x_i, x_j)g(x_j)Non-local Neural Networks的结构示意图如下所示，其中$H,W$表示空间上的范围，$T$表示时间上的范围，这里的示例输入形状是$T\times H \times W \times 1024$，如果是视频处理的话，即表示一共处理相邻的$T$帧特征图，每幅特征图高$H$，宽$W$，channel个数为1024，上面表达式中的$f(x_i, x_j)$由下图中的$\theta(x) \times \phi(x)$来完成，归一化则由softmax操作来完成，softmax的结果和$g(x)$的结果相乘，就完成了“所有位置特征的加权和操作”，得到响应$y$，表示捕获到的远程依赖，这样一来，$y$中每个特征值都聚合了全局的信息，再进行一些处理转换，最终和原始输入相加得到输出Z。 上图中的$y$最终还经过了一次$1\times 1\times 1$卷积得到$z$，因此可以表达为：$z_i = W_z y_i + x_i$，这里论文中将$W_z$初始化为0，且加上$x_i$这一项，其原因是为了在改变网络结构之后，还可以利用之前的预训练权重。 整个Non-local Neural Networks过程虽然简单，但是也是Self-Attention的一种特例，类比之前提到的广义注意力机制表达式，这里只有一个Attention Head，$M=1$，$i$可以看做query元素的位置，$j$则是key元素的位置，query元素和key元素都属于同一个集合（即输入x），$x_i$表示query content，$x_j$则表示key content，$f(x_i, x_j)$则是query and key content方式的$A_m$函数（$A_m$计算得到的Attention weight是归一化的，因此$A_m$还包括$\frac{1}{C(x)}$用于归一化）。$g(x_j)$是对key content的一种变换，其参数就是前面的$W’_m$，最后这里没有$W_m$，softmax的结果和$g(x)$的结果进行的矩阵乘法完成了特征加权和的操作。 Squeeze-and-Excitation Networks（SE Network）在论文《Squeeze-and-Excitation Networks》中，提出了一种用于channel attention的Squeeze-and-Excitation（SE）结构，通过显式地建模通道之间的相互依赖关系，自适应地重新修正不同通道的特征响应幅度，其结构示意图如下所示。其中$F_{sq}$表示Squeeze操作，在论文中使用一个全局平均池化来实现，用于在空间维度上来聚合全局的信息，变成一种逐通道的表示状态。之后的$F_{ex}$表示Excitation操作，其操作顺序为（全连接-ReLU-全连接-Sigmoid，这里没看懂可以参考下面在InceptionNet和ResNet中的详细结构），用于捕获通道之间的依赖性关系，并且形成通道的权重。最终将得到的各通道权重分别乘以原来的通道，达到对不同通道的响应幅度进行自适应修正的目的。 论文中在InceptionNet以及ResNet中都尝试加入SE模块，其加入之后的结构如下图所示。其中Global pooling即Squeeze操作，用于聚合超出像素点感受野之外的信息并形成逐通道的表示，后面紧接着Excitation操作，首先通过全连接对其进行降维，减少计算量，之后使用ReLU激活函数来增加Excitation操作的非线性性质，之后再通过一个全连接层得到逐通道的channel weight，并通过Sigmoid函数将channel weight限制在0到1之间，最后将channel weight与原始特征图相乘，每个通道一个权重，用于调整不同通道的响应幅值。 Excitation操作很容易和之前的广义注意力机制表达式类比，其本质上就是一种简单的Self-Attention，只不过Excitation操作计算出的Attention weight除了query位置以外全是0，这样就不是按照权重去聚合所有通道的信息，而是使用空间维度的全局信息来调整通道的响应幅值。 Convolutional Block Attention Module论文《CBAM: Convolutional Block Attention Module》中提出了一种同时进行channel和spatial attention的attention模块，称为CBAM。 CBAM中包含了两种Attention，如下图所示，上面的结构是channel Attention部分，其接收输入之后分别通过空间维度上的全局最大池化和全局平均池化，和SE模块中的Squeeze类似，只不过这里使用两种池化，得到了两个结果，这两个结果再送到同一个MLP（多层感知机），这里相当于两个共享权重的Excitation操作，最后两个结果相加在进行sigmoid操作，这里可以看出来，整个Channel Attention过程其实就是稍微改变的SE模块。图片下面的结构是Spatial Attention的结构，这里的最大池化和平均池化是在Channel维度进行的，即对每个像素求出所有Channel的最大值或者平均值，最终得到两个Channel数量为1的特征图，两个特征图进行Concatenate之后经过一个$7\times7$卷积，最后使用sigmoid函数处理得到结果，这两个模块得到的结果都是用于和原始特征图相乘，一个是逐通道的调整特征响应的幅值，一个是逐像素的调整特征响应的幅值。 上面展示的两种结构在模型中先后使用，可以使得模型包含对不同channel响应幅度的调整能力的同时也包含对不同位置响应幅度的调整能力，在ResBlock中添加CBAM结构之后，模型的结构如下图所示。 在该论文的实验中，CBAM模块在图像分类任务上提升不大，但是使用轻量级模型（MobileNet，ShuffleNet）试验时错误率相对于SE模块有一个百分点左右的下降（可能是参数增加，模型容量增大导致？），在目标检测任务上，ResNet+CBAM相比于原始的ResNet有一点几个百分点的mAP提升。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[决策树总结]]></title>
    <url>%2F2020%2F05%2F29%2F%E5%86%B3%E7%AD%96%E6%A0%91%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[决策树基础知识信息熵如果某个事件，有$C$种可能，其中第$k$中可能状态发生概率为$p_k$，那么该事件的信息熵定义为$Ent(D) = -\sum\limits_{k=1}^C p_k\log_2p_k$，表示的是所有状态的信息的期望，以2为底数这样的定义的信息熵单位是bit。 在决策树方法中，也类似的定义信息熵：样本集合$D$中第$k$类样本所占比例为$p_k$，则信息熵定义为$Ent(D) = -\sum\limits_{k=1}^C p_k\log_2p_k$，其中$C$为类别个数，其刻画了样本集合的纯度。 信息增益假设离散属性$a$有$v$个取值：$a_1, a_2, …, a_v$，可以将当前数据集合分成$V$个子集：$D^1, D^2, …, D^V$，那么使用属性$a$划分样本集的信息增益定义为$Gain(D,a)=Ent(D)-\sum\limits_{v=1}^V\frac{|D^v|}{|D|}Ent(D^v)$ 增益率信息增益的定义导致其对数量较多的$D^v$更加敏感，因此又提出了信息增益率的概念：$Gain_ratio(D,a)=\frac{Gain(D,a)}{IV(a)}$，其中，$IV(a)=-\sum\limits_{v=1}^V \frac{|D^v|}{|D|} \log_2\frac{|D^v|}{|D|}$称为属性$a$的固有值（或者叫内在价值）。 基尼指数基尼值定义为$Gini(D) = \sum\limits_{k=1}^C\sum\limits_{k’ \ne k}p_k p_{k’} = 1-\sum\limits_{k=1}^Cp_k^2$，其反映了在$D$中随机抽取两个样本，不属于同一类别的概率。和信息熵增益类似，对于在属性$a$上划分出$V$个区间的操作，定义划分后的基尼指数为$Gini_index(D, a) = \sum\limits_{v=1}^V\frac{|D^v|}{|D|}Gini(D^v)$，构造决策树时的最优划分属性选择也可以使用能够使基尼指数最小的属性。 决策树的构造算法ID3算法每次选择一个信息增益最大的属性$a$构造节点，这个节点将数据划分为$V$个子集，然后在每个子集中进行同样的操作（不能选择已经使用过的属性），若无属性可用或者当前子集类别相同，则当前子集标记为类别数量最多的类别，停止继续划分。 C4.5算法由于ID3算法在选择属性时使用信息增益最大值来选取，因此这样构造的决策树偏向于先选择特征取值更多的属性，另外ID3算法还有一个问题是处理连续值属性，这两个问题在C4.5算法中都给出了解决方案。 C4.5算法则是对ID3算法的一个改进版本，其中使用增益率来进行属性选择。 对连续值属性的处理在遇到连续值时，如果连续属性$a$在$D$中出现$n$个取值，则将其从小到大排序为$\begin{bmatrix}a_1, a_2, … a_n\end{bmatrix}$，这样产生$n-1$个离散值$T_a = \{\frac{a_i + a_{i+1}}{2}|1 \le i \le n-1\}$则$Gain(D,a)= \max\limits_{t \in T_a}Gain(D, a, t)$，其中$Gain(D, a, t)$表示将$a$属性使用$t$划分为两部分，这样连续值的决策树构造和离散值类似了，不过连续值属性在构造决策树的过程中可以使用多次。 分类回归树(Classification And Regression Tree, CART)算法对于分类树，CART算法使用基尼指数最小化准则进行特征选择，CART的主要思想是每次将数据划分成两个单元，例如，对于当前的数据集合$D$，根据属性$a$上的取值$s$数据划分成$D^1$和$D^2$两个子集（对于连续变量，划分数据就用大于和小于关系，对于离散变量就是用等于或不等于关系），对于这样的划分，可以得到基尼指数$Gini_index(D, a, s) = \frac{|D^1|}{|D|}Gini(D^1) + \frac{|D^2|}{|D|}Gini(D^2)$，每次划分都选择使得$Gini_index(D, a, s)$最小的$a$和$s$。如此循环，即得到了CART分类树。 不同于ID3和C4.5算法，CART算法还能够处理回归树。对于回归树，其样本标签是连续值，回归树的一般形式表示如下，其中$H(x)$表示回归树的预测函数，$\mathbf{1}$表示指示函数，其基本思想是根据数据的属性，将数据划分成$M$个部分$R_m, m=1,2,…,M$，每个部分对应一个输出值。 H(x) = \sum\limits_{i=1}^M c_i \mathbf{1}(X \in R_i)CART回归树使用平方误差作为每个部分的损失函数，每次利用属性$a$上的取值$s$将数据划分成两个部分：$R^1(a, s)$和$R^2(a, s)$，按照下式的规则来选择每次划分的$a$和$s$： \min\limits_{a, s}[\min\limits_{c_1} \sum\limits_{x_i \in R^1(a,s)} (y_i - c_1)^2 + \min\limits_{c_2} \sum\limits_{x_j \in R^2(a,s)} (y_j - c_2)^2]其中c的值非常好计算，直接使用该数据子集对应标签的平均数即可。 属性缺失的处理令$\tilde{D}$是所有没有缺失属性a的样本集合，对于样本$x$，有样本权重$w_x$，定义如下参数。 \rho = \frac{\sum_{x\in \tilde{D}} w_x}{\sum_{x\in D}w_x}\\ \tilde{p}_k = \frac{\sum_{x\in \tilde{D}_k w_x}}{\sum_{x\in \tilde{D}}w_x}, (1\le k \le C)\\ \tilde{r}_v = \frac{\sum_{x\in \tilde{D}^v}w_x}{\sum_{x \in \tilde{D}} w_x}, (1 \le v \le V)显然，$\rho$表示属性无缺失样本所占比例，$\tilde{p}_k$表示属性无缺失样本中第$k$类所占比例，$\tilde{r}_v$表示属性无缺失样本中在属性$a$上取值$a^v$的样本比例。 由此推广信息增益为： \begin{aligned} Gain(D, a) &= \rho \times Gain(\tilde{D}, a)\\ &=\rho \times (Ent(\tilde{D}) - \sum\limits_{v=1}^V \tilde{r}_v Ent(\tilde{D}^v)) \end{aligned}其中： Ent(\tilde{D}) = -\sum\limits_{k=1}^C \tilde{p}_k log_2 \tilde{p}_k这样解决了最优划分的属性选择问题，在构造子树时，如果样本$x$在属性$a$上的取值已知，那么$x$划分到相应子节点，且权重保持为$w_x$，如果属性$a$未知，则将$s$划分入所有的子节点，且权重调整为$\tilde{r}_v w_x$。 多变量决策树叶节点不再针对某个属性，而是针对属性的线性组合进行划分。 决策树的剪枝预剪枝在生成决策树的过程中，利用验证集验证当前划分是否能提升泛化效果，如果不能，则停止继续划分，直接将当前样本集作为决策树叶节点，并指定数量最多的类别做为叶节点类别。预剪枝训练时间短，但是由于其是贪心策略，容易引起欠拟合。 后剪枝在决策树生成完成之后，自底向上的对非叶节点进行考察，观察将该节点直接指定为叶节点是否能提升验证集泛化效果，如果能则将此节点的子树一起合并为一个叶节点。后剪枝的训练时间比预剪枝长很多，但是其欠拟合风险小，而且泛化性能往往优于预剪枝。]]></content>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Boosting总结]]></title>
    <url>%2F2020%2F05%2F28%2FBoosting%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[BoostingBoosting是集成学习的一类算法的总称，这类算法的流程可以概括为：首先从初始训练集中训练出一个基学习器，然后根据基学习器的表现对训练样本分布进行调整，使得之前做错的样本在后面受到更多的关注，然后基于调整后的样本分布来训练下一个基学习器，如此逐步增加基学习器，直到基学习器的数目达到了指定值T。 AdaBoostAdaBoost是Boosting方法中的一个代表，主要应用在二分类任务上，AdaBoost可以理解为将基学习进行线性组合得到最终的学习器，其中$\alpha_t$是一个正数，表示第$t$个基学习器的权重，如下所示。 H(x) = \sum\limits_{t=1}^T \alpha_t h_t(x)AdaBoost使用指数损失函数作为优化目标，如下所示，其中$f(x)$表示真实函数，$f(x) \in \{1, -1\}$，优化这个指数损失函数其实相当于优化0/1损失函数（证明略），因为指数损失函数的平滑性质更利于优化过程，因此这里用指数损失函数，其中$E$表示期望，$D$表示原始数据的分布（每个数据出现的概率是$\frac{1}{N}$，$N$为数据个数）。 l(H|D) = E_{x \sim D}[e^{-f(x)H(x)}]因此AdaBoost的目标可以表示为： \mathop{\arg\min}\limits_{\alpha, h} E_{x \sim D}[e^{-f(x)(\sum\limits_{t=1}^T \alpha_t h_t(x))}]但是这个问题的直接求解非常困难，因此这里使用一种前向分步算法来解决这个问题，其思路是：每增加一个基学习器，就让损失函数减小一些，这样来逐步逼近最优解。 假设现在已经得到$m$个基学习器组成的集成学习器：$H_m(x)$，现在下一个学习器（$\alpha_{m+1}, h_{m+1}$）的目标可以表示为： \begin{aligned} &\mathop{\arg\min}\limits_{\alpha_{m+1}, h_{m+1}} E_{x \sim D}[e^{-f(x)(H_m(x) + \alpha_{m+1} h_{m+1}(x))}]\\ &=\mathop{\arg\min}\limits_{\alpha_{m+1}, h_{m+1}} E_{x \sim D}[e^{-f(x)(H_m(x)} \times e^{-f(x) \alpha_{m+1} h_{m+1}(x))}] \end{aligned}$H_m(x)$和$\alpha_{m+1}, h_{m+1}$无关，因此可以将$e^{-f(x)(H_m(x)}$看做数据的一种权重，即使用$e^{-f(x)(H_m(x)}$这一项来改变数据的分布，假设这样改变数据权重之后，得到的数据分布为$D_m$，那么可以进一步将目标化简为： \mathop{\arg\min}\limits_{\alpha_{m+1}, h_{m+1}} E_{x \sim D_m}\ e^{-f(x) \alpha_{m+1} h_{m+1}(x)}因为$f(x)$和$h_m{x}$的取值要么是1，要么是$-1$，又因为$\alpha_m$为正数，因此这里如果要最小化目标，可以先不考虑$\alpha_m$，而是先最小化： \mathop{\arg\min}\limits_{h_{m+1}} E_{x \sim D_m}\ e^{-f(x)h_{m+1}(x)}因此第$m+1$个基学习器的目标就是在数据分布$D_m$上优化指数损失函数： \begin{aligned} h^\star_{m+1} &= \mathop{\arg\min}\limits_{h_{m+1}} E_{x \sim D_m}\ e^{-f(x)h_{m+1}(x)}\\ &=\mathop{\arg\min}\limits_{h_{m+1}} \sum\limits_{i=1}^N w_{mi}I\{f(x_i) \ne h_{m+1}(x_i)\} \end{aligned}用$w_{mi} = \frac{e^{-f(x_i)(H_m(x_i)}}{\sum\limits_{i=1}^N e^{-f(x_i)(H_m(x_i)}}$来表示数据$x_i$的权重，这里多出来了一个用于权重归一化的分母，但是不影响$\alpha_{m+1}，h_{m+1}$的训练，只是后面推导会方便些。 第$m+1$个基学习器$h^\star_{m+1}$得到之后，再来确定其权重$\alpha_{m+1}$，其目标是： \begin{aligned} &\mathop{\arg\min}\limits_{\alpha_{m+1}} E_{x \sim D_m}\ e^{-f(x) \alpha_{m+1} h^\star_{m+1}(x)}\\ &=\mathop{\arg\min}\limits_{\alpha_{m+1}} \sum\limits_{i=1}^N w_{mi} e^{-f(x) \alpha_{m+1} h^\star_{m+1}(x)} \end{aligned}求损失函数$l(\alpha_{m+1}) = \sum\limits_{i=1}^N w_{mi} e^{-f(x) \alpha_{m+1} h^\star_{m+1}(x)}$对于$\alpha_{m+1}$的导数，可以得到： \begin{aligned} \frac{\partial l(\alpha_{m+1})}{\partial \alpha_{m+1}} &= \sum\limits_{f(x_i) \ne h_{m+1}(x_i)} w_{mi}e^{\alpha_{m+1}} - \sum\limits_{f(x_i) = h_{m+1}(x_i)} w_{mi}e^{-\alpha_{m+1}} \end{aligned}令导数$\frac{\partial l(\alpha_{m+1})}{\partial \alpha_{m+1}} = 0$可得： \begin{aligned} \sum\limits_{f(x_i) \ne h_{m+1}(x_i)} w_{mi}e^{\alpha_{m+1}} &= \sum\limits_{f(x_i) = h_{m+1}(x_i)} w_{mi}e^{-\alpha_{m+1}}\\ e^{2\alpha_{m+1}} &= \frac{\sum\limits_{f(x_i) = h_{m+1}(x_i)} w_{mi}}{\sum\limits_{f(x_i) \ne h_{m+1}(x_i)} w_{mi}} \end{aligned}容易发现，因为$w_{mi}$是经过了归一化的，因此$\sum\limits_{i=1}^N w_{mi} = 1$，那么可以得到$\sum\limits_{f(x_i) = h_{m+1}(x_i)} w_{mi} = 1 - \sum\limits_{f(x_i) \ne h_{m+1}(x_i)} w_{mi}$，这里令$\sum\limits_{f(x_i) \ne h_{m+1}(x_i)} w_{mi} = e_{m+1}$表示$h_{m+1}$的损失，那么$\sum\limits_{f(x_i) = h_{m+1}(x_i)} w_{mi}$可以表示为$1 - e_{m+1}$，代入上面的导数为0的推导，可以得到： \begin{aligned} e^{2\alpha_{m+1}} &= \frac{1 - e_{m+1}}{e_{m+1}}\\ \Rightarrow \alpha_{m+1} &= \frac{1}{2}\ln{\frac{1 - e_{m+1}}{e_{m+1}}} \end{aligned}因此AdaBoost的流程就很明确了： 在数据分布$D_{m-1}$上训练基学习器$h_m(x)$ 计算$e_m = \sum\limits_{f(x_i) \ne h_m(x_i)} w_{mi}$ 确定当前基学习器的权重$\alpha_m = \frac{1}{2}\ln{\frac{1 - e_m}{e_m}}$ 得到新的学习器$H_m(x) = \sum\limits_{i=1}^m \alpha_i h_i(x)$ 如果学习器个数达到指定个数，则退出，否则使用新的学习器调整权重参数$w_{mi} = \frac{e^{-f(x_i)(H_m(x_i)}}{\sum\limits_{i=1}^N e^{-f(x_i)(H_m(x_i)}}$得到分布$D_m$，跳转到第一步，进行第$m+1$个基学习器的训练。 提升树（Boosting Decision Tree, BDT）残差树是针对回归问题的一种boosting方法。其基学习器是基于CART算法的回归树（关于决策树的相关内容可以见我的另外一篇文章决策树总结），模型依旧为加法模型、损失函数为平方函数、学习算法为前向分步算法。 第$m$个树模型基学习器可以表示为$T(x; \theta_m)$，得到前$m$个基学习器之后，残差树的预测函数可以看做：$H_m(x) = \sum\limits_{i = 1}^mT(x; \theta_i)$，那么下一个树模型$T(x; \theta_{m+1})$的目标损失函数可以写作： \begin{aligned} L(y, T(x; \theta_{m+1})) &= (y - H_m(x) - T(x; \theta_{m+1}))^2\\ &= (r - T(x; \theta_{m+1}))^2 \end{aligned}这里的$r$表示上一次的残差，这也是残差树名字的由来，例如现在需要拟合的值为20，第一次残差树拟合的值为18，那么第二次拟合的目标值为上一次的残差：20-18=2。 梯度提升决策树（Gradient Boosting Decision Tree, GBDT）在提升树的训练过程中，每次增加的决策树是以残差作为目标，并使用CART方法构造决策树，而在GBDT中，添加新的决策树是以损失函数在当前模型预测值下的负梯度作为目标，同样使用CART方法构造决策树。如果损失函数使用的是均方误差，那么新的决策树的优化目标其实和提升树类似。 其实严格来将，GBDT并不是去拟合梯度，而是在进行决策树构建时，尽量将梯度相近的样本划分到同一个叶节点。 例如对于损失函数$\sum\limits_{i=1}^N l(y_i, \hat{y}_i)$，其中$N$为样本个数，$y_i$表示第$i$个样本的目标值，$\hat{y}_i$表示对第$i$个样本的预测值，现在GBDT已经构造出了第$t$颗决策树，目前的预测函数表示为$F_t(x) = \sum\limits_{i=1}^t f_i(x)$，这里每个$f_i(x)$表示一个决策树，现在的损失函数表示为$\sum\limits_{i=1}^N l(y_i, F_t(x_i))$，现在要构造第$t+1$颗树，那么其目标很简单，就是进一步使得损失函数$\sum\limits_{i=1}^N l(y_i, F_t(x_i) + f_{t+1}(x_i))$最小，这里将损失函数进行一次泰勒展开，可以将损失函数近似为： \begin{aligned} \sum\limits_{i=1}^N [l(y_i, F_t(x_i)) + \frac{\partial l(y_i, F_t(x_i))}{\partial F_t(x_i)}f_{t+1}(x_i)] \end{aligned}其中$l(y_i, F_t(x_i))$的值和$f_{t+1}$无关，因此$f_{t+1}$只关心下面的表达式是否能够进一步将损失函数减小： \sum\limits_{i=1}^N \frac{\partial l(y_i, F_t(x_i))}{\partial F_t(x_i)}f_{t+1}(x_i)但是上面这个函数没有最小值，而且损失函数的一次展开是有误差的，$f_{t+1}$的预测值越大，误差越大，不能按照最小化问题来处理，自然的一个想法是使得$f_{t+1}(x_i) = -\frac{\partial l(y_i, F_t(x_i))}{\partial F_t(x_i)}$，因此GBDT这里将其简化为拟合负梯度。 XGBoostXGBoost是GBDT的一种改进版本。 在已知第$t$颗决策树时，下一个决策树的损失函数为：$\sum\limits_{i=1}^N l(y_i, F_t(x_i) + f_{t+1}(x_i)) + \Omega(f_{t+1})$。 这里损失函数中新增了正则化项$\Omega(f_{t+1})$，可以表示为：$\Omega(f_{t+1}) = \gamma T + \frac{1}{2}\lambda\sum\limits_{j=1}^Tw^2_j$，其中$T$表示第$t+1$颗树中的叶节点个数，而$w_j$表示第$j$个叶节点的输出值。 在XGBoost中，对损失函数进行二次泰勒展开近似： \begin{aligned} &\sum\limits_{i=1}^N l(y_i, F_t(x_i) + f_{t+1}(x_i)) + \Omega(f_{t+1})\\ &= \sum\limits_{i=1}^N l(y_i, F_t(x_i) + f_{t+1}(x_i)) + \gamma T + \frac{1}{2}\lambda\sum\limits_{j=1}^Tw^2_j\\ &\simeq \sum\limits_{i=1}^N [l(y_i, F_t(x_i)) + \frac{\partial l(y_i, F_t(x_i))}{\partial F_t(x_i)}f_{t+1}(x_i) + \frac{1}{2}\frac{\partial^2 l(y_i, F_t(x_i))}{\partial^2 F_t(x_i)}f^2_{t+1}(x_i)] + \gamma T + \frac{1}{2}\lambda\sum\limits_{j=1}^Tw^2_j\\ &=constant + \sum\limits_{i=1}^N [\frac{\partial l(y_i, F_t(x_i))}{\partial F_t(x_i)}f_{t+1}(x_i) + \frac{1}{2}\frac{\partial^2 l(y_i, F_t(x_i))}{\partial^2 F_t(x_i)}f^2_{t+1}(x_i)] + \gamma T + \frac{1}{2}\lambda\sum\limits_{j=1}^Tw^2_j\\ &=constant + \sum\limits_{j=1}^T [\sum\limits_{i\in I_j}\frac{\partial l(y_i, F_t(x_i))}{\partial F_t(x_i)}w_j + \sum\limits_{i\in I_j}\frac{1}{2}\frac{\partial^2 l(y_i, F_t(x_i))}{\partial^2 F_t(x_i)}w_j^2] + \gamma T + \frac{1}{2}\lambda\sum\limits_{j=1}^Tw^2_j\\ &=constant + \sum\limits_{j=1}^T[\sum\limits_{i\in I_j} g_i w_j + \frac{1}{2}(\sum\limits_{i\in I_j}h_i + \lambda)w^2_j] + \gamma T\\ &=constant + \sum\limits_{j=1}^T[G_j w_j + \frac{1}{2}(H_j + \lambda)w^2_j] + \gamma T \end{aligned}这里的化简过程中，$I_j$表示属于第$j$个叶节点的样本id的集合，$g_i$表示第$i$个样本在$H_t(x_i)$这个预测结果下的一阶导数：$\frac{\partial l(y_i, F_t(x_i))}{\partial F_t(x_i)}$，$h_i$表示第$i$个样本在$F_t(x_i)$这个预测结果下的二阶导数：$\frac{\partial^2 l(y_i, F_t(x_i))}{\partial^2 F_t(x_i)}$，$G_j$表示属于第$j$个叶节点的样本的导数之和，$H_j$表示属于第$j$个叶节点的样本的二阶导数之和。 得到上面的化简结果之后，首先对$w_j$做个处理，令上式对$w_j$的导数为0，可以得到$w^\star_j = -\frac{G_j}{H_j + \lambda}$就是第$j$个叶节点的预测值的最优解，因此上式可以进一步写成： constant - \sum\limits_{j=1}^T \frac{1}{2}\frac{G^2_j}{H_j + \lambda} + \gamma TXGBoost在构造新的决策树时，和CART类似，将当前节点分裂成两个子节点，但是选择特征以及特征的最优划分，不是使用Gini_index，为了最小化上面的目标，XGBoost在划分新的叶节点时，判断是否将叶节点$j$划分为新的左节点和右节点时，主要考察下式： \begin{aligned} &constant - \frac{1}{2}\frac{G^2_j}{H_j + \lambda} + \gamma T - (constant - \frac{1}{2}\frac{G^2_L}{H_L + \lambda} - \frac{1}{2}\frac{G^2_R}{H_R + \lambda} + \gamma(T+1))\\ &= \frac{1}{2}( \frac{G^2_L}{H_L + \lambda} + \frac{G^2_R}{H_R + \lambda} - \frac{G^2_j}{H_j + \lambda}) - \gamma \end{aligned}这里$G_L， H_L$分别表示划分出的左叶节点的导数和二阶导数和，$G_R， H_R$分别表示划分出的右叶节点的导数和二阶导数和。上式的结果表示将叶节点$j$划分为新的左节点和右节点时的增益情况。如果上式为正，则表示新划分节点有助于降低损失函数值，所以这里创建新节点时，遍历所有特征以及特征的划分位置，找到使得上式为正且最大的划分来创建新节点，如果没有能够使得上式为正的划分，那么停止继续添加叶节点。 按照上面的方法，逐个构造新的决策树以降低损失函数值，就是XGBoost的主要工作原理。]]></content>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[目标检测]]></title>
    <url>%2F2020%2F05%2F19%2F%E7%9B%AE%E6%A0%87%E6%A3%80%E6%B5%8B%2F</url>
    <content type="text"><![CDATA[目标检测任务简单来说就是给定一张图片，模型需要根据图片找到感兴趣的目标，将其位置、大小标注出来。 两阶段检测算法，从RCNN到Faster R-CNN目前的目标检测方法主要分为：两阶段的检测器、单阶段检测器两种，RCNN到Faster RCNN系列检测器是典型的两阶段检测器的代表。 R-CNN论文《Rich feature hierarchies for accurate oject detection and semantic segmentation》中提出了R-CNN模型用于目标检测，其基本流程如下： 使用Selective Search算法提取出2k个region proposal。 对于每个region proposal，先裁剪出对应区域的图片，然后使用AlexNet进行特征提取（特征取最后那个softmax层的输入，是一个4096维的向量）。 训练K个线性SVM分类器，对每个region proposal进行K次分类，得到其属于K个类别的得分。 对每个类别的proposal进行NMS后处理，得到最终的结果。 这里的Selective Search算法本质上是一种先分割图片然后再尝试各种组合的穷举算法，这里没有详细研究。 整体来说，R-CNN的流程非常粗暴，主要有两个阶段：1、获取region proposal，2、对region proposal进行分类，但这也是后面的两阶段检测方法的一般流程。 SPP-net在R-CNN中存在的一个问题是region proposal的大小不相等，但是AlexNet要求固定大小的输入图片，否则特征维度会发生变换（如果现在来看这个问题的话，加个global pooling就OK了，但是当时没想到），因此在RCNN里面只能通过缩放，让region proposal满足输入大小的要求，这样带来了畸变的问题，为了解决这个问题，论文《Spatial Pyramid Pooling in Deep Convolutional Networks for Visual Recognition》提出了空间金字塔池化操作(Spatial Pyramid Pooling，SPP)，而且顺便结局了RCNN中对每个region proposal都提取一次特征带来的重复计算问题。 在SPP-net中，首先将整幅图作为输入（这里使用的模型不再是AlexNet，这里试验了ZF-5、Convnet*-5、Overfeat-5、Overfeat-7这几个模型，我这里不再详细研究），提取出特征图之后，再从特征图上抠出来region proposal对应的区域（这里不同于RCNN对每个region proposal进行一次特征提取，所以解决了重复计算问题），然后将抠出来的区域进行空间金字塔池化操作，使其变成固定大小的特征向量。再使用SVM对特征进行分类。 SPP操作的示意图如下所示，主要思想是将特征图先划分成固定数量的网格，然后再每个格子内进行池化操作，最后将固定大小的池化结果展开、拼接成固定大小的特征向量。 Fast R-CNN有了SPP-net的思路之后，RCNN也做出了相应的改变，在论文《Fast R-CNN》中，接纳了SPP-net中先全图特征提取再裁剪region proposal区域的方法，然后对SPP-net进行改进： 只使用一个固定大小的网格分割，称为ROI Pooling层（并提出了ROI Pooling层的反向传播方法）。（至于为什么要降SPP改成ROI Pooling，可能主要是ROI Pooling有反向传播功能，其实如果SPP也实现了反向传播，那么ROI Pooling不一定比SPP效果好，不知道有没有人进行过对比） 特征提取模型换成了VGG。 对特征的处理采用神经网络，不再使用SVM，对于分类，使用softmax函数得到类别概率。 特征除了用于分类之外，还用于更精确的位置回归（使用smooth L1损失），并且让这两个任务共享一部分全连接层，提出了多任务损失函数的训练方法。 Fast R-CNN的简略流程示意图如下所示： Faster R-CNNFast R-CNN还是没有做到完全的端到端得到输出结果，因为还是要依赖Selective Search算法来生成region proposal。 在论文《Faster R-CNN: Towards Real-Time Object Detection with Region Proposal Networks》中，提出了Faster R-CNN，其中使用一个区域建议网络（Region Proposal Network，RPN）来替代Selective Search算法进行region proposal工作。 RPN的思路应该来源于Fast R-CNN中的位置回归操作，Fast R-CNN中证明神经网络可以用于位置回归问题，因此自然就会有一个想法是使用神经网络来对输出候选位置。但是如果直接回归位置，有两个问题： 特征图上某个像素点，应该回归哪个目标的位置（在之前的方法中，位置回归的目标很明确，因为region proposal有了之后，查找数据标签，可以很容易知道对于这个位置应该去回归哪个目标，或者这里面没有目标，那么就不用回归位置） 如果是输出像素坐标，那么对于不同大小的图片，不同尺度和比例的目标，回归任务很难收敛。 对于上面两个问题，这里提出了Anchor的机制，首先在图片上按照一定的规律分布好各种尺度和比例的方框（Anchor box，一个像素点可以有多个Anchor box），然后每个像素点（Anchor）输出anchor box的类别（前景还是背景）、缩放和偏移量。 每个anchor box要么匹配一个目标（其分类类别为这个目标的类别且回归目标为这个目标的位置），要么在训练过程中将其分类为背景类，并且不计算其回归输出的loss。 RPN和也是用和RCNN一样的特征提取网络（这个特征提取网络一般称为Backbone）。 为什么要使用Smooth L1作为回归任务的损失函数在回归损失函数中，最常用的是L2损失函数，$L = (h(x;\theta) - y) ^ 2$，其导数$\frac{\partial L}{\partial \theta} = \frac{\partial h(x;\theta)}{\partial \theta}(h(x;\theta) -y)$，这里的问题在于：如果模型初始化得不好，再训练开始时$h(x;\theta) - y$特别大，则会导致梯度特别大，模型将难以训练。那么一个自然地想法是使用L1损失函数，但是L1损失函数不是处处可导，梯度会发生突变，也不利于训练，因此将L1损失函数进行一些平滑操作，给出了smooth L1损失函数如下所示： \begin{aligned} L(\theta) = \begin{cases} 0.5 x^2 &|x| < 1\\ |x| - 0.5 &|x| >= 1 \end{cases} \end{aligned}RPN训练时的loss计算需要对负样本进行采样RPN的训练目标是将Anchor Box进行前景和背景分类，并且对分类为前景的Anchor Box进行位置偏移量和缩放量的回归。 在训练时，需要首先分配Anchor Box的训练目标：给定一张图像样本上的所有gt box标签，首先需要计算所有gt box和每个Anchor Box的iou。 对于每一个Anchor Box，找到和其iou最大的gt box，按照和这个gt box之间的iou，有三种可能： 如果大于某个iou阈值，那么则被分配给这个gt box作为前景来训练（即这个Anchor Box的分类目标是正样本，回归目标是对应的gt box）。 如果小于某个iou阈值，则作为背景类处理（即这个Anchor Box的分类目标是负样本，没有回归目标，不计算回归损失）。 如果iou不大又不小，则忽略这个Anchor Box，计算损失的过程中不考虑，这是因为一些Anchor Box包含了一部分目标，但是又包含得不够多，不能武断的将其作为前景或者背景来训练，因此干脆不管这一部分的训练，反正参数是共享的，其他的地方训练好之后，测试过程中这种地方输出是什么样影响不大。 在分配Anchor Box训练目标的过程中，绝大部分Anchor Box是被分配成负样本（背景类）的，，因此RPN训练时存在前景和背景两个类别不平衡的情况。所以一般的Faster R-CNN实现中，RPN的训练部分，会对Anchor Box进行采样。(例如mmdetection中的实现中，RPN部分的默认配置是采样的256个Anchor Box进行训练，首先随机采样128个正样本，然后采样128个负样本，如果正样本不够，那么负样本多采样一些填满256个) 多阶段检测器Cascade R-CNN在论文《Cascade R-CNN: Delving into High Quality Object Detection》中指出了Faster R-CNN中按照某个iou阈值分配Proposal正负样本存在的问题：iou阈值过低会导致大量的低阈值预测框，对于regression head来说非常难修正，因此会出现大量的close false positive预测（即iou不够从而被判定为false positive的预测），iou阈值过大又导致匹配到的正样本非常少，容易引起过拟合或者类别不平衡问题。 Cascade R-CNN中为了解决这个问题，提出了使用多级head，将预测iou逐渐提高的方法。 Cascade R-CNN提出的多级head预测如下图(d)所示，这里的H1、H2、H3都是独立参数，且在训练过程中使用的iou不相同。 Cascade R-CNN的思路很简单：在理想状况中，H1接收RPN输出的ROI Proposal作为B0，对B0进行分类和修正，并从中筛除一些背景或者iou较低的box得到新的预测框集合B1，H2则接收B1作为输入，对B1进行分类和修正，得到B2，H3对B2进行同样的操作，得到最终的预测输出。 三个步骤不同之处首先在于背景类的含义，例如存在三个iou阈值$u_1 &lt; u_2 &lt; u_3$，在H1中低于$u_1$的才被认为是负样本，而H2中低于$u_2$的被认为是负样本，H3则使用$u_2$作为负样本阈值，这样设置的意思就是每个Head负责筛掉不同精度的预测框，另外，就算这里$u_3$较高，也不会引起之前iou阈值高导致的匹配到的正样本非常少的问题，因为原始RPN的proposal虽然可能iou较低，但是经过H1、H2的逐步修正之后，B2之中iou普遍比较高了，即使$u_3$较高，也能匹配到较多的正样本用于H3的训练。 在预测过程中，Cascade R-CNN的不同Head先后进行预测，最终的分类得分将综合三个Head的预测结果（例如C1、C2、C3取平均），而回归结果直接使用H3得到的B3。 一阶段目标检测方法一阶段目标检测方法主要是相对于Faster R-CNN这类二阶段检测方法来说的，在Faster R-CNN中，主要被分为region proposal和proposal的分类回归。 一阶段目标检测方法将region proposal这一步去掉了，不需要RPN来进行初步的定位，而是直接得到box输出结果。 一阶段检测器按照时间顺序主要有：YOLO v1、SSD、DSSD、YOLO v2、Retinanet、YOLO v3、YOLO v4等，这里做个简单的梳理。 YOLO v1论文《You Only Look Once:Unified, Real-Time Object Detection》中提出了YOLO检测方法，其基本思想是将一幅图像分成$S \times S$的网格，如果某个目标的中心落在这个网格中，那么这个网格就负责预测这个目标。 网格中的一个格子可以输出$B$个bounding boxes，每个格子的输出如下： $B$个bounding boxes的位置回归信息（中心偏移：x,y（使用格子大小来归一化）；box的宽高：w,h（使用图片的长宽来归一化））共$4\times B$个值。 bounding boxes对应的$B$个confidence scores（这个confidence score表示当前bounding box包含目标的概率乘以bounding box位置回归的置信度），其的回归目标定义为$Pr(Object) \times IOU^{truth}_{pred}$。 C个类别信息，表示为$Pr(Class_i|Object),i = 1,2,…,C$，这是个条件概率，其含义是如果这个格子有目标，那么这个目标属于不同的类别的概率。 因此一个格子的输出个数是$5 \times B + C$，整个YOLO v1模型的输出个数是$S\times S \times (5 \times B + C)$。 在YOLO v1模型中，输入图片大小固定为$448 \times 448$，经过6次下采样之后变成$7\times 7$大小的特征图，作者取$S=7$，$B=2$，Pascal VOC的类别个数$C=20$，因此YOLO v1的输出是个$7\times 7 \times 30$的特征图，整个模型结构图如下所示，其中激活函数使用Leaky ReLU，参数为0.1。 损失函数YOLO v1使用平方和误差来进行训练，但是论文中表示这样的损失函数不能完美的匹配优化Average Precision的目标（就是说定位误差和分类误差的权重相等不太合适），而且每张图片上不包含目标的格子居多，这些格子主导了梯度，导致预测置信度（confidence score）普遍偏低，同时模型训练过程容易震荡，因此作者在损失函数中引入了两个超参数：$\lambda_{coord} = 5$、$\lambda_{noobj} = 0.5$作为不同损失的权重。 另外，论文中还提到平方和损失在回归位置的时候，还有个问题是large box和small box对于同样的宽高偏差敏感度不同，为了缓解这个问题，论文中提出直接让模型预测$w,h$的平方，需要获取真正的长宽时，需要将模型的预测输出开方进行还原。 最终YOLO v1的损失函数如下： \begin{aligned} &\lambda_{coord} \sum\limits_{i=0}^{S^2} \sum\limits_{j=0}^{B} \mathbf{1}^{obj}_{ij}[(x_i - \hat{x}_i)^2 + (y_i - \hat{y}_i)^2]\\ &+ \lambda_{coord} \sum\limits_{i=0}^{S^2} \sum\limits_{j=0}^{B} \mathbf{1}^{obj}_{ij}[(w_i - \sqrt{\hat{w}_i})^2 + (h_i - \sqrt{\hat{h}_i})^2]\\ &+ \sum\limits_{i=0}^{S^2} \sum\limits_{j=0}^{B} \mathbf{1}^{obj}_{ij}(C_i - \hat{C}_i)^2\\ &+ \lambda_{noobj} \sum\limits_{i=0}^{S^2} \sum\limits_{j=0}^{B} \mathbf{1}^{noobj}_{ij}(C_i - \hat{C}_i)^2\\ &+ \sum\limits_{i=0}^{S^2} \mathbf{1}^{obj}_{i} \sum\limits_{c \in Classes} (p_i(c) - \hat{p}_i(c))^2 \end{aligned}其中$\mathbf{1}^{obj}_{ij}$是个指示函数，表示第$i$个格子的第$j$个box是否对应一个目标。$\mathbf{1}^{obj}_{i}$则表示第$i$个格子是否包含目标，$\mathbf{1}^{noobj}_{ij} = 1 - \mathbf{1}^{obj}_{ij}$表示第$i$个格子的第$j$个box是否不对应一个目标。 bounding boxYOLO v1中虽然一个格子对应两个bounding box，但是这里的bounding box概念和anchor box不同。 anchor box需要设置先验大小和长宽比，然后预测输出是相对于anchor box的偏移和缩放，计算loss时需要先用anchor box去和gt box匹配，确定好优化目标才能计算loss。 而bounding box不需要先验大小和长宽比，bounding box输出的是相对于格子中心的像素偏移（使用格子大小来归一化）以及像素长宽比（使用图片大小来归一化），计算loss时要看这个格子输出的两个bounding box哪个和gt box的iou大，iou大的那个bounding box才进行回归优化。但是每个格子中的两个bounding box都会去训练confidence score，这个的回归目标也是和bounding box的输出有关（$Pr(Object) \times IOU^{truth}_{pred}$，其中$IOU^{truth}_{pred}$是bounding box预测值和gt box的iou）。 这样看来YOLO v1算是一种anchor free的检测方法。 YOLO v2在论文《YOLO9000:Better, Faster, Stronger》中对YOLO v1进行了一些改进最终提出了YOLO v2检测方法。 相对于YOLO v1的改进点 batch norm，每个卷积层后面都添加Batch Normalization层，并且不再使用droput。 hi-res classifier，YOLO v1使用ImageNet上预训练的模型来进行初始化，这个预训练的输入图像是224大小的，但是为了提高检测结果的准确性，检测训练使用的输入大小是448，这样的切换模型难以适应，因此YOLO v2中增加了一个在ImageNet数据集上使用448大小finetune 10个epoch的过程。 Convolutional，在YOLO v1中，最终是使用全连接层来得到最终的预测，直接预测目标像素坐标和大小，这样做导致位置定位比较困难，难以训练，论文中参考了Faster RCNN的做法，认为使用卷积的方式直接得到每个像素点相对于Anchor box的偏移缩放信息的预测方法比较简便且容易训练，因此这里将全连接层改为卷积层的同时引入了Anchor box的做法（引入了Anchor box之后，预测的类别和是否包含目标这些问题都是针对每个anchor box的，之前是一个格子一个类别，且只判断格子是否包含目标。） Anchor boxes，像Faster RCNN那样使用手工设计的Anchor box大小和长宽比，使用手工设计的Anchor box之后在Recall上面有提高，但是在mAP上面反而下降了一些（增加了一些假阳性）。 new network，YOLO v2使用一种更加接近VGG的backbone：Darknet-19，相比于YOLO v1的特征提取网络，这里除了替换$7\times 7$的卷积、通道个数、卷积核层个数等变化以外，下采样次数从6次变成了5次，如果输入448大小的图片，那么输出大小将变成$14\times 14$，但论文中认为最终输出的格子数为奇数比较好（便于那种占满整图的大目标的中心落在格子中心而不是格子边缘），因此将输入大小调整为$416$，得到$13\times 13$大小的输出。 dimension clusters，论文中认为通过聚类学习Anchor box的大小设置比手工设计Anchor box的大小更便于模型回归目标位置，因此使用k-means聚类方法，从标注中的gt box形状中找到适合的Anchor box大小，在聚类时，两个box之间的距离使用$d = 1 - IOU$来表示（这里的IOU计算假定两个box的中心重合，这里不用考虑box的位置），作者给出的数据中，如果是手工设计的9个Anchor box大小，和gt box平均IOU在0.609，但是通过聚类学习到Anchor box，只需要5个大小就能够达到平均IOU0.61，明显好于手工设计的Anchor box先验大小。 location prediction，在基于Anchor的预测的方法中，模型给出预测的偏移量（$t_x, t_y$）之后，预测的中心位置计算方式为$x = (t_x \times w_a) + x_a, y = (t_y \times h_a) + y_a$，其中$w_a, h_a, x_a, y_a$分别表示Anchor Box的宽、高、中心x坐标、中心y坐标，但是这样的预测方式论文中认为没有对预测box的位置进行约束（对于一个左上角的Anchor box，其预测结果可能跑到图片右下角），论文中认为这给模型带来了不利于优化的不稳定性。因此论文中提出将预测方式改为$b_x=\sigma(t_x) + C_x, b_y=\sigma(t_y) + C_y, b_w = p_we^{t_w}, b_h = p_he^{t_h}, Pr(object)\times IOU_{b}^{object} = \sigma(t_o)$（这里的$C_y,C_x$代表格子的左上角坐标，因为$\sigma(t_x),\sigma(t_y)$都是在0~1范围，因此可以保证这样预测出来的中心不会超出这个格子，$p_w, p_h$则代表Anchor Box的宽高，可以见后面的YOLOv2预测方式示意图），这里的意思是，对于每个Anchor Box，都模型都输出四个值$\sigma(t_x), \sigma(t_y), t_w, t_h, \sigma(t_o)$，对于模型的输出值，计算出最终预测框的中心：$b_x,b_y$以及预测框的宽高：$b_w, b_h$，以及预测框的置信度：$Pr(object)\times IOU_{b}^{object}$。 passthrough，将$26\times 26\times 512$的特征图resize成$13\times 13\times 2048$然后和最后的$13\times13\times 1024$的特征图进行Concatenate操作，共同用于模型的最终预测，论文中解释说是为了提供更加细粒度的特征。 multi-scale，因为YOLO v2抛弃了之前的全连接操作，因此可以使用不同尺度的输入进行训练，在训练过程中，每10个batch随机按照$320:32:608$来选择一次尺度。 hi-res detector，这个论文中没有详细说明，不过从原文来看，这个意思就是在预测的时候使用$544\times 544$大小的图片。 以上各种改进的消融实验结果见下表： location prediction这一项改进中所描述的预测方法示意图： 这里有个细节是在$b_x=\sigma(t_x) + C_x, b_y=\sigma(t_y) + C_y$这两个运算里面所有的项都不是像素单位，如果想要转换成像素单位，那么需要除以S，然后乘上图片的或着高 另外为了节省计算量，passthrough还有个改进版本：增加一次卷积操作，先将$26\times 26\times 512$的特征图变为$26\times 26\times 64$的特征图，然后再resize到$13\times 13\times 256$，最后拿去和$13\times13\times 1024$的特征图进行Concatenate操作。 YOLO v2的损失函数这部分其实类似YOLO v1，全都是L2损失，只不过其中YOLO v1使用的那个开方的操作这里不用了，而且计算的类别是相对于每个Anchor来说的，其输出大小为$S\times S \times B \times (5 + C)$（这里的$B$是anchor个数），其他损失的计算和YOLO v1类似，论文中没有详细描述，如果感兴趣可以去研究YOLO v2的实现代码。 YOLO v3在论文《YOLOv3: An Incremental Improvement》中提出了YOLO v3检测方法。 在YOLO v3中，相对于YOLO v2，首先更换了Backbone，从YOLO v2的Darknet-19改成了Darknet-53（根据论文中提供的Backbone分类任务训练结果中，Darknet-53的分类性能可以和ResNet-152比肩，但是速度是ResNet-152的两倍），这个新backbone中引入了resnet中的残差模块，顺便将Darknet-19中的池化层全替换成了步长为2的卷积层。 YOLO v3的另外一个主要改进是借鉴了FPN的思路，不仅在$13\times 13$大小的特征图上进行预测，同时也在$26\times 26$，$52\times 52$大小的特征图上进行预测，三种大小的特征图上分类的anchor box都是每个格子三个，但是大小不同，在$52\times 52$大小的特征图的Anchor Box最小，在$13\times 13$大小的特征图的Anchor Box最大，下图中最后的输出部分，每个像素点有255个预测值，其实就是$3 \times (5 + 80)$，其中3是Anchor Box个数，80是COCO数据集的类别个数。 在YOLO v3中，还有一个地方是其目标分配机制和YOLO v2稍有不同，对于一个格子，如果目标落在了这个格子中（这里因为三个不同大小的特征图都有输出，因此一个目标肯定会落在三个格子中，分别位于三个特征图上，这就关系到了$3\times 3 = 9$个Anchor Box，YOLO v2因为是单尺度特征图预测，因此只需要考虑其设定的5个Anchor Box），YOLO v3在处理这个问题的时候，计算了这9个Anchor Box和当前目标的iou，iou最大的那个Anchor Box用于预测当前目标，其余Anchor Box如果iou大于某个阈值（），则计算损失时忽略其置信度损失，否则当成负样本处理。 YOLO v3论文中并没有给出详细的结构描述图，下面的结构描述图来自于CSDN博客《yolo系列之yolo v3【深度解析】》 YOLO v3在COCO上进行的对比实验结果如下所示，这里可以看出YOLO v3在精度上并没有特别明显的竞争力，但是如果精度要求不是那么高的话，YOLO v3在速度上超越其他的单阶段检测器很多。 YOLO v4YOLO v4在论文《YOLOv4: Optimal Speed and Accuracy of Object Detection》中被提出，其主要结构组件如下： backbone：CSPDarknet53 neck：SPP、PANet head：YOLO v3 另外YOLO v4中使用了很多技巧： Bag of Freebies (BoF) for backbone： CutMix and Mosaic data augmentation DropBlock regularization Class label smoothing Bag of Specials (BoS) for backbone： Mish activa-tion Cross-stage partial connections (CSP) Multi-input weighted residual connections (MiWRC) Bag of Freebies (BoF) for detector： CIoU-loss CmBN DropBlock regularization Mosaic data augmentation Self-Adversarial Training Eliminate grid sensitivity Using multiple anchors for a single groundtruth Cosine annealing scheduler Optimal hyper-parameters Random training shapes Bag of Specials (BoS) for detector： Mish activation SPP-block SAM-block PAN path-aggregation block DIoU-NMS 其中Bag of Freebies (BoF)表示只在训练过程中增加时间成本，不影响预测过程的技巧，Bag of Specials (BoS)表示在预测过程中会稍微增加一点时间成本的技巧。 YOLO v4基本上算是个大型炼丹现场，作者在论文中都是用实验数据证明各种操作的效果，很少有理论分析，因此这篇论文更适合作为检测任务寻找提分手段的一个手册（一切以实验数据说话）。 多尺度问题特征金字塔网络(Feature Pyramid Network，FPN)在论文《Feature Pyramid Networks for Object Detection》中提出的特征金字塔操作已经是目前目标检测模型上的标准配置，无论是单阶段检测模型还是两阶段检测模型，均可以添加FPN结构来让模型获得更好的尺度鲁棒性。 FPN的结构示意图如下，其中左上角从下到上是检测模型中backbone的特征提取过程，每一层代表不同stride的特征图，原本的检测模型，例如Fatser R-CNN,是在最上面一个特征来进行roi proposal、roi classification和roi regression等操作，在加入FPN之后，获得了右上角的一系列特整图，不同stride的特征图预测不同大小的目标。 上图中画得不是很完整，一般backbone中会进行5次下采样，得到的特征图stride分别是2、4、8、16、32，对应的特征图称为C1、C2、C3、C4、C5，在mmdetection检测框架的Faster R-CNN-FPN的实现中，C5将通过大小$3\times 3$，stride为2的卷积下采样得到P6，另外C5经过$1\times 1$卷积之后变为P5，P5上采样后和C4的$1\times 1$卷积结果相加变成P4，P4再上采样和C3的$1\times 1$卷积结果相加变成P3，以此类推，分别得到P2、P3、P4、P5、P6，其中P2、P3、P4、P5、P6均参与RPN的roi proposal，而对于不同大小的roi，则在P2、P3、P4、P5中选择对应尺度的特征图来进行roi pooling/roi align。 原始FPN论文中，对于宽高为$w,h$的roi，其对应的特征金字塔等级计算方法如下，这里设置$k_0=4$表示第4级特征金字塔，即$P5$，因为ImageNet预训练的backbone，一般使用224大小的输入图像，而且最终结果是在C5得到的，因此这里让224以上的目标对应于$P5$特征图，小于224大小的目标则每小一倍，对应的特征等级减一 level = \lfloor k_0 + \log_2(\frac{\sqrt{wh}}{224})\rfloor在mmdetection框架的实现中，roi的分配则和原论文不同，mmdetection中，计算方式如下： scale &lt; finest_scale * 2: level 0 finest_scale 2 &lt;= scale &lt; finest_scale 4: level 1 finest_scale 4 &lt;= scale &lt; finest_scale 8: level 2 scale &gt;= finest_scale * 8: level 3 这里的scale是指$\sqrt{wh}$即roi的尺度，finest_scale默认值为56，这里的level 0对应P2，level 1对应P3，以此类推。 FPN在保证大目标的检测效果的同时，可以大幅提高小目标的检测效果，我认为其原因在于小目标物体的检测需要更加精细的位置信息，如果使用stride过大的特征图，会导致小目标的预测不够精确，同时小目标的信息容易被大目标掩盖，FPN中stride较小的特征图上融合了深层的特征图的信息，这可以让其的感受野非常大，而且感受野分布比较密集，小目标的检测可能也需要大感受野信息同时加上细粒度空间信息。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[算法总结]]></title>
    <url>%2F2020%2F05%2F19%2F%E7%AE%97%E6%B3%95%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[排序冒泡排序逐对交换，每次迭代，确定一个元素的最终位置。 时间复杂度：$O(N^2)$ 空间复杂度：$O(1)$12345678910111213141516int* bubbleSort(int* A, int n) &#123; if(A == NULL || n &lt;= 1)&#123; return A; &#125; int temp; for(int i = 1; i &lt; n - 1; ++i)&#123; for(int j = 0; j &lt; n - i; ++j)&#123; if(A[j] &gt; A[j+1])&#123; temp = A[j+1]; A[j+1] = A[j]; A[j] = temp; &#125; &#125; &#125; return A;&#125; 选择排序每次选择一个剩下的最小或者最大元素放到对应位置。 时间复杂度：$O(N^2)$ 空间复杂度：$O(1)$1234567891011121314151617181920212223int* selectionSort(int* A, int n) &#123; if(A == NULL || n &lt;= 1)&#123; return A; &#125; int min_value, j, min_index; for(int i = 0;i&lt;n-1;++i)&#123; min_value = 0x7fffffff; min_index = j = i; while(j &lt; n)&#123; if(A[j] &lt; min_value)&#123; min_value = A[j]; min_index = j; &#125; ++j; &#125; if(min_index != i)&#123; A[i] ^= A[min_index]; A[min_index] ^= A[i]; A[i] ^= A[min_index]; &#125; &#125; return A;&#125; 插入排序时间复杂度：$O(N^2)$ 空间复杂度：$O(1)$123456789101112131415161718int* insertionSort(int* A, int n) &#123; if(A == NULL || n &lt;= 1)&#123; return A; &#125; int cur, j; for(int i=1;i&lt;n;++i)&#123; cur = A[i]; for(j = i-1; j&gt;= 0; --j)&#123; if(cur &lt; A[j])&#123; A[j+1] = A[j]; &#125;else&#123; break; &#125; &#125; A[j+1] = cur; &#125; return A;&#125; 归并排序时间复杂度：$O(N\log(N))$ 空间复杂度：$O(N)$1234567891011121314151617181920212223242526272829int* merge(int* A, int lenA, int*B, int lenB)&#123; int *result = new int[lenA + lenB]; int i=0,j=0; while(i &lt; lenA &amp;&amp; j &lt; lenB)&#123; if(A[i] &lt;= B[j])&#123; result[i+j] = A[i]; ++i; &#125;else&#123; result[i+j] = B[j]; ++j; &#125; &#125; while(i &lt; lenA)&#123; result[i+j] = A[i]; ++i; &#125; while(j &lt; lenB)&#123; result[i+j] = B[j]; ++j; &#125; return result;&#125;int* mergeSort(int* A, int n) &#123; if(A == NULL || n &lt;= 1)&#123; return A; &#125; int center = n / 2; return merge(mergeSort(A, center), center, mergeSort(A + center, n - center), n - center);&#125; 快速排序时间复杂度：$O(N\log(N))$ 空间复杂度：$O(\log(N))$ ~ $O(N)$1234567891011121314151617181920212223242526272829303132333435363738int partition(int* A, int n)&#123; if(n &lt;= 1)&#123; return 1; &#125;else&#123; int small = -1; int i = 0, j = n-2; while(i &lt;= j)&#123; if(A[i] &lt;= A[n-1])&#123; small = i++; &#125;else if(A[j] &lt;= A[n-1])&#123; if(j != i)&#123; A[j] ^= A[i]; A[i] ^= A[j]; A[j] ^= A[i]; &#125; small = i++; &#125;else&#123; --j; &#125; &#125; if(small + 1 != n - 1)&#123; A[small + 1] ^= A[n - 1]; A[n - 1] ^= A[small + 1]; A[small + 1] ^= A[n - 1]; &#125; return small + 1; &#125;&#125;int* quickSort(int* A, int n) &#123; if(A == NULL || n &lt;= 1)&#123; return A; &#125;else&#123; int center = partition(A, n); quickSort(A, center); quickSort(A+center+1, n-center-1); return A; &#125;&#125; 堆排序时间复杂度：$O(N\log(N))$ 空间复杂度：$O(1)$ 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384int parent(int i)&#123; return (i - 1) / 2;&#125;int left(int i)&#123; return i * 2 + 1;&#125;int right(int i)&#123; return i * 2 + 2;&#125;void DownHeap(int* A, int cur, int n)&#123; int left_idx, right_idx, max_idx, end = parent(n - 1); while(cur &lt;= end)&#123; left_idx = left(cur); right_idx = right(cur); max_idx = A[cur] &lt; A[left_idx]? left_idx : cur; if(right_idx &lt; n)&#123; max_idx = A[max_idx] &lt; A[right_idx]? right_idx : max_idx; &#125; if(max_idx != cur)&#123; A[cur] ^= A[max_idx]; A[max_idx] ^= A[cur]; A[cur] ^= A[max_idx]; cur = max_idx; &#125;else&#123; break; &#125; &#125;&#125;void createHeap(int* A, int n)&#123; int end = parent(n - 1); for(int i = end; i &gt;= 0; --i)&#123; DownHeap(A, i, n); &#125;&#125;int* heapSort(int* A, int n)&#123; if(A == NULL || n &lt;= 1)&#123; return A; &#125; createHeap(A, n); for(int i = n-1;i&gt;0;--i)&#123; DownHeap(A, 0, i+1); A[i] ^= A[0]; A[0] ^= A[i]; A[i] ^= A[0]; &#125; return A;&#125;void DownHeap(vector&lt;int&gt;&amp; heap, int i, int n)&#123; // 大顶堆的下滤操作 int temp = heap[i]; int l,r,maxChild=i; while(true)&#123; l = i * 2 + 1; r = l + 1; maxChild = (r &lt; n &amp;&amp; heap[r] &gt; heap[l]) ? r : l; maxChild = (maxChild &lt; n &amp;&amp; heap[maxChild] &gt; temp) ? maxChild : i; if(maxChild != i)&#123; heap[i] = heap[maxChild]; i = maxChild; &#125;else&#123; break; &#125; &#125; heap[i] = temp; return ;&#125;vector&lt;int&gt; sortArray(vector&lt;int&gt;&amp; nums) &#123; //堆排序 int n = nums.size(); //构建大顶堆 for(int i = n / 2; i &gt;= 0; --i)&#123; DownHeap(nums, i, n); &#125; //逐个删除堆顶，进行排序 for(int i = n; i &gt; 1; --i)&#123; swap(nums[0], nums[i - 1]); DownHeap(nums, 0, i - 1); &#125; return nums;&#125; 希尔排序时间复杂度：$O(N\log(N))$ 空间复杂度：$O(1)$123456789101112131415161718192021222324252627282930313233343536373839404142434445464748void insertionSort(int* A, int n, int space) &#123; int cur, j; for(int i=space;i&lt;n;i++)&#123; cur = A[i]; for(j = i-space; j&gt;= 0; j-=space)&#123; if(cur &lt; A[j])&#123; A[j+space] = A[j]; &#125;else&#123; break; &#125; &#125; A[j+space] = cur; &#125;&#125;int* shellSort(int* A, int n) &#123; if(A == NULL || n &lt;= 1)&#123; return A; &#125; for(int space = n / 2; space&gt;=1; space/= 2)&#123; insertionSort(A, n, space); &#125; return A;&#125;vector&lt;int&gt; shellSort(vector&lt;int&gt;&amp; nums) &#123; //希尔排序 int n = nums.size(); int inc = 2; int temp; int i,j; // 计算Hibbard增量 while(inc * 2 &lt; n)&#123; inc *= 2; &#125; inc -= 1; while(inc &gt;= 1)&#123; for(i = inc; i &lt; n; ++i)&#123; // 执行增量插入排序 temp = nums[i]; for(j = i; j &gt;= inc &amp;&amp; nums[j-inc] &gt; temp; j -= inc)&#123; nums[j] = nums[j-inc]; &#125; nums[j] = temp; &#125; inc = (inc - 1) / 2; &#125; return nums;&#125; 计数排序时间复杂度：$O(N)$ 空间复杂度：$O(M)$，这里的$M$和待排序元素的取值范围有关。 1234567891011121314151617181920212223242526272829int* countingSort(int* A, int n) &#123; if(A==nullptr || n &lt;=1)&#123; return A; &#125; int min = A[0], max = A[0]; for(int i = 1;i &lt; n; ++i)&#123; if(A[i] &gt; max)&#123; max = A[i]; &#125;else if(A[i] &lt; min)&#123; min = A[i]; &#125; &#125; int num = max - min + 1; int *count = new int[num](); for(int i = 0;i &lt; n; ++i)&#123; count[A[i] - min] += 1; &#125; int i = 0, j = 0; while(i &lt; n)&#123; if(count[j] &gt; 0)&#123; count[j] -= 1; A[i++] = j + min; &#125;else&#123; ++j; &#125; &#125; delete []count; return A;&#125; 基数排序时间复杂度：$O(N)$ 空间复杂度：$O(M)$，这里的$M$和待排序元素的取值范围有关。 123456789101112131415161718192021222324252627282930int getdig(int x, int base)&#123; int ubase = 1; for(int i = 0; i &lt; base; i++)&#123; ubase *= 10; &#125; x = x % (ubase * 10); return x / ubase;&#125;int* radixSort(int* A, int n) &#123; int base = 0, dig, j; vector&lt;int&gt; temp; map&lt;int, vector&lt;int&gt; &gt; buckets; while(base &lt; 4)&#123; // 如果待排序数字全都小于10000，那么这里只用到4 for(int i = 0; i &lt; n; ++i)&#123; dig = getdig(A[i], base); buckets[dig].push_back(A[i]); &#125; j = 0; for(int i = 0; i &lt; 10; ++i)&#123; temp = buckets[i]; for(int k = 0; k &lt; temp.size(); ++k)&#123; A[j++] = temp[k]; &#125; buckets[i].clear(); &#125; ++base; &#125; return A;&#125; 基本有序的情况如果一个序列已经基本有序，即排好序之后，其中每个元素的移动位置偏移不超过K，那么这种情况下，可以使用改进的堆排序方法，每次使用前K个数字建立小根堆（建好后最小的数字肯定在第一个位置），每次向后移动一个位置，维持小根堆，这样一直往后移动最终完成排序，其时间复杂度为$O(N \times \log(K))$，其次，这种情况下插入排序是比较好的，其时间复杂度为$O(N \times K)$，改进的堆排序算法如下（这里为了实现简单，使用了额外$O(K)$的空间，其实不需要，只不过代码可能会变得比较复杂）： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162int parent(int i)&#123; return (i - 1) / 2;&#125;int left(int i)&#123; return i * 2 + 1;&#125;int right(int i)&#123; return i * 2 + 2;&#125;void modifyHeap(int *A, int cur, int n)&#123; int left_idx, right_idx, min_idx, end = parent(n - 1); while(cur &lt;= end)&#123; left_idx = left(cur); right_idx = right(cur); min_idx = A[cur] &gt; A[left_idx]? left_idx : cur; if(right_idx &lt; n)&#123; min_idx = A[min_idx] &gt; A[right_idx]? right_idx : min_idx; &#125; if(min_idx != cur)&#123; A[cur] ^= A[min_idx]; A[min_idx] ^= A[cur]; A[cur] ^= A[min_idx]; cur = min_idx; &#125;else&#123; break; &#125; &#125;&#125;void createHeap(int *A, int n)&#123; int end = parent(n - 1); for(int i = end; i &gt;= 0; --i)&#123; modifyHeap(A, i, n); &#125;&#125;vector&lt;int&gt; sortElement(vector&lt;int&gt; A, int n, int k) &#123; if(A.empty() || n &lt;= 1 || k &lt; 1)&#123; return A; &#125;else&#123; int start = 0, group_num = 0; int* B = new int[k + 1]; for(int i = 0; i &lt; n &amp;&amp; i &lt; k + 1; ++i)&#123; B[i] = A[i]; group_num += 1; &#125; createHeap(B, group_num); while(group_num &gt; 1)&#123; A[start++] = B[0]; if(start + group_num &gt; n)&#123; group_num--; B[0] = B[group_num]; modifyHeap(B, 0, group_num); &#125;else&#123; B[0] = A[start + group_num - 1]; modifyHeap(B, 0, group_num); &#125; &#125; if(group_num == 1)&#123; A[start] = B[0]; &#125; return A; &#125;&#125; 字符串KMP字符串匹配算法。 1234567891011121314151617181920212223242526272829303132bool kmp(string A, int lena, string B, int lenb)&#123; // 在A中查找B int *next = new int[lenb](); int i=1,j=0; while(i &lt; lenb)&#123;// 构造next数组 if(B[i] == B[j])&#123; next[i++] = ++j; &#125;else&#123; j = next[j - 1]; if(j == 0)&#123; next[i++] = 0; &#125; &#125; &#125; i = j = 0; while(i &lt;= lena - lenb + j)&#123;// 进行匹配 while(A[i] == B[j])&#123; ++i; ++j; if(j == lenb)&#123; return true; &#125; &#125; if(j == 0)&#123; ++i; &#125;else&#123; j = next[j - 1]; &#125; &#125; return false;&#125; 栈和队列滑动窗口最大值1234567891011121314151617vector&lt;int&gt; slide(vector&lt;int&gt; arr, int n, int w) &#123; // write code here vector&lt;int&gt; result; deque&lt;int&gt; maxque; for(int i=0;i&lt;n;++i)&#123; while(!maxque.empty() &amp;&amp; arr[maxque.back()] &lt; arr[i])&#123; maxque.pop_back(); &#125; maxque.push_back(i); if(i &gt;= w - 1)&#123; while(maxque.front() &lt;= i - w)&#123; maxque.pop_front(); &#125; result.push_back(arr[maxque.front()]); &#125; &#125; return result; 构造MaxTree对于一个没有重复元素的整数数组，请用其中元素构造一棵MaxTree，MaxTree定义为一棵二叉树，其中的节点与数组元素一一对应，同时对于MaxTree的每棵子树，它的根的元素值为子树的最大值。现有一建树方法，对于数组中的每个元素，其在树中的父亲为数组中它左边比它大的第一个数和右边比它大的第一个数中更小的一个。若两边都不存在比它大的数，那么它就是树根。请设计O(n)的算法实现这个方法。 给定一个无重复元素的数组A和它的大小n，请返回一个数组，其中每个元素为原数组中对应位置元素在树中的父亲节点的编号，若为根则值为-1。 测试样例： [3,1,4,2],4 返回：[2,0,-1,2]1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950vector&lt;int&gt; buildMaxTree(vector&lt;int&gt; A, int n) &#123; // write code here vector&lt;int&gt; result; int l,r; vector&lt;int&gt; left_max, right_max; stack&lt;int&gt; left_stack, right_stack; for(int i = 0; i &lt; n; ++i)&#123; if(left_stack.empty())&#123; left_max.push_back(-1); &#125;else&#123; while(!left_stack.empty() &amp;&amp; A[left_stack.top()] &lt;= A[i])&#123; left_stack.pop(); &#125; if(left_stack.empty())&#123; left_max.push_back(-1); &#125;else&#123; left_max.push_back(left_stack.top()); &#125; &#125; left_stack.push(i); if(right_stack.empty())&#123; right_max.push_back(-1); &#125;else&#123; while(!right_stack.empty() &amp;&amp; A[right_stack.top()] &lt;= A[n - i - 1])&#123; right_stack.pop(); &#125; if(right_stack.empty())&#123; right_max.push_back(-1); &#125;else&#123; right_max.push_back(right_stack.top()); &#125; &#125; right_stack.push(n - i - 1); &#125; for(int i = 0; i &lt; n; ++i)&#123; l = left_max[i]; r = right_max[n - i - 1]; if(l == -1 &amp;&amp; r == -1)&#123; result.push_back(-1); &#125;else if(l == -1)&#123; result.push_back(r); &#125;else if(r == -1)&#123; result.push_back(l); &#125;else&#123; result.push_back(A[l] &gt; A[r]? r : l); &#125; &#125; return result;&#125; 链表链表的回文判断请编写一个函数，检查链表是否为回文。 给定一个链表ListNode* pHead，请返回一个bool，代表链表是否为回文。 测试样例： {1,2,3,2,1} 返回：true {1,2,3,2,3} 返回：false 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758/*struct ListNode &#123; int val; struct ListNode *next; ListNode(int x) : val(x), next(NULL) &#123;&#125;&#125;;*/void reverse(ListNode *pHead, ListNode *pTail)&#123; if(pHead == nullptr || pTail == pHead)&#123; return; &#125; ListNode *cur=pHead, *next=pHead-&gt;next, *temp; while(next!=pTail)&#123; temp = next-&gt;next; next-&gt;next = cur; cur = next; next = temp; &#125; pTail-&gt;next = cur; pHead-&gt;next = nullptr;&#125;bool isPalindrome(ListNode* pHead) &#123; // write code here if(pHead == nullptr)&#123; return false; &#125; ListNode *center=pHead, *tail=pHead, *pcur=pHead, *ncur; bool result = true; while(center-&gt;next != nullptr &amp;&amp; tail-&gt;next!=nullptr)&#123; center = center-&gt;next; tail = tail-&gt;next; if(tail-&gt;next!=nullptr)&#123; tail = tail-&gt;next; &#125;else&#123; break; &#125; &#125; if(center == tail)&#123; if(pHead-&gt;val == tail-&gt;val)&#123; return true; &#125;else&#123; return false; &#125; &#125; reverse(center, tail); ncur = tail; while(pcur!=center)&#123; if(pcur-&gt;val != ncur-&gt;val)&#123; result = false; break; &#125;else&#123; pcur = pcur-&gt;next; ncur = ncur-&gt;next; &#125; &#125; reverse(tail, center); return result;&#125; 复杂链表的复制输入一个复杂链表（每个节点中有节点值，以及两个指针，一个指向下一个节点，另一个特殊指针指向任意一个节点），复制这个复杂链表并返回。 123456789101112131415161718192021222324252627282930313233343536373839/*struct RandomListNode &#123; int label; struct RandomListNode *next, *random; RandomListNode(int x) : label(x), next(NULL), random(NULL) &#123; &#125;&#125;;*/RandomListNode* Clone(RandomListNode* pHead)&#123; RandomListNode *cur=pHead, *copy, *resultHead, *resultCur; if(pHead==nullptr)&#123; return nullptr; &#125; while(cur!=nullptr)&#123; copy = new RandomListNode(cur-&gt;label); copy-&gt;next = cur-&gt;next; cur-&gt;next = copy; cur = copy-&gt;next; &#125; cur = pHead; while(cur!=nullptr)&#123; if(cur-&gt;random != nullptr)&#123; cur-&gt;next-&gt;random = cur-&gt;random-&gt;next; &#125; cur = cur-&gt;next-&gt;next; &#125; resultCur = resultHead = pHead-&gt;next; cur = pHead; while(cur!=nullptr &amp;&amp; resultCur!=nullptr)&#123; cur-&gt;next = cur-&gt;next-&gt;next; if(resultCur-&gt;next!=nullptr)&#123; resultCur-&gt;next = resultCur-&gt;next-&gt;next; &#125; cur = cur-&gt;next; resultCur = resultCur-&gt;next; &#125; return resultHead;&#125; 判断链表是否有环如何判断一个单链表是否有环？有环的话返回进入环的第一个节点的值，无环的话返回-1。如果链表的长度为N，请做到时间复杂度O(N)，额外空间复杂度O(1)。 给定一个单链表的头结点head，请返回所求值。 123456789101112131415161718192021222324252627int chkLoop(ListNode* head)&#123; // write code here ListNode *fast=head, *slow=head; bool hasLoop = false; while(fast != nullptr)&#123; slow = slow-&gt;next; fast = fast-&gt;next; if(fast)&#123; fast = fast-&gt;next; &#125;else&#123; break; &#125; if(fast == slow)&#123; hasLoop = true; break; &#125; &#125; if(!hasLoop)&#123; return -1; &#125; fast = head; while(fast!=slow)&#123; fast = fast-&gt;next; slow = slow-&gt;next; &#125; return fast-&gt;val;&#125; 无环链表是否相交直接判断最后一个节点是否相等即可，如果要求返回第一个共同节点，那么则需要先统计各链表长度$m,n$，假如$m \le n$，那么第二个链表从第$n-m$个节点开始遍历，第一个链表从头结点开始遍历，两个遍历同步，同时比较当前节点是否相同，第一个相同节点即返回。 123456789101112131415161718192021222324/*struct ListNode &#123; int val; struct ListNode *next; ListNode(int x) : val(x), next(NULL) &#123;&#125;&#125;;*/bool chkIntersect(ListNode* headA, ListNode* headB) &#123; // write code here ListNode *nodeA=headA, *nodeB=headB; if(nodeA == nullptr || nodeB == nullptr)&#123; return false; &#125; while(nodeA-&gt;next)&#123; nodeA = nodeA-&gt;next; &#125; while(nodeB-&gt;next)&#123; nodeB = nodeB-&gt;next; &#125; if(nodeA == nodeB)&#123; return true; &#125;else&#123; return false; &#125;&#125; 有环链表是否相交如何判断两个有环单链表是否相交？相交的话返回第一个相交的节点，不想交的话返回空。如果两个链表长度分别为N和M，请做到时间复杂度O(N+M)，额外空间复杂度O(1)。 给定两个链表的头结点head1和head2。请返回一个bool值代表它们是否相交。 如果还需要找到第一个共同节点，先看是否相交，如果相交，有两种情况，第一种是入环之前相交，这样的话和首先将环去掉，然后按照无环链表的情况来进行查找，第二种情况则直接返回任意一个链表的入环节点都可以算作第一个相交节点。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354/*struct ListNode &#123; int val; struct ListNode *next; ListNode(int x) : val(x), next(NULL) &#123;&#125;&#125;;*/ListNode* firstLoopNode(ListNode* head) &#123; // write code here ListNode *fast=head, *slow=head; bool hasLoop = false; while(fast != nullptr)&#123; slow = slow-&gt;next; fast = fast-&gt;next; if(fast)&#123; fast = fast-&gt;next; &#125;else&#123; break; &#125; if(fast == slow)&#123; hasLoop = true; break; &#125; &#125; if(!hasLoop)&#123; return nullptr; &#125; fast = head; while(fast!=slow)&#123; fast = fast-&gt;next; slow = slow-&gt;next; &#125; return fast;&#125;bool chkInter(ListNode* head1, ListNode* head2) &#123; // write code here ListNode *loopNode1 = firstLoopNode(head1); ListNode *loopNode2 = firstLoopNode(head2); ListNode *temp; if(head1 == nullptr || head2 == nullptr)&#123; return false; &#125; if(loopNode1 == loopNode2)&#123; return true; &#125; temp = loopNode2; while(temp-&gt;next != loopNode2)&#123; if(temp == loopNode1)&#123; return true; &#125; temp = temp-&gt;next; &#125; return false;&#125; 二分查找查找局部最小值定义局部最小的概念。arr长度为1时，arr[0]是局部最小。arr的长度为N(N&gt;1)时，如果arr[0]&lt; arr[1]，那么arr[0]是局部最小；如果arr[N-1] &lt; arr[N-2]，那么arr[N-1]是局部最小；如果0 &lt; i &lt; N-1，既有arr[i] &lt; arr[i-1]又有arr[i] &lt; arr[i+1]，那么arr[i]是局部最小。 给定无序数组arr，已知arr中任意两个相邻的数都不相等，写一个函数，只需返回arr中任意一个局部最小出现的位置即可。1234567891011121314151617181920212223242526int getLessIndex(vector&lt;int&gt; arr) &#123; int start = 0, end = arr.size(), mid; if(end == 0)&#123; return -1; &#125;else if(end == 1)&#123; return 0; &#125; if(arr[start] &lt; arr[start])&#123; return start; &#125; if(arr[end - 2] &gt; arr[end - 1])&#123; return end; &#125; while(start &lt; end - 1)&#123; mid = start + (end - start)/2; if(arr[mid] &gt; arr[mid - 1])&#123; end = mid; &#125;else&#123; start = mid; &#125; &#125; return start;&#125; 查找循环有序数组的最小值对于一个有序循环数组arr，返回arr中的最小值。有序循环数组是指，有序数组左边任意长度的部分放到右边去，右边的部分拿到左边来。比如数组[1,2,3,3,4]，是有序循环数组，[4,1,2,3,3]也是。 给定数组arr及它的大小n，请返回最小值。 12345678910111213141516171819202122232425262728293031int getMin(vector&lt;int&gt; arr, int n) &#123; int start = 0, end = n, mid; if(n == 0)&#123; return -1; &#125;else if(n == 1)&#123; return arr[0]; &#125;else if(n == 2)&#123; return arr[0] &gt; arr[1] ? arr[1] : arr[0]; &#125; while(start &lt; end - 1)&#123; mid = start + (end - start) / 2; if(arr[start] &gt; arr[end - 1])&#123; if(arr[mid] &gt; arr[end-1])&#123; start = mid + 1; &#125;else if(arr[mid] &lt; arr[end-1])&#123; end = mid + 1; &#125;else&#123; start = mid; &#125; &#125;else if(arr[start] &lt; arr[end])&#123; return arr[start]; &#125;else&#123; if(arr[mid] &lt; arr[end - 1])&#123; end = mid + 1; &#125;else&#123; start += 1; &#125; &#125; &#125; return arr[start];&#125; 完全二叉树的节点个数给定一棵完全二叉树的根节点root，返回这棵树的节点个数。如果完全二叉树的节点数为N，请实现时间复杂度低于O(N)的解法。 给定树的根结点root，请返回树的大小。 123456789101112131415161718192021222324252627282930int count(TreeNode* root) &#123; int nodeNum = 0; int lldepth = -1, rldepth = 0; TreeNode *cur_root = root, *cur; while(cur_root)&#123; if(lldepth==-1)&#123; cur = cur_root; while(cur)&#123; lldepth += 1; cur = cur-&gt;left; &#125; &#125; cur = cur_root-&gt;right; while(cur)&#123; rldepth += 1; cur = cur-&gt;left; &#125; if(rldepth == lldepth)&#123; nodeNum += 1 &lt;&lt; lldepth; cur_root = cur_root-&gt;right; lldepth = rldepth - 1; rldepth = 0; &#125;else&#123; nodeNum += 1 &lt;&lt; rldepth; cur_root = cur_root-&gt;left; lldepth -= 1; &#125; &#125; return nodeNum;&#125; 二叉树前序、中序、后序三种遍历方式请用递归方式实现二叉树的先序、中序和后序的遍历打印。 给定一个二叉树的根结点root，请依次返回二叉树的先序，中序和后续遍历(二维数组的形式)。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475void pre(TreeNode* root, vector&lt;int&gt; &amp;result)&#123; stack&lt;TreeNode*&gt; s; TreeNode *cur; if(root == nullptr)&#123; return ; &#125; s.push(root); while(!s.empty())&#123; cur = s.top(); s.pop(); if(cur-&gt;right)&#123; s.push(cur-&gt;right); &#125; if(cur-&gt;left)&#123; s.push(cur-&gt;left); &#125; result.push_back(cur-&gt;val); &#125; return ;&#125;void center(TreeNode* root, vector&lt;int&gt; &amp;result)&#123; stack&lt;TreeNode*&gt; s; TreeNode *cur = root; if(root == nullptr)&#123; return ; &#125; while(!s.empty() || cur != nullptr)&#123; if(cur)&#123; while(cur)&#123; s.push(cur); cur = cur-&gt;left; &#125; &#125; cur = s.top(); s.pop(); result.push_back(cur-&gt;val); cur = cur-&gt;right; &#125; return ;&#125;void post(TreeNode* root, vector&lt;int&gt; &amp;result)&#123; stack&lt;TreeNode*&gt; s; TreeNode *cur, *h=root; if(root == nullptr)&#123; return ; &#125; s.push(root); while(!s.empty())&#123; cur = s.top(); if(cur-&gt;left &amp;&amp; h != cur-&gt;left &amp;&amp; h != cur-&gt;right)&#123; while(cur-&gt;left)&#123; s.push(cur-&gt;left); cur = cur-&gt;left; &#125; &#125;else if(cur-&gt;right &amp;&amp; h != cur-&gt;right)&#123; s.push(cur-&gt;right); &#125;else&#123; s.pop(); result.push_back(cur-&gt;val); h = cur; &#125; &#125; return ;&#125;vector&lt;vector&lt;int&gt; &gt; convert(TreeNode* root) &#123; // write code here vector&lt;vector&lt;int&gt; &gt; result(3); pre(root, result[0]); center(root, result[1]); post(root, result[2]); return result;&#125; 二叉搜索树的后序遍历序列输入一个整数数组，判断该数组是不是某二叉搜索树的后序遍历结果。如果是则返回 true，否则返回 false。假设输入的数组的任意两个数字都互不相同。 1234567891011121314bool verifyPostorder(vector&lt;int&gt;&amp; postorder) &#123; stack&lt;int&gt; s;//这个栈在下面的过程中，其中的内容保证是单调递增的 int root = 0x7fffffff;//整棵树都当做一个正无穷节点的左子树，保证当前访问的节点是root的左子树节点。 for(int i = postorder.size() - 1; i &gt;= 0; --i)&#123; if(postorder[i] &gt; root) return false;//左子树的节点值比父节点大，直接判定false while(! s.empty() &amp;&amp; postorder[i] &lt; s.top())&#123; /*倒序访问过程中，访问顺序是当前根节点的左子树的根节点-&gt;左子树根节点的右子树-&gt;左子树根节点的左子树,出现当前节点比较小，说明左子树根节点的右子树访问完了（或者左子树根节点没有右子树），现在访问的是到了左子树根节点的左子树，这个时候需要把左子树根节点的右子树清除，并调整root为当前左子树的根节点*/ root = s.top();//栈中最后一个比当前元素大的那个节点是当前节点的父节点 s.pop(); &#125; s.push(postorder[i]);//当前父节点的左子树节点入栈 &#125; return true;//访问完都没有出现不符合条件的，则判定为true&#125; 动态规划找零问题有数组penny，penny中所有的值都为正数且不重复。每个值代表一种面值的货币，每种面值的货币可以使用任意张，再给定一个整数aim(小于等于1000)代表要找的钱数，求换钱有多少种方法。 给定数组penny及它的大小(小于等于50)，同时给定一个整数aim，请返回有多少种方法可以凑成aim。 1234567891011int countWays(vector&lt;int&gt; penny, int n, int aim) &#123; int record[1001] = &#123;0&#125;; record[0] = 1; for(int i=0; i&lt;n;++i)&#123; for(int j=penny[i];j&lt;=aim;++j)&#123; record[j] += record[j - penny[i]]; &#125; &#125; return record[aim];&#125; 最长上升子序列这是一个经典的LIS(即最长上升子序列)问题，请设计一个尽量优的解法求出序列的最长上升子序列的长度。 给定一个序列A及它的长度n(长度小于等于500)，请返回LIS的长度。 测试样例： [1,4,2,5,3],5 返回：3123456789101112131415161718192021222324252627int getLIS(vector&lt;int&gt; A, int n) &#123; // write code here if(n == 0)&#123; return 0; &#125; int max_len=1; int *record = new int[n]; int max_pos; record[0] = 1; for(int i = 1; i &lt; n; ++i)&#123; max_pos = -1; for(int j = i-1; j&gt;=0;--j)&#123; if(A[j] &lt; A[i] &amp;&amp; (max_pos &lt; 0 || record[j] &gt; record[max_pos]))&#123; max_pos = j; &#125; &#125; if(max_pos &gt;= 0)&#123; record[i] = record[max_pos] + 1; &#125;else&#123; record[i] = 1; &#125; if(record[i] &gt; max_len)&#123; max_len = record[i]; &#125; &#125; return max_len;&#125;]]></content>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[ResNet总结]]></title>
    <url>%2F2020%2F05%2F15%2FResNet%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[ResNet在论文《Deep Residual Learning for Image Recognition》中提出了ResNet结构，其主要目的是使更深层的模型训练变得更加容易。 ResNet的主要特点在于其残差块（Residual Block）结构，如下图所示，其中左边为ResNet-18和ResNet-34中使用的残差块结构，右边则是ResNet-50、ResNet-101、ResNet-152使用的残差快结构，两者不同之处在于瓶颈层（BottleNeck）的使用，瓶颈层的作用主要是减少参数量和运算量。 在原论文中，上图的曲线连接被称为“identity mapping”，通过这样的短连接，显式地改变了模型所模拟的映射函数，从$H(x)$变成$H(x) + x$，这里的相加操作有两个细节：1、如果$H(x)$的通道数和$x$不一样，那么$x$需要调整通道数，论文中尝试了三种方案，第一种是使用0填充，第二种是仅仅在通道数不一样的层使用$1\times 1$卷积来调整通道，第三种是所有的residual block中都使用$1\times 1$卷积，实验结果发现第三种方案最好，但是作者解释说这主要是因为参数量上升带来的性能增加，而且和第二种方案相比效果提升不大，因此最终还是用的第二种方案。2、如果除了通道不一样，$H(x)$的大小和$x$也不一样，那么在$x$上使用的$1\times 1$卷积的stride设置为2。 对ResNet的理解ResNet中的短连接主要有两个好处：短连接使得梯度的传递更加稳定，短连接显式的增加了特征利用率，使得模型的深层仍然可以看到浅层特征。 Residual Block还有一个好处是可以让模型自由的选择深度，例如模型在最后几层如果觉得在加深深度没有效果，完全可以把最后几层的$H(x)$中的参数学习为全0，这样最后几层相当于没有任何的操作，从而让学习过程来决定模型的深度。 ResNet在设计的过程中，可以借鉴的一个思路是：设计模型结构的时候，如果能够保证模型参数在某个情况下能够使得当前模型和没有改变结构时的原始模型相同，那么设计出来的模型结构效果下限也就是原来的模型了。 ResNetV2在论文《Identity Mappings in Deep Residual Networks》中，提出了ResNetV2结构，这里主要是将Residual Block的结构做了一些修改，如下图所示，其中（a）图为原始的Residual Block结构，（e）图为ResNetV2的Residual Block结构，其余结构在论文中也有实验，但是最终发现（e）图所示结构效果最佳。 上图五种结构的实验结果如下表，其中数字代表分类错误率。 以上结构中，（b）图的结构表现非常差，我认为其原因在于addition之前两个分支的分布不统一，导致在addition之后经过BN层时对identity mapping的输出分布有一定程度上的扰乱（也可以理解为identity mapping因为已经经过BN层，因此方差小，但是另一个分支方差大，导致经过BN层之后，基本上只保留了另外一个分支的信号）。 （c）图中效果差的原因我认为主要是ReLU在addition之前，导致每次相加都是加的一个非负值，因此限制了模型的信号强度只能越来越大，模型的表达能力受限。 （d）图的结果其实和（a）图接近，我认为这两个结构比（e）图稍差的地方在于：在经过权重层之前，信号的分布已经被打乱且没有经过BN层的整合，减弱了BN层在整个模型中的作用。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[各种优化算法总结]]></title>
    <url>%2F2020%2F05%2F12%2F%E5%90%84%E7%A7%8D%E4%BC%98%E5%8C%96%E7%AE%97%E6%B3%95%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[梯度下降（Gradient Descnet, GD）是目前DNN（Deep Neural Network）中使用最多的优化算法，相同的数据，相同的DNN结构，相同的初始化参数值，使用不同的优化算法会得到截然不同的结果，因此优化算法非常重要，但是梯度下降法有一些缺点，为了对这些缺点进行修正，GD优化算法经历了很多演进，但是目前仍然没有一种普适的优化算法，这里根据论文《An overview of gradient descent optimization algorithms》的描述，将GD算法的的各种改进做了一些总结。 最初的GD算法对于待优化的函数$J(\theta)$，其中$\theta$是待优化参数，GD算法的思路非常简洁，即指定学习速率$\eta$和参数初始值$\theta^0$，按照如下方式迭代的更新参数，其中$\nabla_\theta J(\theta)$表示梯度，$\theta^i$表示第$i$次更新后的参数值， \theta^{i+1} = \theta^i - \eta \nabla_\theta J(\theta)对于凸问题或者拟凸问题，这样的迭代最终会使得参数在最优点附近震荡，对于非凸且非拟凸的问题，这样的迭代大概率会最终在一个局部极小值附近震荡。 这里还有个问题是$J(\theta)$往往无法准确地计算，因为$J(\theta)$和所有的训练数据相关，如果每一次迭代都去完整的遍历一遍所有数据，那么时间上可能不可接受，因此有了最初的两个GD算法的变种：随机梯度下降（Stochastic gradient descent，SGD）、批梯度下降（Mini-batch gradient descent） 随机梯度下降（Stochastic gradient descent，SGD）针对$J(\theta)$和所有的训练数据相关从而导致计算耗时长的问题，随机梯度下降的思想非常朴素：每次迭代时随机选取一个样本进行训练。如下所示，其中$\nabla_\theta J(\theta;x^{(i+1)},y^{(i+1)})$表示根据第$i+1$次随机选择到的样本标签对$(x^{(i+1)},y^{(i+1)})$计算得到的损失函数。 \theta^{i+1} = \theta^i - \eta \nabla_\theta J(\theta;x^{(i+1)},y^{(i+1)})小批梯度下降（Mini-batch gradient descent）和随机梯度下降的动机相同，为了简化损失函数的计算，这里每一次使用一批数据（n个样本）来进行损失函数的计算（本质上是将多个损失函数进行求和或者求平均），如下所示： \theta^{i+1} = \theta^i - \eta \nabla_\theta J(\theta;x^{((i+1)\times n:(i+2)\times n)},y^{((i+1)\times n:(i+2)\times n)})这里需要注意的是，很多深度学习框架的实现中，SGD代表的不是随机梯度下降，而是随机小批梯度下降，即随机打乱数据之后，再使用小批梯度下降进行优化。 这几个方法没有什么本质上的区别，在数据的选择上，如果每次迭代时计算梯度所用到的数据越多，那么梯度的计算自然更加准确，但是数据过多，又会引起计算时间增加导致迭代缓慢，因此每次选择多少个数据来进行计算，即批大小（batch size）的选择就是在这两个问题上进行权衡。至于为什么要随机选择，自然是为了将数据充分混合，在pytorch的dataloader实现中，一般是在最初将数据shuffle一次，然后每次迭代都顺序选择下一批数据。 梯度下降算法的主要难点： 学习速率的选择，过大的学习速率会导致算法不能很好的收敛（在局部极小值附近大幅度震荡），因此一般使用初始大学习速率，然后学习速率逐渐衰减的方式，但是学习速率的初始值和衰减速度同样难以设置。 对于存在很多鞍点的问题，梯度下降算法理论上无法正确的处理在鞍点的情况（但实际上每次随机使用不同的数据计算梯度，很难得到一致的鞍点，总有可能随机到一些数据计算出来在原来的鞍点处又有梯度的情况，因此后面的一些说能够有助于逃离鞍点的动量化方法，我认为其实在这方面意义不是非常大，这可能也是很多论文中依旧在使用SGD，而且效果也还可以的原因） 在狭长的损失函数区域中，梯度下降算法非常容易震荡，如下图所示。 Momentum为了解决上面的最后一个问题，在SGD中可以使用动量项（Momentum term）进行改进，类似于物理上的动量概念：运动方向的改变不是瞬间变化的，而是缓慢改变，平滑过渡。动量项可以表达为： m^t = \gamma m^{t-1} + \nabla_\theta J(\theta^{t-1})有些地方也写成 m^t = \gamma m^{t-1} + (1 - \gamma)\nabla_\theta J(\theta^{t-1})其中$m^0$一般初始化为0，$0 &lt; \gamma &lt;1$，$\gamma$一般被设置为$0.9$，这个表达式的意思就是将之前的梯度方向累积下来，优化算法在迭代时，不再是直接向着梯度方向更新，而是使用动量来更新： \theta^t = \theta^{t-1}- \eta m^t这样的好处是可以避免更新方向的频繁变化，减少震荡，加速收敛。 Momentum可以抑制更新过程中的震荡，以狭长形状的损失函数为例，使用动量项之后，其更新轨迹类似下图： Nesterov accelerated gradient（NAG）NAG方法对Momentum方法进行了改进，其思想非常简单：既然需要沿着动量方向进行参数更新，那么梯度的计也可以在动量更新之后再进行，即沿着动量方向进行参数更新之后再计算梯度来修正动量，最后再使用修正后的动量来更新。 可以通俗的理解一定程度上预测了Momentum下一步的位置，然后根据未来的位置计算梯度来对动量项进行修正。 因此这里将动量定义为： m^t = \gamma m^{t-1} + \nabla_\theta J(\theta^{t-1} - \eta \gamma m^{t-1})这样的动量项也被称为Nesterov Momentum，参数更新过程依旧不变： \theta^t = \theta^{t-1} - \eta m^tMomentum和NAG的运行差别如下图所示（图片来自于《路遥知马力——Momentum - 无痛的机器学习 - 知乎专栏》），上面的是Momentum方法，下面的是NAG方法。 NAG的另外等价一种表达方式可以写成： \begin{aligned} d^t &= \gamma d^{t-1} + \nabla_\theta J(\theta^{t-1}) + \gamma(\nabla_\theta J(\theta^{t-1}) - \nabla_\theta J(\theta^{t-2}))\\ \theta^t &= \theta^{t-1} - \eta d^t \end{aligned}这个等价的推理过程可见比Momentum更快：揭开Nesterov Accelerated Gradient的真面目，这里表达的意思就是“如果这次的梯度比上次的梯度变大了，那么有理由相信它会继续变大下去，那就把预计要增大的部分提前加进来；如果相比上次变小了，那么需要动量项上需要做相应的减少”，$\gamma(\nabla_\theta J(\theta^{t-1}) - \nabla_\theta J(\theta^{t-2}))$这一项其实是利用了近似二阶导数信息。因此NAG相比于Momentum要快些。 AdagradAdagrad的主要思想是让学习速率能够自动调整，使得更新频繁的参数有更小的学习速率，更新少的参数有更大的学习速率。 这里的意思是每个参数的学习速率各不相同，使用每个参数每次的梯度平方和来计算，例如对于参数$\theta$，其第$i$次更新的梯度$g^i$，初始学习速率为$\eta$，那么第$t$次更新的时候，该参数的学习速率$\eta^t$的计算方式如下： \begin{aligned} v^t &= \sum\limits_{i=1}^t (g^i)^2\\ \eta^t &= \frac{\eta}{\sqrt{v^t + \epsilon}}\\ \end{aligned}如果每次梯度都很大，那么这个参数就被认为是频繁更新，学习速率衰减比较快，否则学习速率衰减比较慢，可以说Adagrad是一种学习速率衰减方法。 Adadelta在Adagrad中，由于$v^t$是单调递增的，因此Adagrad很容易导致提前停止训练，所以在Adadelta中将其改成不关心全局的梯度，而是只关心最近一段时间的梯度，这里将$v^t$改成如下计算方式： v^t = \gamma v_{t-1, j} + (1 - \gamma)(g^t)^2RMSPropRMSProp算法是Adadelta的一个特列：$\gamma=0.9$ Adam（Adaptive Moment Estimation）Adam其实就是在Adadelta的基础上添加了momentum，然后稍微做了些修正，修正是因为考虑到其初值是0，会对动量和二阶动量的估计造成偏差，其实这里可以看出，修正之后$\hat{m}_{1,j}$其实就是$g_{1, j}$，$\hat{v}_{1,j}$也是类似的。 \begin{aligned} m^t &= \beta_1 m^{t-1} + (1 - \beta_1)g^t\\ v^t &= \beta_2 v^{t-1} + (1 - \beta_2)(g^t)^2\\ \hat{m}^t &= \frac{m^t}{1 - \beta^t_1}\\ \hat{v}^t &= \frac{v^t}{1 - \beta^t_2} \end{aligned}参数$\theta$的更新变成： \theta^t = \theta^t - \frac{\eta}{\sqrt{\hat{v}^t}+\epsilon}\hat{m}^tAdam在实际使用中也是非常常见的优化方法，既有动量方法的优点，又能自动调整学习速率。 NAdam既然Adam都使用了动量方法，那肯定要试一下将NAG（Nesterov accelerated gradient）方法加入进来，因此就有了NAdam。 对于NAG，这里做了一些变化，首先令$\hat{\theta}^{t-1} = \theta^{t-1} - \eta \gamma m^{t-1}$，那么Nesterov Momentum可以写为： \begin{aligned} m^t &= \beta_1 m^{t-1} + (1 - \beta_1)\nabla_\theta J(\theta^{t-1} - \eta \gamma m^{t-1})\\ &=\beta_1 m^{t-1} + (1 - \beta_1)\nabla_\theta J(\hat{\theta}^t) \end{aligned}则NAG的更新可以写成： \begin{aligned} \theta^t &= \theta^{t-1} - \eta m^t\\ \hat{\theta}^t &= \theta^t - \eta \beta_1 m^t\\ &=\theta^{t-1} - \eta m^t - \eta \beta_1 m^t\\ &=\theta^{t-1} - \eta (\beta_1 m^{t-1} + (1 - \beta_1)\nabla_\theta J(\hat{\theta}^t)) - \eta \beta_1 m^t\\ &=\hat{\theta}^{t-1} - \eta (\beta_1 m^t + (1 - \beta_1)\nabla_\theta J(\hat{\theta}^t)) \end{aligned}根据NAG的思路得到了$m^t$，那么在Adam中得到$\hat{m}^t = \frac{m^t}{1 - \beta^t_1} = \frac{\beta_1 m^{t-1} + (1 - \beta_1)\nabla_\theta J(\hat{\theta}^t)}{1 - \beta^t_1}$之后，$\hat{\theta}^t$的计算变为： \begin{aligned} \hat{\theta}^t &=\hat{\theta}^{t-1} - \frac{\eta}{\sqrt{\hat{v}^t}+\epsilon}\hat{m}^t\\ &=\hat{\theta}^{t-1} - \frac{\eta}{\sqrt{\hat{v}^t}+\epsilon}\frac{\beta_1 m^{t-1} + (1 - \beta_1)\nabla_\theta J(\hat{\theta}^t)}{1 - \beta^t_1}\\ \end{aligned}这里如果用$\beta_1\hat{m}^{t-1}$来替换$\frac{\beta_1 m^{t-1}}{1 - \beta^t_1}$，则可以简化为： \hat{\theta}^t = \hat{\theta}^{t-1} - \frac{\eta}{\sqrt{\hat{v}^t}+\epsilon}(\beta_1\hat{m}^{t-1} + \frac{(1 - \beta_1)\nabla_\theta J(\hat{\theta}^t)}{1 - \beta^t_1})完整的NAdam表达如下： \begin{aligned} \hat{\theta}^{t-1} &= \theta^{t-1} - \eta \gamma m^{t-1}\\ g^t &= \nabla_\theta J(\hat{\theta}^t)\\ m^t &= \beta_1 m^{t-1} + (1 - \beta_1)g^t\\ v^t &= \beta_2 v^{t-1} + (1 - \beta_2)(g^t)^2\\ \hat{m}^t &= \frac{m^t}{1 - \beta^t_1}\\ \hat{v}^t &= \frac{v^t}{1 - \beta^t_2}\\ \hat{\theta}^t &= \hat{\theta}^{t-1} - \frac{\eta}{\sqrt{\hat{v}^t}+\epsilon}(\beta_1\hat{m}^{t-1} + \frac{(1 - \beta_1)}{1 - \beta^t_1} g^t) \end{aligned}一般如果使用Adam有效的场景，都可以使用NAdam来得到更好的效果。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[正则化方法]]></title>
    <url>%2F2020%2F05%2F12%2F%E6%AD%A3%E5%88%99%E5%8C%96%E6%96%B9%E6%B3%95%2F</url>
    <content type="text"><![CDATA[偏差和方差在监督学习中，模型的泛化误差（测试集误差）被视作偏差、方差和噪声之和。 其中，偏差表示学习算法的期望预测与真实结果的偏离程度，刻画的是算法的拟合能力。 方差则表示一定大小的训练数据扰动所导致的学习性能的变化大小，刻画的是输入扰动所造成的影响 噪声则表示在当前任务上任何学习算法所能达到的期望泛化误差的下界，刻画的是问题本身的难度。 偏差主要是因为模型的复杂度不够而引起，可以从训练集误差上体现出来。 方差主要是因为模型复杂度过高而引起，表现为测试集误差和训练集误差的差值。 模型复杂度高，容易导致方差变大，模型复杂度低，又容易导致偏差变大，因此需要平衡模型的方差与偏差，使得模型的泛化误差最小。 正则化方法正则化方法是向原始模型引入额外信息的一种方法的统称，其目的是通过为复杂模型添加正则化的方式，尽量在不影响偏差的同时降低方差。 参数范数正则化通过在损失函数中加入参数范数项来降低模型容量，从而达到正则化的效果，形如下式，其中$J$表示原始的损失函数，$\Omega(w)$表示参数的一种范数，$\alpha$参数用于控制正则化程度，$\tilde{J}(w;X,y)$表示添加了正则化之后的损失函数。 \tilde{J}(w;X,y) = J(w;X,y) + \alpha\Omega(w)L2正则化（参数范数正则化的特例，范数使用2范数）普通的损失函数可以写成如下： J(w;X,y)在权重参数上加上l2正则化项之后，损失函数如下，为方便分析，这里将$\alpha$写成$\frac{\alpha}{2}$： \begin{aligned} &\tilde{J} = \frac{\alpha}{2}w^Tw + J(w;X,y)\\ &对w的求导：\\ &\nabla_w\tilde{J} = \alpha w + \nabla_w J(w;X,y) \end{aligned}每次的参数更新可以写成如下，其中$\epsilon$表示学习速率： \begin{aligned} &w \leftarrow w - \epsilon(\alpha w + \nabla_w J(w;X,y))\\ &等价于：\\ &w \leftarrow (1 - \epsilon\alpha) w + \epsilon\nabla_w J(w;X,y) \end{aligned}从这里可以看到，学习规则发生了变化，在每次执行普通的梯度更新之前，会首先将原来的参数乘以一个常数项$(1 - \epsilon\alpha)$。 对于原来的损失函数中存在的鞍点（梯度$\nabla_w J(w;X,y) = 0$），则L2正则化将使得参数在这些点任然会得到更新，从而有助于模型逃离鞍点。（但是这个说法我认为不太对，因为加入$\alpha w$这一项，只是将鞍点的位置进行了偏移，并不是去掉了鞍点。） 为了分析l2正则化的作用，假定$J(w ^ \ast ; X , y)$是损失函数的极小值。$w^\ast$是使损失函数取得极小值的参数，先简单的对损失函数进行二次近似如下，这里没有一次项是因为在函数极小值的地方，一次导数应该为0。 \begin{aligned} \hat{J}(w;X,y) = J(w^\ast;X,y) + \frac{1}{2}(w-w^\ast)^TH(w-w^\ast) \end{aligned}对加上l2正则化项的损失函数近似形式如下： \begin{aligned} \hat{J}(w;X,y) = J(w^\ast;X,y) + \frac{1}{2}(w-w^\ast)^TH(w-w^\ast) + \frac{\alpha}{2} w^Tw \end{aligned}对损失函数进行求导如下。 \begin{aligned} \nabla_w \hat{J}(w;X,y) = \alpha w + H(w - w^\ast) \end{aligned}当$\nabla_w\hat{J}(w;X,y) = 0$时，有如下推导： \begin{aligned} \alpha w + H(w - w^\ast) = 0\\ (H + \alpha I)w = Hw^\ast\\ w = (H + \alpha I)^{-1}Hw^\ast \end{aligned}可见当$\alpha \rightarrow 0$时，$w \rightarrow w^\ast$，进一步，因为$H$是实对称矩阵，因此必定可以正交对角化，$H=Q \Lambda Q^T$，因此进一步推导如下： \begin{aligned} w &= (Q \Lambda Q^T+ \alpha I)^{-1}Q \Lambda Q^Tw^\ast\\ &=[Q (\Lambda+ \alpha I) Q^T]^{-1}Q \Lambda Q^Tw^\ast\\ &=Q (\Lambda+ \alpha I)^{-1}\Lambda Q^Tw^\ast \end{aligned}其中，假设$\Lambda$如下： \begin{bmatrix} &\lambda_1 &0 &0 &\cdots &0\\ &0 &\lambda_2 &0 &\cdots &0\\ &0 &0 &\lambda_3 &\cdots &0\\ &\vdots &\vdots &\vdots &\ddots &\vdots\\ &0 &0 &0 &\cdots &\lambda_n\\ \end{bmatrix}则$(\Lambda+ \alpha I)^{-1}\Lambda$如下： \begin{bmatrix} &\frac{\lambda_1}{\lambda_1 + \alpha} &0 &0 &\cdots &0\\ &0 &\frac{\lambda_2}{\lambda_2 + \alpha} &0 &\cdots &0\\ &0 &0 &\frac{\lambda_3}{\lambda_3 + \alpha} &\cdots &0\\ &\vdots &\vdots &\vdots &\ddots &\vdots\\ &0 &0 &0 &\cdots &\frac{\lambda_n}{\lambda_n + \alpha}\\ \end{bmatrix}这就相当于在原损失函数极小值点的Hession矩阵$H$的特征向量方向上，将$w^\ast$进行了缩放，而且特征值$\lambda_i$越小的方向，$\alpha$对其影响越大，缩小得越大，即加入l2正则化项之后，模型参数倾向于对损失函数影响显著的方向（Hession矩阵特征值大的方向）偏移。 以最小二乘线性回归为例，其损失函数如下： (Xw-y)^T(Xw-y)如果加上l2正则化，则损失函数变成如下形式： (Xw-y)^T(Xw-y) + \frac{1}{2}\alpha w^Tw那么线性回归的解就从： w = (X^TX)^{-1}X^Ty变成了： w = (X^TX + \alpha I)^{-1}X^Ty其中$X$可以写成如下,其中$x_{ij}$表示第i个样本$x_i$的第j维： X=\begin{bmatrix} &x_{11} &x_{12} &\cdots &x_{1m}\\ &x_{21} &x_{22} &\cdots &x_{2m}\\ &\vdots &\vdots &\ddots &\vdots\\ &x_{n1} &x_{n1} &\cdots &x_{nm}\\ \end{bmatrix}则$X^TX$可以表示如下： X^TX=\begin{bmatrix} &\sum_i^nx_{i1}x_{i1} &\sum_i^nx_{i1}x_{i2} &\cdots &\sum_i^nx_{i1}x_{im}\\ &\sum_i^nx_{i2}x_{i1} &\sum_i^nx_{i2}x_{i2} &\cdots &\sum_i^nx_{i2}x_{im}\\ &\vdots &\vdots &\ddots &\vdots\\ &\sum_i^nx_{im}x_{i1} &\sum_i^nx_{im}x_{i2} &\cdots &\sum_i^nx_{im}x_{im}\\ \end{bmatrix}$X^TX$同样可以正交对角化，$X^TX = Q \Lambda Q^T$，这里的$\Lambda$对角线上的值是$X$奇异值的平方，之后的推导和上面相同，可见，加入l2正则化项之后，模型参数倾向于对损失函数影响显著的方向偏移，从而忽略掉数据中的一些干扰，增强模型泛化能力。 L1正则化（参数范数正则化的特例，范数使用1范数）进行l1正则化之后的损失函数如下： \tilde{J} = \alpha ||w||_1 + J(w;X,y)对其进行求导： \nabla_w\tilde{J} = \alpha\ sign(w) + \nabla_wJ(w;X,y)从这里可以看出l1正则化项对导数的影响是一个固定值，和L2有很大区别（L2是固定的缩放）。 使用在L2正则化分析中的损失函数近似方法，将原本的损失函数二次近似为$\hat{J}(w;X,y) = J(w^\ast;X,y) + \frac{1}{2}(w-w^\ast)^TH(w-w^\ast)$，其导数如下： \nabla_w\hat{J} = H(w - w^\ast)加上正则化项之后，其损失函数的二次近似可以表示为： \hat{J}(w;X,y) = J(w^\ast;X,y) + \frac{1}{2}(w-w^\ast)^T H(w-w^\ast) + \alpha||w||_1如果将Hessian矩阵简化成了对角阵（这个类似对数据进行PCA之类的操作，将数据之间的相关性去掉了，因此Hessian矩阵变成对角阵，这样分析要简单一些）。 则： \hat{J}(w;X,y) = J(w^\ast;X,y) + \sum_i[\frac{1}{2}H_{i,i}(w_i-w_i^\ast)^2 + \alpha|w_i|]则对$w_i$进行求导，如下： \nabla_{w_i}\hat{J}(w;X,y) = H_{i,i}(w_i - w_i^\ast) + \alpha\ sign(w_i)可以看出，加了L1正则化之后梯度为0的点变成了如下： w_i = sign(w_i^\ast)max\{|w_i^\ast| - \frac{\alpha}{H_{i,i}}, 0\}描述成图像大概如下： 可以看出，l1使得权重变得更加稀疏，这在特征选择方面非常有用。 数据集扩充数据集扩充的本质是要求模型对于相似的输入（例如图片的平移、缩放、添加噪声、旋转等不改变图片语义的操作），必须有相似的输出。 对噪声的鲁棒性除了在原始输入上添加噪声之外，还有两种在隐层添加噪声的方法： dropout在隐层的输出上添加噪声。 直接对学习到的权重添加噪声，可以视作模拟贝叶斯方法，认为模型权重有一定的随机性，同时也鼓励模型进入到参数空间中比较稳定的区域。 输出目标噪声数据标签很可能包含一定的错误，而且在分类任务重，由于softmax\sigmoid函数的特点，模型不能拟合绝对的0和1标签，因此标签平滑技术通过把标签进行平滑，可以缓解这样的问题。 稀疏表示类似L1正则化产生的参数稀疏效果，在隐层的输出上，也可以尝试使用L1正则化，让隐层的输出倾向于变得稀疏。 参数绑定和参数共享 某些足够相似的任务，其参数也应该相似，因此可以将参数之间相减的范数添加到损失函数中进行训练。 CNN中的卷积层通过“提取类似特征所需权重相同”的思想，大量的权重共享，显著降低了模型的参数量，同时使得模型更加容易训练。 训练早停验证误差不再下降之后，及时停止训练模型，不需要等到训练集误差最小，这样有助于防止模型过拟合。 多任务学习一些任务需要相同或者相似的特征，让共享大部分参数的多个模型来同时进行多个任务的拟合，可以起到使共享部分的参数学习到更优值的效果。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[CNN权重初始化]]></title>
    <url>%2F2020%2F05%2F11%2FCNN%E6%9D%83%E9%87%8D%E5%88%9D%E5%A7%8B%E5%8C%96%2F</url>
    <content type="text"><![CDATA[权重初始化权重初始化的意义CNN在训练开始之前，首先需要进行权重的初始化，如果初始化搞得好，例如直接初始化到最优点，那么训练步骤就可以省略了，当然这个概率基本为零，但是初始化如果做得好，让避免梯度爆炸或者消失，使梯度更加均匀，跳开一些平坦区域或者局部最优点也是可能的，这就是为什么神经网络如果不固定参数的初始化，每次训练得到的结果可能差别很大的原因。 初始化这里有个要注意的是：所有权重不能同时初始化为0。因为CNN中的反向传播如下所示，具体推导和含义可以查看我的另一篇文章CNN的反向传播。如果全都初始化为0，那么所有的$\delta_i$全是0，从下面的式子可以看出，$\frac{\partial E}{\partial w_i}$和$\frac{\partial E}{\partial b_i}$恒为零，也就导致会导致学习过程中权重完全不变，那么学习过程也就失去了意义。 \begin{aligned} \delta_l = \frac{\partial E}{\partial z_l} &= diag(\sigma_l'(z_l))E'\\ \delta_{i-1} &= \begin{cases} diag(\sigma_i'(z_{i-1}))w_i^T\delta_i &如果第i层是全连接层\\ padpad(\delta_i) \star rot_{180}trans(w_i) \odot\sigma'(z_{i-1}) &如果第i层是卷积层\\ M\odot upsample(\delta_i) &如果第i层是池化层 \end{cases}\\ \frac{\partial E}{\partial w_i} &= \begin{cases} \delta_il_{i-1}^T &如果第i层是全连接层\\ padpad(\delta_i) \star rot_{180}trans(l_{i-1}) &如果第i层是卷积层\\ \end{cases}\\ \frac{\partial E}{\partial b_i} &= \delta_i,\ i=1,2,...,l \end{aligned}另外，初始化的选择还和激活函数有关，如果使用ReLU激活函数，那么如果初始化权重让ReLU函数的输入全是小于0的，那得到的输出也是全0，并且不能进行有效的更新（ReLU死节点），如果使用Sigmoid激活函数，那么初始化权重如果使得Sigmoid函数的输入值偏离原点太远，也会导致梯度非常小（Sigmoid函数饱和）。 一般在初始化时，选择随机初始化方式，目前主流的初始化方式有：随机分布的初始化、Xavier、MSRA等。 随机分布的初始化一般选择Gaussian分布或者均匀分布，这个没啥技术含量，主要是让均值和方差在合适的位置就好（参考前面对于不同激活函数的初始化要求的分析）。 Xavier初始化Xavier初始化来源于论文《Understanding the difficulty of training deep feedforward neural networks》，其中以tanh为激活函数，对权重初始化的方差提出了一些要求。 首先从正向传播过程来看： 如果第$i$层输入的方差是$\sigma^{i-1}_x$，权重初始化的方差$\sigma^i_w$，如果是卷积层，那么对于输出特征图的一个像素$z^{i+1}_j = \sum\limits_{k=1}^n x^{i-1}_k \times w^i_k$，其中$n = channel_{in} \times k_w \times k_h$，即输入通道数乘以卷积核面积。因为$x_i$，$w_i$相互独立（方差的和等于和的方差，方差的乘积等于乘积的方差），因此方差$\sigma^i_z = n^i \times \sigma^i_w \times \sigma^{i-1}_x$，因为tanh激活函数在原点附近区域可以近似为$f(x) = x$这样一个函数，所以论文中假设$x^i = z^i$，所以根据上面的分析，$\sigma^i_x = \sigma^i_z = n^i \times \sigma^i_w \times \sigma^{i-1}_x$，进一步可以得到： \begin{aligned} \sigma^i_x = \sigma^0_x\prod\limits_{k=1}^i(n^k \times \sigma^k_w) \end{aligned}其中$\sigma^0_x$为模型的输入。从这个结果看，如果每一层的$\sigma^k_w$过大，则会引起深层的方差爆炸，如果过小，又会引起深层的方差消失。如果想要保证每一层的输入和输出的方差基本一致，需要$\sigma^k_w = \frac{1}{n^k}$。 其次从反向传播过程来看: 根据最前面列出的CNN反向传播过程，如果假设激活函数大致等价于$f(x) = x$，那么激活函数的导数$\sigma’(z_{i-1}) = 1$，因此同样有类似的结论$Var(\frac{\partial E}{\partial w_i}) = Var(\frac{\partial E}{\partial w_j}) \prod\limits_{k=i}^{j-1}(m^{k+1} \sigma^k_w)$，其中$m^{k+1}$表示第$k+1$层的每个输入值连接的权重个数，如果第$k+1$层是卷积层，则$m = channel_{out} \times k_w \times k_h$。 同样，如果要保证反向传播时梯度方差基本不变，则需要满足$\sigma^k_w = \frac{1}{m^{k+1}}$ 因此根据以上前向传播和反向传播过程的分析，论文均衡了两个分析的结果，提出了Xavier初始化： \sigma^k_w = \frac{2}{m^{k+1} + n^k}如果使用均匀分布初始化，因为要满足权重分布在0附近的假设（否则上面的假设激活函数等价于$f(x) = x$不成立），我们选择$[-a, a]$范围的均匀分布，其方差$\frac{a^2}{3}$需要满足： \frac{a^2}{3} = \sigma^k_w = \frac{2}{m^{k+1} + n^k}可以得出$a = \sqrt{\frac{6}{m^{k+1} + n^k}}$，因此Xavier初始化建议使用$[-\sqrt{\frac{6}{m^{k+1} + n^k}}, \sqrt{\frac{6}{m^{k+1} + n^k}}]$范围的均匀分布，其中$m^{k+1}$表示第$k+1$层的每个输入值的连接个数，$n^k$表示第$k$层每个输出值的连接个数。 MSRA初始化（一些深度学习框架中称为he_normal）在论文 《Delving Deep into Rectifiers:Surpassing Human-Level Performance on ImageNet Classification》中提出了PReLU激活函数和MSRA初始化方法。 MSRA初始化和Xavier初始化的动机类似，不过MSRA初始化是对于ReLU（或者PReLu）激活函数在原点附近进行分析，且使用Gaussian分布。 因为是ReLU激活函数，和上面Xavier初始化的分析类似，从正向传播的角度可以得到$\sigma^k_w = \frac{2}{n^k}$，从反向传播的角度可以得到$\sigma^k_w = \frac{2}{m^{k+1}}$。（这里的分析使用了x为0均值的假设） 如果是PReLU，则变成$\sigma^k_w = \frac{2}{(a^2 + 1)n^k}$和$\sigma^k_w = \frac{2}{(a^2 + 1)m^{k+1}}$，$a$为PReLU负区间的斜率。 因此MSRA初始化方法建议使用均值为0，方差为$\sigma^k_w = \frac{2}{(a^2 + 1)n^k}$或者$\sigma^k_w = \frac{2}{(a^2 + 1)m^{k+1}}$的Gaussian分布初始化。这里没有像Xavier初始化那样使用两个推理结果的折中，论文中说两个初始化方式差不多，都能够使模型收敛。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[CNN感受野]]></title>
    <url>%2F2020%2F05%2F09%2FCNN%E6%84%9F%E5%8F%97%E9%87%8E%2F</url>
    <content type="text"><![CDATA[感受野（Receptive Filed, RF）由于卷积层是一种局部连接操作，对于卷积层的输入和输出，在输出的特征图上，一个像素的值，只取决于输入特征图上的一部分区域，如图所示： 感受野的大小从原始输入图像开始，如果第一层卷积的卷积核是$3\times 3$大小，那么第一层卷积层的输出特征图上，左上角第一个像素点连接了原始输入图像左上角的$3\times3$大小的范围（这个范围被称作这个像素对于输入图像的感受野），即这个特征图上，每个像素对于输入图像的的感受野大小是$3\times 3$，如果第二层卷积也是$3\times 3$大小的卷积核，那么第二层输出的特征图上的一个像素对于第一层输出的特征图的感受野是$3\times 3$，但是对应到原图上的感受野大小和范围就要依赖于第一层卷积层的卷积步长参数了，如果第一层卷积的卷积步长为1，那么可以得出第二层输出的特征图上，一个像素对应于原始输入图像上$5\times 5$的区域，即这层特征图上每个像素对于输入图像的感受野大小是$5\times 5$。 对于特征图上的一个像素，其感受野之外的区域不影响该像素的值，因此对于该像素，不能从其值上获取其感受野之外的信息。 感受野的步长从原始输入图像开始，如果第一层卷积的卷积核是$3\times 3$大小，卷积步长是1，那么在第一层卷积层的输出特征图上坐标为$(0, 0)$的第一个像素的感受野是输入图像的左上角从$(0,0)$坐标开始的$3\times 3$的区域，而第一层卷积层的输出特征图上坐标为$(0, 1)$的像素则对应输入图像的左上角从$(0,1)$坐标开始的$3\times 3$的区域，这两个区域之间的步长为1，也称为第一层卷积层的输出特征图对于输入图像的感受野步长为1。 感受野的padding感受野的padding和卷积层的padding是类似的，指的就是感受野分布在对应特征图上时，超出特征图的范围大小，这个是由于卷积层引入padding导致的。 感受野的理解感受野可以理解为将信息经过的层合并成一个大核卷积层（虽然一个大卷积层不能完全实现多层堆叠的计算，例如其中包含了激活、池化等层，但是在感受野大小、步长、padding方面是等效的），其中卷积核大小就是感受野大小，卷积步长就是感受野步长。 感受野大小和步长的计算一个适合口算的方式是从后往前计算，假设现在要计算第$i+m$层的输出特征图对于第$i$层特征图的感受野大小和步长，那么首先看第$i+m$层的输出特征图对于第$i+m-1$层的输出特征图的感受野大小和步长。 这里只考虑卷积核大小和步长在长宽上相同的情况，否则的话，需要按照长宽分别计算，记第$n$层输出特征图对于第$m$层输出特征图的感受野大小为$F^n_m$，感受野步长为$S^n_m$，感受野padding记为$P^n_m$，如果第$i$层是卷积层，则记其卷积核大小为$k_i$，记其卷积步长为$s_i$，记padding大小为$p_i$，如果第$i$层是池化层，则同样记其池化核大小为$k_i$，记其池化步长为$s_i$，记padding大小为$p_i$，那么不论对池化层还是卷积层，很显然有： \begin{aligned} F^n_n &= 1\\ S^n_n &= 1\\ P^n_n &= 0\\ F^n_m &= (F^n_{m+1} - 1) \times s_{m+1} + k_{m+1} = F^n_{m+1} \times s_{m+1} - s_{m+1} + k_{m+1}\\ S^n_m &= S^n_{m+1} \times s_{m+1}\\ P^n_m &= P^n_{m+1} * s_{m+1} + p_{m+1} \end{aligned}VGG模型的配置如图所示： 这里以VGG-16为例，其中的卷积层全是卷积核大小为3，卷积步长为1，padding为1，其中的池化层全是池化核大小为2，池化步长为2，padding为0，记第四个maxPooling之前的特征图即Conv4-3为第13层的输出，记输入的$224\times 224$大小的图片为第0层的输出。 这里尝试计算$F^{13}_0$, $S^{13}_0$, $P^{13}_0$: \begin{aligned} F^{13}_0 &= F^{13}_1 \times s_1 - s_1 + k_1\\ &= F^{13}_1 \times 1 - 1 + 3\\ &= (F^{13}_2 \times s_2 - s_2 + k_2) \times 1 - 1 + 3\\ &= (F^{13}_2 \times 1 - 1 + 3) \times 1 - 1 + 3\\ &\cdots\\ &= 1 \times 1 - 1 + 3) \times 1 - 1 + 3) \times 1 - 1 + 3) \times 2 - 2 + 2) \times 1 - 1 + 3) \times 1 - 1 + 3) \times 1 - 1 + 3) \times 2 - 2 + 2) \times 1 - 1 + 3) \times 1 - 1 + 3) \times 2 - 2 + 2) \times 1 - 1 + 3) \times 1 - 1 + 3\\ &= 1 + 2 + 2 + 2)\times 2 + 2 + 2 + 2)\times 2 + 2 + 2)\times 2 + 2 + 2 &= 92\\ S^{13}_0 &= S^{13}_1 \times s_1\\ &= S^{13}_2 \times s_2 \times s_1\\ &= \cdots\\ &= 1 \times 1\times 1\times 1\times 2\times 1\times 1\times 1\times 2\times 1\times 1\times 2\times 1\times 1\\ &= 8\\ P^{13}_0 &= P^{13}_1 * s_1 + p_1\\ &=P^{13}_2 * s_2 + p_2) * s_1 + p_1\\ &= \cdots\\ &=0 * 1 + 1) * 1 + 1) * 1 + 1) * 2 + 0) * 1 + 1) * 1 + 1) * 1 + 1) * 2 + 0) * 1 + 1) * 1 + 1) * 2 + 0) * 1 + 1) * 1 + 1\\ &= 42 \end{aligned}即Conv4-3的输出特征图上，每个像素的感受野在原始输入图片上的大小是92，padding 42，步长为8的分布，因为Conv4-3的输出特征图大小是$28\times 28$，所以一共有$28\times 28$个感受野，按照这个排列，其一行所占的像素可以计算为$92 + (28 - 1) \times 8 = 308$正好等于$224 + 2 \times 42$。 对于ResNet、Inception这种包含多个路径模型来说，其某一层的感受野，应该取其中感受野最大的路径来计算。 空洞卷积（Dialated Convolution）有助于增加感受野，可以将其卷积核看做一个填充了0值的大卷积核。 有效感受野（Efficient Receptive Filed）一个特征图上某个像素的感受野虽然可以反应哪些位置可以影响该像素的值，但是在其感受野中，不同区域的影响程度是不一样的，论文《Understanding the Effective Receptive Field in Deep Convolutional Neural Networks》中讨论了这个问题，论文中指出，如果卷积核全都初始化为相同的值，那么感受野中的原图像素对于特征像素的影响大小成正态分布，这取决于中间的卷积核权重，并在模型训练前后会发生变化，如图所示，其中越亮的区域代表有越大的影响。 基于这个现象，论文中提出了有效感受野的概念，即真正能影响特征图像素值的范围只占理论感受野的一小部分。 从上图中可以看出，即使在训练完成之后，有效感受野也不能铺满整个理论感受野范围，因此在设计模型时，还需要让感受野超出目标大小，否则很难提取到完整的目标信息。 根据论文中给出的示意图，不同的初始化方式对有效感受野有一些影响，随着层数加深，理论感受野增大，有效感受野占理论感受野的比重越来越小。 为了使得感受野中的影响分布更加均匀，论文中提出加大卷积核边缘权重，减少卷积核中心权重的初始化方式。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[CNN的反向传播]]></title>
    <url>%2F2020%2F05%2F07%2FCNN%E7%9A%84%E5%8F%8D%E5%90%91%E4%BC%A0%E6%92%AD%2F</url>
    <content type="text"><![CDATA[在卷积神经网络中，主要由三种层结构构成：卷积层、池化层、全连接层。 全连接层的反向传播全连接层的反向传播比较简单，使用单纯的BP算法即可，这里先来复习一下全连接层的BP算法： 对于$l$层神经网络,输入$x \in R^n$，标签$y \in R^c$，第$i$层权重表示为$w_i \in R^{O_i \times I_i}, I_1 = n，O_l = c$，第$i$层偏移表示为$b_i \in R^{O_i}$，第$i$层激活函数表示为$\sigma_i$，这一般是个逐元素函数，第$i$层输入即第$i-1$层的输出，表示为$l_{i-1}$，其中$l_0 = x, z_i = w_i l_{i-1} + b_i, l_i = \sigma_i(z_i)$ loss函数记为$E(l_l, y)$，BP算法每次更新$w_i = w_i - \eta \frac{\partial E}{\partial w_i}$， $b_i = b_i - \eta \frac{\partial E}{\partial b_i}$，即让参数像梯度最小的方向前进。 首先定义$E$对$l_l$的偏导为$\frac{\partial E}{\partial l_l} = E’$，这个值由loss函数决定。因此 \begin{aligned} dE &= E'^Tdl_l\\ &=E'^T(\sigma_l'(z_l) \odot (dz_l))\\ &=E'^Tdiag(\sigma_l'(z_l))da_l\\ \Rightarrow \frac{\partial E}{\partial z_l} &= diag(\sigma_l'(z_l))E' \end{aligned}这里把$\frac{\partial E}{\partial z_l}$记作$\delta_l$ 因为： \begin{aligned} da_i &= w_idl_{i-1}\\ &= w_i(\sigma_i'(z_{i-1}) \odot (dz_{i-1}))\\ &=w_idiag(\sigma_{i-1}'(z_{i-1}))dz_{i-1}\\ \Rightarrow \frac{\partial z_i}{\partial z_{i-1}} &= diag(\sigma_{i-1}'(z_{i-1}))w_i^T\\ \end{aligned}所以定义: \begin{aligned} \delta_i &= \frac{\partial E}{\partial z_i},\ i=1,2,...,l-1\\ \Rightarrow \delta_{i-1} &= \frac{\partial z_i}{\partial z_{i-1}}\frac{\partial E}{\partial z_i},\ i=2,...,l\\ &= diag(\sigma_{i-1}'(z_{i-1}))w_i^T\delta_i\\ \end{aligned}现在再来考虑$E$对$w_{l-k}$的导数： \begin{aligned} dE &= \frac{\partial E}{\partial z_{l-k}}^Tdz_{l-k}\\ &= \delta_{l-k}^T(dw_{l-k}l_{l-k-1} + db_{l-k})\\ &= tr(\delta_{l-k}^Tdw_{l-k}l_{l-k-1} + \delta_{l-k}^Tdb_{l-k})\\ &= tr(l_{l-k-1}\delta_{l-k}^Tdw_{l-k} + \delta_{l-k}^Tdb_{l-k})\\ \Rightarrow \frac{\partial E}{\partial w_{l-k}} &= \delta_{l-k}l_{l-k-1}^T\\ \Rightarrow \frac{\partial E}{\partial b_{l-k}} &= \delta_{l-k} \end{aligned}这里的变换属于标量对矩阵求导$d f = tr((\frac{\partial f}{\partial X}) ^ T dX)$，且用到了迹的一个性质：$tr(A B) = tr(B A)$，其中$A$和$B^T$大小相同 全连接层的BP算法看起来很复杂，其实非常简单，只要使用以下几个等式即可求出任一层的权重和偏置的导数： \begin{aligned} \delta_l = \frac{\partial E}{\partial z_l} &= diag(\sigma_l'(z_l))E'\\ \delta_{i} = \frac{\partial E}{\partial z_i} &= diag(\sigma_i'(z_i))w_{i+1}^T\delta_{i+1},\ i=1,2,...,l-1\\ \frac{\partial E}{\partial w_i} &= \delta_il_{i-1}^T,\ i=1,2,...,l\\ \frac{\partial E}{\partial b_i} &= \delta_i,\ i=1,2,...,l \end{aligned}卷积层的反向传播对于卷积层而言，和前面的定义类似，只不过其表达式变为：$l_0 = x, z_i =l_{i-1} \star w_i + b_i, l_i = \sigma_i(z_i)$，其中$\star$表示卷积操作，$w_i \in R^{H\times W\times C_{i}\times C_{i-1}}$，$x\in R^{H\times W\times C_0}$，$z_i,l_i \in R^{H\times W\times C_i}$，$b_i \in R^{C_i}$。 按照之前全连接层的反向传播套路，自然也希望首先定义 \begin{aligned} \delta_l &= \frac{\partial E}{\partial z_l} = diag(\sigma_l'(z_l))E'\\ \delta_{i} &= \frac{\partial E}{\partial z_i} \end{aligned}那么第一个问题是如何根据$\delta_{i+1}$求出$\delta_{i}$。 因为： \begin{aligned} \delta_{i} &= \frac{\partial E}{\partial z_i}\\ &= \frac{\partial z_{i+1}}{\partial z_i}\frac{\partial E}{\partial z_{i+1}}\\ &= \frac{\partial z_{i+1}}{\partial z_i}\delta_{i+1} \end{aligned}所以问题在于如何求$\frac{\partial z_{i+1}}{\partial z_i}$，如果$i+1$层是个卷积层，那么$z_{i+1} = \sigma_i(z_i) \star w_{i+1} + b_{i+1}$，这里想使用一般的方法求解$\frac{\partial z_{i+1}}{\partial z_i}$是很困难的，下面直接对$\delta_{i}$给出一般化的表达。 卷积操作的具体分析需要画图讨论，这里不再赘述，定义一个卷积操作，$O = I \star W$，其中卷积核$W$的大小为$H_k\times W_k\times C_o\times C_i$，输入特征图的大小为$H\times W\times C_i$，卷积操作的padding大小为$P_H,P_W$，stride大小为$S_H,S_W$，则输出特征图的大小为$\lceil\frac{H + 2P_H - H_k + 1}{S_H}\rceil\times \lceil\frac{W + 2P_W - H_k + 1}{S_W}\rceil\times C_o$，注意这里$0 \le P_H \le H_k - 1$，$0 \le P_W \le W_k - 1$，否则过多的padding没有意义。 设L为损失函数，令$\frac{\partial E}{\partial O}$记作$\delta_O$，其大小和$O$相同。 \begin{aligned} \frac{\partial E}{\partial I} &= padpad(\delta_O) \star rot_{180}trans(W) \end{aligned}其中$padpad(\delta_O)$是将$\delta_O$进行外部padding和内部padding得到的，其外部padding大小：$P’_H = H_k - P_H - 1$和$P’_W = W_k - P_W - 1$，内部padding大小为$S_H - 1$和$S_W - 1$。 $rot_{180}trans(W)$则首先需要将$W$在$H_k\times W_k$大小上，旋转180度，然后对其中每个像素（其实每个像素都是个$C_o\times C_i$大小的矩阵）求转置得到，最终的形状是$H_k\times W_k\times C_i\times C_o$，其中$rot_{180}trans(W)_{i,j,k,l} = W_{H_k-i+1,W_k-j+1,l,k},\ i=1,2,…,H_k,\ j=1,2,…,W_k,\ k=1,2,…,C_o,\ l=1,2,…,C_i$ 则有: \delta_{i} = padpad(\delta_{i+1}) \star rot_{180}trans(w_{i+1}) \odot\sigma_{i}'(z_i)其中$\odot$表示逐元素乘法，由于这里不是向量和向量乘积，因此不能像之前一样表示成$diag$矩阵乘以向量的形式。 $\delta_i,\ i=1,2,…,l$能够求出来了，接下来的问题是如何根据$\delta_i$求出$\frac{\partial E}{\partial w_i}$。 因为$a_i =l_{i-1} \star w_i + b_i$，将$a_i$、$l_{i-1}$和$w_i$都旋转180度之后，可以看成$rot180(a_i) = rot180(w_i) \star rot180(l_{i-1}) + b_i$，这里将$l_{i-1}$看成卷积核，卷积核大小$\hat{W}_k = W, \hat{H}_k = H$，Padding变成了$\hat{P}_H = H - H_k + P_H$和$\hat{P}_W = W - W_k + P_W$，stride不变，则按照上面的分析结果，对$rot_{180}(\delta_i)$和$rot_{180}(l_{i-1})$做同样的变换： \begin{aligned} \frac{\partial E}{\partial w_i} &= rot_{180}(padpad(rot_{180}(\delta_i)) \star rot_{180}trans(rot_{180}(l_{i-1})))\\ &= rot_{180}(padpad(rot_{180}(\delta_i)) \star trans(l_{i-1}))\\ &= padpad(\delta_i) \star rot_{180}trans(l_{i-1}) \end{aligned}其中$padpad(rot180(\delta_i))$是将$rot180(\delta_i)$进行外部padding和内部padding得到的，其外部padding大小：$P’_H = \hat{H}_k - \hat{P}_H - 1 = H - H + H_k - P_H - 1 = H_k - P_H - 1$和$P’_W = \hat{W}_k - \hat{P}_W - 1 = W_k - P_W - 1$，内部padding大小为$S_H - 1$和$S_W - 1$，可以看出来这里的padpad操作和之前的padpad操作是一样的，两个地方完美等价。 因此如果知道了$\delta_i$，那么： \frac{\partial E}{\partial w_i} = padpad(\delta_i) \star rot_{180}trans(l_{i-1})因此如果第$i$层是个卷积层，那么这一层的反向传播核心公式如下： \begin{aligned} \delta_{i-1} &= padpad(\delta_i) \star rot_{180}trans(w_i) \odot\sigma_{i-1}'(z_{i-1})\\ \frac{\partial E}{\partial w_i} &= padpad(\delta_i) \star rot_{180}trans(l_{i-1})\\ \frac{\partial E}{\partial b_i} &= \delta_{i} \end{aligned}池化层的反向传播池化层没有参数，如果第$i$层是池化层，其反向传播主要需要计算$\delta_i$和$\delta_{i-1}$的关系即可。 池化层的$\delta_i$和$\delta_{i-1}$的关系取决于池化的类型，如果是最大池化，则需要构造一个非0即1的掩码矩阵，用于标记哪些位置被向前传播，如果是平均池化，则将权重1平均分配到池化核大小的窗口中，以此来构造掩码矩阵 首先上采样$\delta_i$，使其和$\delta_{i-1}$的大小相同，然后根据池化类型，构造掩码矩阵$M$，则$\delta_{i-1} = M\odot upsample(\delta_i)$ CNN反向传播的总结对于共$l$层的CNN，如果第$i$层是全连接层，则其权重表示为$w_i \in R^{O_i \times I_i},O_l = c$，第$i$层偏移表示为$b_i \in R^{O_i}$，第$i$层激活函数表示为$\sigma_i$，这一般是个逐元素函数，第$i$层输入即第$i-1$层的输出，表示为$l_{i-1}$，其中$z_i = w_i l_{i-1} + b_i, l_i = \sigma_i(z_i)$ 如果第$i$层是卷积层，则其表达式变为：$z_i =l_{i-1} \star w_i + b_i, l_i = \sigma_i(z_i)$，其中$\star$表示卷积操作，$w_i \in R^{H\times W\times C_{i}\times C_{i-1}}$，$x\in R^{H\times W\times C_0}$，$z_i,l_i \in R^{H\times W\times C_i}$，$b_i \in R^{C_i}$。 loss函数记为$E(l_l, y)$。 这里默认最后一层即第$l$层为全连接层。 \begin{aligned} \delta_l = \frac{\partial E}{\partial z_l} &= diag(\sigma_l'(z_l))E'\\ \delta_{i-1} &= \begin{cases} diag(\sigma_{i-1}'(z_{i-1}))w_i^T\delta_i &如果第i层是全连接层\\ padpad(\delta_i) \star rot_{180}trans(w_i) \odot\sigma_{i-1}'(z_{i-1}) &如果第i层是卷积层\\ M\odot upsample(\delta_i) &如果第i层是池化层 \end{cases}\\ \frac{\partial E}{\partial w_i} &= \begin{cases} \delta_il_{i-1}^T &如果第i层是全连接层\\ padpad(\delta_i) \star rot_{180}trans(l_{i-1}) &如果第i层是卷积层\\ \end{cases}\\ \frac{\partial E}{\partial b_i} &= \delta_i,\ i=1,2,...,l \end{aligned}其中$padpad$表示内部padding（列和行中每两个元素之间）：$(S_H - 1, S_W - 1)$和外部padding：$(H_k - P_H - 1, W_k - P_W - 1)$，$rot_{180}$表示在空间维度(H,W)上旋转180度，$trans$表示在通道维度上转置，$M$表示池化操作的掩码。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[CNN相关知识]]></title>
    <url>%2F2020%2F05%2F07%2FCNN%E7%9B%B8%E5%85%B3%E7%9F%A5%E8%AF%86%2F</url>
    <content type="text"><![CDATA[使用CNN这么久了，最近抽点时间来复习一下CNN相关的一些基础内容。 CNN中的基本操作卷积层一般的神经网络中，每一层是上层输出到该层输出的全连接，但是对于图像数据，如果使用全连接操作，参数量会变得非常大，难以计算和优化而且容易过拟合。 卷积层有一维卷积、二维卷积、三维卷积几种常见的类型，这里主要讨论二维卷积，其他的卷积只是输入的维度数量不同，并无特殊。 在Pytorch框架下的CNN实现中，卷积层的输入一般是$(B\times C\times H\times W)$的形状（其他框架例如Tensorflow，使用的输入形状是$(B\times H\times W\times C)$，只是维度顺序不同），其中$B$是代表batch size，表示不同的图片样本，$C$代表通道数量，例如原始彩色图片一般是三个通道：R、G、B，最后的$H,\ W$是特征图的像素高度和像素宽度。 卷积层的参数主要有两个：Kernel、Bias。 其中Kernel的形状是$(H_k\times W_k\times C_o\times C_i)$，$H_k, W_k$表示卷积核的大小，例如下图的卷积核大小为$3\times 3$，$C_i$表示卷积核的通道数，如果输入的特征图是彩色图像，即一个三通道图片，那么$C_i=3$即表示在每一个像素点，对其三个通道进行全连接操作，$C_o$则是对每个像素所有通道进行全连接操作的输出通道数，也可以理解为卷积核个数（一个卷积核输出一个通道）。 Bias一般是个$R^{C_o}$的向量，在卷积操作结束之后，对输出的特征图进行偏移，每个通道共用其中的一个偏置参数。 卷积操作如图所示： 卷积层的参数量和计算量参数量即卷积核的大小加上偏置的大小：$H_k\times W_k\times C_o\times C_i + C_o$计算量受到步长，padding方式等影响，从输入的大小来看不好计算，但是可以根据输出的特征图大小来计算：$H’ \times W’ \times (H_k\times W_k\times C_o\times C_i + C_o)$ 对卷积操作的理解卷积操作的本质就是在特征图上使用相同的参数滑动进行局部的全连接，这样做能够减少参数量和计算量，并且因为共享权值，在进行优化时更加容易。 卷积层有一些主要特点： 权值共享：在图像的不同部分，使用相同的特征提取是有意义的，这也是卷积层具备平移不变性的原因。 稀疏连接：对于一个输出值只和输入的一部分相关。 卷积操作可以看做一种局部特征的提取，例如一些边缘检测算子，实际上就是在图片上使用相同的参数滑动进行乘法操作（匹配特征方向），将代表边缘的像素点赋予更大的像素值，从而在结果图片中将边缘表示为亮的线条，这里就是在提取局部的边缘特征并保留其位置信息，最终组成整幅图片的边缘表示。 CNN的反向传播这部分内容可见我的另一篇文章CNN的反向传播 激活函数激活函数一般是一个非线性的逐元素的函数，例如Sigmoid和ReLU。 为什么要使用激活函数在NN中，如果不使用一些非线性函数作为激活函数，那么模型的深度将毫无意义（仿射变换之后，在进行仿射变换，依旧等效于一个仿射变换）。在CNN中也有类似的问题，如果多层卷积堆叠，中间没有非线性处理，那么完全可以用一个大核卷积层来进行替换，虽然会导致运算量上升，但是由于小核卷积的参数共享更严重，所以多个小核卷积层的表达能力还不如一个大核卷积层。 激活函数不论是加在NN中还是CNN中，其主要目的是引入非线性操作，提升模型的表达能力。 池化层池化层的本质是采样操作，用于对上一层得到的结果进行压缩，减少模型计算量，同时有助于滤除噪声。 池化层主要有两类： 最大池化：保留池化核范围内最大的元素，注重于纹理信息的提取、过滤噪声，更适合浅层特征。 平均池化：将池化核范围内的元素取平均，注重保留背景信息、混合不同空间位置的信息，更适合深层特征。]]></content>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[回归方法总结]]></title>
    <url>%2F2020%2F05%2F06%2F%E5%9B%9E%E5%BD%92%E6%96%B9%E6%B3%95%E6%80%BB%E7%BB%93%2F</url>
    <content type="text"><![CDATA[在机器学习中，回归方法主要有线性回归和逻辑回归两种，其中线性回归是真的用于回归任务，而逻辑回归主要用于分类任务。 线性回归（Linear Regression, LR）线性回归的表达方式很简单：$\hat{y} = x^Tw + b$，其中$x$是一个表示为$R^n$的样本，$\hat{y}$表示对样本$x$的回归值，$w,\ b$是线性回归的参数。 如果令$X=\begin{bmatrix}x_1 &amp; 1\\ x_2 &amp; 1\\ \vdots &amp; \vdots \\ x_m &amp; 1 \end{bmatrix} \in R^{m \times (n+1)}$表示所有数据的扩展矩阵，其中$m$是数据数量，$Y=\begin{bmatrix}y_1 \\ y_2 \\ \vdots \\ y_m\end{bmatrix} \in R^{m \times 1}$表示数据的标签，$W \in R^{(n+1) \times 1}$表示线性回归的参数，其中$n$是数据特征维数，则线性回归可以表示为$\hat{Y} = XW$。 如果首先对数据和标签进行了中心化，即$\sum_i x_i=0,\ \sum_i y_i=0$，那么可以令$X=\begin{bmatrix}x_1\\ x_2\\ \vdots\\ x_m \end{bmatrix} \in R^{m \times n}$表示所有数据的矩阵，其中$m$是数据数量，$W \in R^{n \times 1}$表示线性回归的参数。 线性回归的最小二乘法求解如果定义线性回归的损失函数为$L = (\hat{Y} - Y)^T(\hat{Y} - Y)$，虽小化损失函数即最小化均方误差。 损失函数可以进一步表达为： \begin{aligned} L(W) &= (XW - Y)^T (XW - Y) \\ &= W^TX^TXW - YXW - XWY + Y^TY\\ \end{aligned}求损失函数对$w$的偏导： \begin{aligned} dL(W) &= Z^TdZ + dZ^TZ,\ Z = XW-Y\\ &=2Z^TdZ\\ &=2Z^Td(XW-Y)\\ &=2Z^TXdW\\ \frac{\partial L(W)}{\partial W} &= X^TZ\\ &=X^T(XW-Y) \end{aligned}如果$X^TX$可逆，则令$\frac{\partial L(W)}{\partial W} = 0$可得$W = (X^TX)^{-1}X^TY$，如果$X^TX$不可逆，则可以利用梯度下降法进行求解。 最小二乘法线性回归的理解如果假设数据标签服从高斯分布，则可以根据最大似然法推导得出最小二乘法，这里不再赘述。 如果首先对数据进行了中心化，则$X^TY$是表示各个属性和回归值相关性的一个向量，$X^TX$表示的是数据的协方差矩阵。 如果数据量小于特征个数（$X$中肯定有列相关的情况），那么$X^TX$不可逆，且容易导致过拟合。 如果一些属性之间存在近似线性关系甚至线性关系，即$X^TX$是个病态矩阵甚至$X^TX$中存在相关（$X^TX$不可逆，可以看做病态矩阵的一种极端情况），根据$X^TXW = X^TY$，当$X^TY$稍作变动，所求得的$W$变化会非常大，一般情况下，没有做特征工程的数据，都会存在特征之间有近似线性关系的情况，因此线性回归的一个问题是对噪声非常敏感。 如果$X^TX$不可逆，则$X^TXW = X^TY$的解不止一个，这种时候，为了防止过拟合，同时也防止对噪声数据过于敏感，可以在线性回归的损失函数上加一个正则化项，因此就有了岭回归和LASSO回归。 岭回归（ridge regression, RR）岭回归在线性回归的损失函数中加入了L2正则化，即$L(W) = (XW - Y)^T (XW - Y) + \lambda W^TW$。对该损失函数求梯度如下： \begin{aligned} dL(W) &= 2(XW-Y)^TXdW + 2\lambda W^TdW\\ &=(2(XW-Y)^TX + 2\lambda W^T)dW\\ \frac{\partial L(W)}{\partial W} &= 2X^T(XW-Y) + 2\lambda W \end{aligned}令$\frac{\partial L(W)}{\partial W} = 0$可得$(X^TX + \lambda I)W = X^TY$ 对岭回归的理解这里和线性回归的区别在于$\lambda I$这一项。 由于$X^TX$是实对称矩阵，其特征分解可以表示为$X^TX = Q\Lambda Q^T$，其中$Q$是由特征向量构成的正交矩阵。 那么$X^TX + \lambda I = Q\Lambda Q^T + \lambda I = Q (\Lambda + \lambda I) Q^T$，可以看出加入了L2正则化之后，可以理解为一定程度上可以防止特征值为0，也可以理解为减小了$X^TX$为病态矩阵的影响。 LASSO回归（Least absolute shrinkage and selection operator Regression）类似于岭回归，LASSO回归在线性回归的损失函数中加入了L1正则化，即$L(W) = (XW - Y)^T (XW - Y) + \lambda ||W||_1$。 如果将$W$分解成$W^+,\ W^-$两部分，其中$W^+$只包含非负数，$W^-$只包含非正数，$W = W^+ - W^-$，那么$L(W^+, W^-) = (XW - Y)^T (XW - Y) + \lambda (I^n)^TW^+ + \lambda (I^n)^TW^-$ 为了最小化$L(W^+, W^-)$，求导可得： \begin{aligned} dL(W^+, W^-) &= 2Z^TdZ + \lambda (I^n)^TdW^+ + \lambda (I^n)^TdW^-,\ Z = XW-Y\\ &=2Z^TXdW + \lambda (I^n)^TdW^+ + \lambda (I^n)^TdW^-\\ &=2Z^TXdW^+ - 2Z^TXdW^- + \lambda (I^n)^TdW^+ + \lambda (I^n)^TdW^-\\ &=(\lambda (I^n)^T + 2Z^TX)dW^+ + (\lambda (I^n)^T - 2Z^TX)dW^-\\ \frac{\partial L(W^+, W^-)}{\partial W^+} &= \lambda I^n + 2X^TZ\\ &=\lambda I^n + 2X^T(XW-Y)\\ \frac{\partial L(W^+, W^-)}{\partial W^-} &= \lambda I^n - 2X^TZ\\ &=\lambda I^n - 2X^T(XW-Y)\\ \end{aligned}令导数为0： \begin{aligned} \lambda I^n + 2X^T(XW-Y) = 0\\ \Rightarrow 2X^TXW = 2X^TY - \lambda I^n\\ \lambda I^n - 2X^T(XW-Y) = 0\\ \Rightarrow 2X^TXW = 2X^TY + \lambda I^n \\ \end{aligned}可以发现，如果不是$\lambda$为0，则两个导数不可能同时为0，所以希望通过导数为0得到损失函数极小值的办法行不通。 LASSO回归可以考虑坐标下降法或者是转换为带约束条件的优化问题来解决。 对LASSO回归的理解如果是基于梯度的方法来求解LASSO回归，则可以发现，如果$W$中某个元素当前的值为负数，则其梯度中包含$-\lambda$这一项，向着梯度相反的方向更新的话，则需要加上$\alpha \lambda$这一项，其实是在让$W$中的元素趋近于0，即让参数变得稀疏，有助于模型的可解释性，同时$\alpha \lambda$这一项也倾向于使得这个元素的绝对值变小，有助于缓解过拟合。 逻辑回归逻辑回归表面上是个回归方法，其实是用于二分类任务的，只不过需要回归的值变成了$ln\frac{P(y=1)}{P(y=0)}$，其中$P(y=1)$表示样本类别为1的概率，$P(y=0)$表示样本类别为0的概率，因为是二分类：$P(y=1) = 1 - P(y=0)$，所以$\frac{P(y=1)}{P(y=0)}$其实就是$\frac{P(y=1)}{1 - P(y=1)}$即类别为1的对数几率，因此逻辑回归也叫对数几率回归。 如果令$x \in R^n$表示一个样本，$w \in R^n,\ b\in R^n$表示逻辑回归的参数，其中$n$是数据特征维数，则逻辑回归可以表示为$ln\frac{P(y=1)}{P(y=0)} = z,\ z = w^Tx + b$，即使用$w^Tx + b$去回归类别为1的对数几率。 因为$ln\frac{P(y=1)}{1 - P(y=1)} = z,\ P(y=1) = 1 - P(y=0)$，所以可以得出$P(y=1) = \frac{1}{1 + e^{-z}},\ P(y=0) = \frac{1}{1+e^z}$。 其对数似然函数可以写成$\sum\limits_X ylnP(y=1) + (1-y)lnP(y=0)$，这个表达式就是负的二值交叉熵，因此极大化其对数似然，就是极小化二值交叉熵损失。 在分类CNN中线性回归的部分被替换为卷积神经网络，最后一层的激活函数使用Sigmoid或者Softmax，将回归输出转换为（多）类别概率，如果使用（二值）交叉熵损失函数，其实就是在最大化对数似然。]]></content>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[BatchNormalization学习笔记]]></title>
    <url>%2F2020%2F05%2F04%2FBatchNormalization%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[BatchNormalization的作用机制推理过程对于深度神经网络中的一层，可以表示为$o = g(x),\ x=wl + b$，其中$w,b$是该层的参数，$l$是该层的输入，$g$表示激活函数，$o$表示该层输出。 在提出BN的原始论文《Batch Normalization: Accelerating Deep Network Training byReducing Internal Covariate Shift》中，BN层是加在每一层的激活函数之前的，即对上面的$x$进行操作。 BN层的操作有两步： 首先对数据进行归一化（normalize）：设$\mu$，$\sigma^2$分别是$x$的均值和方差，归一化之后的$x$变为$\hat{x} = \frac{x - \mu}{\sqrt{\sigma^2 + \epsilon}}$，其中$\epsilon$是个防止分母为零的平滑项， 然后对归一化的数据进行scale和shift（transform）：$\gamma \hat{x} + \beta$，最终该层加入BN之后，表达式变为$o = g(\gamma \hat{x} + \beta)$ 这里先不管$\mu,\ \sigma^2,\ \epsilon,\ \beta$怎么得到，上面的操作看起来非常简单，不过这里有一点细节：一般情况下，这里的$x,\ x_i,\ \mu,\ \sigma^2,\ \epsilon,\ \gamma,\ \beta$全都是向量，因为一个样本由多个特征构成，BatchNormalization其实是对每个特征进行normalize和transform，因此上面涉及到的计算，其实全都是逐元素运算，如果是对于图像数据，在CNN中的BatchNormalization其实是将特征图上的每个点当做一个样本的，例如一个$N\times H\times W\times C$大小的特征图（$N$是batch size），那么其中$1\times 1 \times 1 \times C$的一个点就被当成一个样本，其包含$C$个特征，所有样本共用$C$个均值和方差。 训练过程对于BatchNormalizatioin层，训练过程其实就是确定$\mu,\ \sigma^2,\ \gamma,\ \beta$的过程。 首先，对于$\mu,\ \sigma^2$，在训练过程中，对于每一个batch的数据，我们可以得到$\mu_B = \frac{1}{m}\sum\limits_{i=1}^m x_i$，$\sigma^2_B = \frac{1}{m}\sum\limits_{i=1}^m(x_i -\mu)^2$作为当前batch数据的均值和方差，其中$m$是一个batch中的样本数，下标$i$表示某个样本，训练是迭代进行的，为了得到对所有数据的均值和方差，需要使用一些方法根据$\mu_B,\ \sigma^2_B$来估计整体数据的均值和方差。 在主流的深度学习框架（tensorflow、pytorch）中，一般使用滑动平均来进行估计：将变量$\mu,\ \sigma^2$初始化为$\mu_0,\ \sigma^2_0$，因为每次迭代处理一个batch的数据，均可以得到$\mu_B,\ \sigma^2_B$，第$k$次迭代时，更新$\mu_k = \alpha\mu_{k-1} + (1-\alpha)\mu_B,\ \sigma^2_k = \alpha\sigma^2_{k-1} + (1-\alpha)\sigma^2_B$，其中$0 &lt; \alpha &lt; 1$是滑动平均的滑动速率，训练完成之后，假设一共迭代了$K$次，则令$\mu = \mu_K,\ \sigma^2 = \sigma^2_K$，即可用于推理过程。 对于$\gamma,\ \beta$两个参数，在训练过程中，将其和$w,\ b$一样，使用优化器根据梯度进行更新即可。 BatchNormalization的推理加速将BatchNormalization的两个步骤结合起来，可以得到： \begin{aligned} o &= g(\hat{z})\\ &= g(\gamma \hat{x} + \beta)\\ &= g(\gamma \frac{x - \mu}{\sqrt{\sigma^2 + \epsilon}} + \beta)\\ &= g(\gamma \frac{wl + b - \mu}{\sqrt{\sigma^2 + \epsilon}} + \beta)\\ &= g(\frac{\gamma wl}{\sqrt{\sigma^2 + \epsilon}} + \frac{\gamma b -\gamma \mu}{\sqrt{\sigma^2 + \epsilon}}+ \beta)\\ \end{aligned}可以发现，在模型训练完成之后，如果仅需要使用模型进行推理，则可以将$\frac{\gamma w}{\sqrt{\sigma^2 + \epsilon}}$保存为该层的$w$参数，将$\frac{\gamma b -\gamma \mu}{\sqrt{\sigma^2 + \epsilon}}+ \beta$保存为该层的$b$参数。这样在推理过程中完全不增加计算量。 有一种说法是如果使用了BatchNormalization，则该层的偏置参数$b$可以省略掉，这也是根据上式得来的，因为BatchNormalization中包含了一个可学习的$\beta$项，完全可以替代$b$参数的效果，而且不影响模型的表达能力。 BatchNormalization的理解原论文中认为：BatchNormalization可以让深度神经网络使用更大的学习速率训练以达到加速收敛的目的。 论文中作者解释说是因为深度神经网络在训练过程中，由于前一层参数变化，导致每一层的输入分布不断发生变化，这种情况在论文中被称为“internal convariate shift”，BatchNormalization的作用是缓解这种“internal convariate shift”：通过强行将激活之前的特征进行减平均除方差的操作，使得特征均值为0，方差为1，让激活之后的输出，即下一层的输入，维持在一个相对稳定的分布中。 但是在该论文中，作者又说如果简单的将某一层的激活函数的输入进行normalize，会改变该层的表示能力（原文：Note that simply normalizing each input of a layer may change what the layer can represent. For instance, nor-malizing the inputs of a sigmoid would constrain them tothe linear regime of the nonlinearity.），因此需要在normalize之后，再对其进行transform。 这里的transform之后，如果不是接近于0均值，1方差的，那之前的normalize就白做了，原论文中也明确说了transform有能力将之前的normalize的效果完全还原，所以原论文得出的结论是这样一来模型可以选择是否进行normalize，相当于增强了模型的表达能力。 我对这里的理解是：BatchNormalization先将特征变换到0均值，1方差，再transform到$\beta$均值，$\gamma$标准差，这样一定程度上可以让模型来显式地选择特征的均值和方差，可以让数据的分布向更加适合随机初始化的权重的位置靠拢（否则就需要单方面调整随机初始化的权重来适应数据的分布，这样显然需要更长时间的训练），因此BatchNormalization有缩短收敛时间的效果，也可以说是减小了对参数初始化的依赖，另外，BN层的先normalize再transform的操作，如果通过$\beta$和$\gamma$将特征的均值和方差限定在一定范围，一定程度上是对模型表达能力的一种限制，有一定的类似正则化的作用，一定情况下或许可以稍微提高模型的泛化能力。 从梯度方面来看，BN是通过某种对梯度的调整，从而对整个学习过程产生影响，我分析了论文中得出的求导结果，并将$\frac{\partial l}{\partial x_i}$继续化简，如下： \begin{aligned} \frac{\partial l}{\partial \hat{x}} &= \frac{\partial l}{\partial \hat{z}} \gamma\\ \frac{\partial l}{\partial \sigma^2_B} &= \sum\limits_{i=1}^m \frac{\partial l}{\partial \hat{x}_i}(x_i - \mu_B)\cdot-\frac{1}{2}(\sigma^2_B +\epsilon)^{-\frac{3}{2}}\\ &= \sum\limits_{i=1}^m \frac{\partial l}{\partial \hat{z}_i} \gamma (x_i - \mu_B)\cdot-\frac{1}{2}(\sigma^2_B +\epsilon)^{-\frac{3}{2}}\\ \frac{\partial l}{\partial \mu_B} &= \sum_{i=1}^m\frac{\partial l}{\partial \hat{x}_i} \cdot -\frac{1}{\sqrt{\sigma^2_B + \epsilon}}\\ &=\sum\limits_{i=1}^m \frac{\partial l}{\partial \hat{z}_i}\gamma \cdot -\frac{1}{\sqrt{\sigma^2_B + \epsilon}}\\ \frac{\partial l}{\partial x_i} &= \frac{\partial l}{\partial \hat{x}_i} \frac{1}{\sqrt{\sigma^2_B + \epsilon}} + \frac{\partial l}{\partial \sigma^2_B} \frac{2(x_i - \mu_B)}{m} + \frac{\partial l}{\partial \mu_B} \frac{1}{m}\\ &= \frac{\partial l}{\partial \hat{z}_i} \gamma \frac{1}{\sqrt{\sigma^2_B + \epsilon}} + \sum\limits_{j=1}^m (\frac{\partial l}{\partial \hat{z}_j} \gamma (x_j - \mu_B)\cdot-\frac{1}{2}(\sigma^2_B +\epsilon)^{-\frac{3}{2}}) \frac{2(x_i - \mu_B)}{m} + \sum\limits_{k=1}^m (\frac{\partial l}{\partial \hat{z}_k}\gamma \cdot -\frac{1}{\sqrt{\sigma^2_B + \epsilon}}) \frac{1}{m}\\ &= \frac{\partial l}{\partial \hat{z}_i} \gamma \frac{1}{\sqrt{\sigma^2_B + \epsilon}} + \sum\limits_{j=1}^m \{\frac{\partial l}{\partial \hat{z}_j} \gamma [(x_j - \mu_B)\cdot-\frac{1}{2}(\sigma^2_B +\epsilon)^{-\frac{3}{2}}\frac{2(x_i - \mu_B)}{m} - \frac{1}{\sqrt{\sigma^2_B + \epsilon}} \frac{1}{m}]\}\\ &= \frac{\partial l}{\partial \hat{z}_i} \gamma \frac{1}{\sqrt{\sigma^2_B + \epsilon}} - \sum\limits_{j=1}^m \{\frac{\partial l}{\partial \hat{z}_j} \frac{\gamma}{m} [(x_j - \mu_B)\cdot(\sigma^2_B +\epsilon)^{-\frac{3}{2}}(x_i - \mu_B) + \frac{1}{\sqrt{\sigma^2_B + \epsilon}}]\}\\ &= \frac{\partial l}{\partial \hat{z}_i} \gamma \frac{1}{\sqrt{\sigma^2_B + \epsilon}} - \sum\limits_{j=1}^m \{\frac{\partial l}{\partial \hat{z}_j} \frac{\gamma}{m} [\frac{(x_j - \mu_B)(x_i - \mu_B)}{(\sigma^2_B +\epsilon)^{\frac{3}{2}}} + \frac{1}{\sqrt{\sigma^2_B + \epsilon}}]\}\\ &= \frac{\partial l}{\partial \hat{z}_i} \gamma \frac{1}{\sqrt{\sigma^2_B + \epsilon}} - \sum\limits_{j=1}^m \{\frac{\partial l}{\partial \hat{z}_j} \frac{\gamma}{m} [\frac{(x_j - \mu_B)(x_i - \mu_B) + \sigma^2_B +\epsilon}{(\sigma^2_B +\epsilon)^{\frac{3}{2}}}]\}\\ \end{aligned}如果没有BatchNormalization，则$\hat{z} = \hat{x} = x = wl + b$，可以看出，添加了BatchNormalization之后，计算$\frac{\partial l}{\partial x_i}$的过程变得复杂了很多，仅考虑$\frac{\partial l}{\partial \hat{z}_i} \gamma \frac{1}{\sqrt{\sigma^2_B + \epsilon}}$这一项的话，可以看出，如果一个batch中，如果特征的标准差越大，则传递回去的梯度越小，如果模型学习到的标准差$\gamma$很大，则梯度也相应变大，这里相当于有个自动调整学习速率的功能因此增加了BN层的模型，能够适应更高的学习速率。但是后面一项就不好分析了，我暂时没有什么思路。如果有自己的想法欢迎留言讨论。 BatchNormalization的缺点也很明显：如果batch size比较小，那么想要通过$\sigma^2_B$和$\mu_B$来估计所有特征的方差和均值非常困难。 BatchNormalization的变种除了BatchNormalization，还有一些Normalization方式，其对比如下图所示。 LayerNormalizationLN对每个样本计算一个标量均值和方差，计算过程不受batch size的影响，常用在RNN中，但是如果同一个样本的特征区别较大，则不适合使用统一的均值和方差。 InstanceNormalization对每个样本计算C个均值和方差，不受batch size和通道数的影响，常用于风格化迁移，如果通道之间有相关性，则不适合使用IN。 GroupNormalization作为BatchNormalization的变种之一，GroupNormalization主要解决的问题是BatchNormalization对batch大小的依赖性。 在CNN中，对于$N\times H\times W\times C$大小的特征图，BatchNormalization将其中每一个大小为$1\times 1\times 1\times C$看做一个样本，而GroupNormalization首先将$N\times H\times W\times C$大小的特征图拆分成$N\times H\times W\times \frac{C}{G}\times G$，然后在$H\times W\times \frac{C}{G}$范围内求方差和均值，得到$N \times G$个均值和方差，可以理解为样本个数为$H\times W\times \frac{C}{G}$，每个样本的维度为$N \times G$，这样做的好处是样本个数不依赖batch size，原论文中作者还解释说使用GroupNormalization将特征分组处理，更加符合特征之间的依赖性，对模型性能有提升。 训练时GroupNormalization的方差和均值都是$N\times 1\times 1\times 1\times G$大小，在测试时，Batch size基本上不会和训练时一样，不能和BN一样保存均值和方差参数，因此GN在模型保存时，只保存$\gamma$和$\beta$的值，均值和方差在预测过程中是每个样本计算一次的。]]></content>
      <tags>
        <tag>深度学习</tag>
        <tag>论文笔记</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[《DeepLearning》读书笔记]]></title>
    <url>%2F2020%2F05%2F02%2F%E3%80%8ADeepLearning%E3%80%8B%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"></content>
      <tags>
        <tag>深度学习</tag>
        <tag>读书笔记</tag>
        <tag>杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[线性代数杂项]]></title>
    <url>%2F2020%2F04%2F23%2F%E7%BA%BF%E6%80%A7%E4%BB%A3%E6%95%B0%E6%9D%82%E9%A1%B9%2F</url>
    <content type="text"><![CDATA[方阵的一些理解方阵的特征分解和方阵的相似关系特征分解可以表示为$A = Pdiag(\lambda)P^{-1}$，其中$\lambda$是A的特征值组成的对角矩阵，$P$是$A$的特征向量作为列向量组成的矩阵。 方阵和向量的乘法，例如$Ax$，其中$x$在标准正交基下的坐标可以表示为$(x_1\ x_2\ x_3\ …\ x_n)$，可以看做一个对$x$的映射，而$Ax = Pdiag(\lambda)P^{-1}x$，则可以理解为首先将x从标准正交基下的坐标转换到以$A$的特征向量作为基的坐标（$P^{-1}x$），然后在此坐标下进行缩放（$diag(\lambda)P^{-1}x$），最后再将坐标转换回标准正交基的坐标（$Pdiag(\lambda)P^{-1}x$）。 方阵$A$相似与$B$即表示$A=PBP^{-1}$，也可以和方阵的特征分解类似理解，$A$可以看做在标准正交基下做的变换，$B$则是对应的在$P$的列向量作为基的情况下的相同变换，即同一个变换，在不同的基下的不同表示。]]></content>
      <tags>
        <tag>线性代数</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[SVM学习笔记]]></title>
    <url>%2F2020%2F04%2F23%2FSVM%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[线性SVM详细推导首先，一个点$p \in \mathbb{R}^d$到超平面$w^Tx+b=0$的距离可以表示为$\frac{1}{||w||}|w^Tp + b|$。 对于一个两类别数据集$X\in\mathbb{R}^d, Y\in\{-1, 1\}$，定义间隔$\gamma=2\min_i\frac{1}{||w||}|w^Tx_i + b|$ 线性支持向量机的目标即找到一组适合的参数$(w, b)$使得 \max_{w,b}\gamma = \max_{w,b} 2\min_i\frac{1}{||w||}|w^Tx_i + b|\\ s.t. \ y_i(w^Tx_i + b) > 0, i = 1,2,...,m若一组$(w^, b^)$是支持向量机的一个解，那么对于$\lambda &gt; 0$，$(\lambda w^, \lambda b^)$也是该优化问题的一个解，因为间隔不会变化。 所以这里添加一个约束条件，让$\min_i |w^T x_i + b| = 1$，因此支持向量机的优化目标可以进一步化成如下，即支持向量机的基本型： \max_{w,b} 2\min_i\frac{1}{||w||}|w^Tx_i + b| \\ = \max_{w,b} \frac{2}{||w||}\\ = \min_{w,b} \frac{1}{2}w^T w\\ s.t. \ y_i(w^Tx_i + b) \ge 1, i = 1,2,...,m可以看出，$w^T w$是一个正定二次型，如此一来，支持向量机的可以看做一个凸二次优化问题： \min_{u} \frac{1}{2} u^T Q u + t^T u \\ s.t. \ c_i^T u \ge d_i, i = 1, 2,...,m其中$u=\begin{bmatrix}w\\b\end{bmatrix}$, $Q=\begin{bmatrix}I &amp;\mathbf{0}\\\mathbf{0}&amp;0\end{bmatrix}$, $t=\mathbf{0}$, $c_i = y_i\begin{bmatrix}x_i\\ 1\end{bmatrix}$, $d_i = 1$ 也可以运用拉格朗日法来求解支持向量机，定义其拉格朗日函数$\mathcal{L}(w,b,\alpha) = \frac{1}{2}w^T w + \sum_{i=1}^{m} \alpha_i(1 - y_i(w^T x_i + b))$原问题可以表示为 \min_{w,b} \max_\alpha \mathcal{L}(w, b, \alpha)\\ s.t. \ \alpha_i \ge 0, i=1,2,...,m其对偶问题可以表示为: \max_\alpha \min_{w,b} \mathcal{L}(w, b, \alpha)\\ s.t. \ \alpha_i \ge 0, i=1,2,...,m其KKT条件表示为: \begin{aligned} &\triangledown_w\mathcal{L}(w,b,\alpha) = 0 \\ &\triangledown_b\mathcal{L}(w,b,\alpha) = 0 \\ &1 - y_i(w^T x_i + b) \le 0 \\ &\alpha_i \ge 0 \\ &\alpha_i(1 - y_i(w^Tx_i + b)) = 0 \end{aligned}这里对支持向量机的对偶问题进行第一步求解:$\min_{w,b} \mathcal{L}(w, b, \alpha)$，直接令一阶导数等于0： \frac{\partial\mathcal{L}}{\partial w} = 0 \Rightarrow w = \sum_{i=1}^m \alpha_i y_i x_i \\ \frac{\partial\mathcal{L}}{\partial b} = 0 \Rightarrow \sum_{i=1}^m \alpha_i y_i = 0这里可以看出，$w$仅和$\alpha_i &gt; 0$的样本有关，而根据KKT条件，$\alpha_i &gt; 0$的地方必须满足$(1 - y_i(w^Tx_i + b)) = 0$，即这些$x_i$在最大间隔边界上，这样的样本称为支持向量，支持向量机的解仅仅和支持向量有关。 将求得结果代入$\mathcal{L}$： \begin{aligned} \mathcal{L} &= \frac{1}{2}\sum_{i=1}^m\sum_{j=1}^m \alpha_i \alpha_j y_i y_j x_i^T x_j + \sum_{i=1}^m \alpha_i - \sum_{i=1}^m \alpha_i y_i(w^Tx_i + b)\\ &= \frac{1}{2}\sum_{i=1}^m\sum_{j=1}^m \alpha_i \alpha_j y_i y_j x_i^T x_j + \sum_{i=1}^m \alpha_i - \sum_{i=1}^m \alpha_i y_iw^Tx_i\\ &= \sum_{i=1}^m \alpha_i - \frac{1}{2}\sum_{i=1}^m\sum_{j=1}^m \alpha_i \alpha_j y_i y_j x_i^T x_j \end{aligned}此时，再求解 \max_{\alpha} \mathcal{L}(w, b, \alpha)\\ s.t. \ \alpha_i \ge 0,i=1,2,...,m\\ \ \sum_{i=1}^m \alpha_i y_i = 0这个问题的求解使用SMO(序列最小优化)算法，大致思路和坐标上升法类似，迭代进行，每次选取两个$\alpha$进行更新，同时更新参数$b$，具体步骤后面有时间再详细学习。 SVM的核技巧线性SVM基于一个基本假设：数据在空间$\mathbb{R}^d$中线性可分，但这个假设在实际应用中，基本不满足。 但是存在一个定理：当$d$有限时，一定存在$\hat{d}$，使得样本在空间$\mathbb{R}^{\hat{d}}$中线性可分。 因此我们可以构造一种映射：$x_i \rightarrow \phi(x_i)$，然后在这个映射的空间中使用线性SVM进行分类。 这样最终需要求解的拉格朗日函数可以写成：$\mathcal{L} = \sum_{i=1}^m \alpha_i - \frac{1}{2}\sum_{i=1}^m\sum_{j=1}^m \alpha_i \alpha_j y_i y_j \phi(x_i)^T \phi(x_j)$，看起来很简单，只需要换一种计算方式就行，但是这里存在一个问题：$\hat{d}$可能非常大，导致计算困难。 针对上述问题，需要使用核技巧：构造一个计算复杂度为$O(d)$的$k(x_i, x_j)$使得：$k(x_i, x_j)=\phi(x_i)^T \phi(x_j)$，即我们只需要一种快速的计算内积的方式，并不关心其他运算。 核函数的选择：当数据维数$d$超过了样本数量的时候，一般选用线性核，当数据维数$d$较小，而样本量$m$中等时，可以选择RBF核，但是当数据维数$d$较小，而样本量$m$特别大时，不需要选择了，直接使用深度神经网络吧。 核函数的定义需要满足Mercer条件：核函数矩阵必须是半正定的。可以理解为内积大于等于0。 核函数有一些性质：如果$k_1$、$k_2$是核函数，那么下列函数也是核函数： $c_1k_1(x_i, x_j) + c_2k_2(x_i, x_j), \ c1,c2 &gt; 0$ $k_1(x_i, x_j)k_2(x_i, x_j)$ $f(x_i)k_1(x_i, x_j)f(x_j)$ 软间隔SVM数据中不能总是找到线性可分的空间，而且数据存在噪声或者错误标注，这个时候我们如果按照SVM的优化方式，很可能造成过拟合的问题，因此可以允许少量分类错误出现，定义松弛变量$\epsilon_i = \begin{cases} 0 &amp; y_i(w^T\phi(x_i) + b) \ge 1\\ 1 - y_i(w^T\phi(x_i) + b) &amp; y_i(w^T\phi(x_i) + b) &lt; 1\end{cases}$，由此定义软间隔支持向量机的基本型： \min_{w,b,\epsilon} \frac{1}{2}w^T w + C\sum_{i=1}^m \epsilon_i\\ s.t. y_i(w^T\phi(x_i) + b) \ge 1 - \epsilon_i, i = 1, 2,...,m\\ \epsilon_i \ge 0, i=1,2,...,m其中$C$是一个可调节参数，用于调节错误分类的惩罚。 软间隔支持向量机的求解方式和支持向量机类似，不过从一个约束变成了两个约束。 另外$\epsilon_i$也可以表示成$\max(0, 1 - y_i(w^T\phi(x_i) + b))$，因此软间隔支持向量机的基本型的对偶问题可以表示为$\min_{w, b} \frac{1}{m}\sum_{i=1}^m \max(0, 1 - y_i(w^T\phi(x_i) + b)) + \frac{1}{2mC}w^Tw$，其中第一项称为经验风险，度量模型对数据的拟合程度，第二项称为结构风险，度量模型的复杂程度，也可以称为正则化项。因此还衍生出一种损失函数：hinge loss：$\mathbb{l}(s) = max(0, 1-s)$。]]></content>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[凸优化学习笔记]]></title>
    <url>%2F2020%2F04%2F23%2F%E5%87%B8%E4%BC%98%E5%8C%96%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[仿射集、凸集、锥集、凸锥集、仿射组合、凸组合、凸锥组合、仿射包、凸包、凸锥包 锥：$C = \{x| \theta x \in C\}, x \in R^n,\theta \ge 0$ 仿射组合：$\theta_1+\theta_2+…=1$ 凸组合：$\theta_1+\theta_2+…=1,\theta_1+\theta_2+… \ge 0$ 凸锥组合：$\theta_1+\theta_2+… \ge 0$ 一些凸集：超平面、半空间、球、椭球、多面体、单纯形、对称（半）（正定）矩阵 超平面：$\{x|w^Tx=b\},x \in R^n,w \in R^n, b \in R, w \ne \mathbb{0}$ 半空间：$\{x|w^Tx \ge b\},x \in R^n,w \in R^n, b \in R, w \ne \mathbb{0}$或$\{x|w^Tx \le b\},x \in R^n,w \in R^n, b \in R, w \ne \mathbb{0}$ 球：$B(x_c, r) = \{x |\ ||x-x_c||_2 \le r\}, x_c \in R^n, x \in R^n, r \ge 0$ 椭球：$\epsilon(x_c, P) = \{x |\ (x-x_c)^T P^{-1} (x-x_c) \le 1\}, x_c \in R^n, x \in R^n, P \in S_{++}^n$ 多面体：$P=\{x|a_i^T x \le b_i, c_j^T x = d_j, i = 1,2,…,m, j=1,2,…,n\}$ 单纯形：$R^n$中的$k+1$个点$v_0, v_1, …, v_k$，满足$v_1-v_0, v_2-v_0, …, v_k-v_0$线性无关，则$v_0, v_1, …, v_k$的单纯形为$v_0, v_1, …, v_k$的凸包，单纯形是多面体的一种，证明的时候，可以借助”秩为$k$的矩阵$B$,可以用一个非奇异矩阵$A$转换成$\begin{bmatrix}I_k \\ \mathbb{0} \end{bmatrix}$的形式”这个定理。 仿射函数、透视函数、线性分数函数 仿射函数：$f:R^n \rightarrow R^m, f(x)=Ax+b, A \in R^{(m \times n)}, b \in R^n$ 缩放：$\alpha S = \{\alpha x| x \in S\}$、移位：$S+a=\{x+a|x \in S\}$是仿射变换的一种。 线性矩阵不等式的解集是凸集：$\{x|A(x) \preceq B\}$ 透视函数：$P: R^{n+1} \rightarrow R^n, P(x,y) = \{\frac{x}{y}\}\ x \in R^n, y\in R_{++}$ 保凸集运算： 交集 集合的和：$S_1 + S_2 = \{x_1 + x_2 | x_1 \in S_1, x_2 \in S_2\}$ 集合的笛卡尔积：$S_1 \times S_2 = \{(x_1 , x_2) | x_1 \in S_1, x_2 \in S_2\}$ （逆）仿射函数 透视函数 线性分数函数：首先定义仿射函数$g:R^n \rightarrow R^{m+1}, g(x)=\begin{bmatrix} A \\ c^T \end{bmatrix} x + \begin{bmatrix} b \\ d \end{bmatrix}, A \in R^{m \times n}, c \in R^n, b \in R^m, d \in R$，再定义透视函数$p:R^{m+1} \rightarrow R^m$，则线性分数函数$f = p \circ g = \frac{Ax + b}{c^T x + d},\ dom f = \{x| c^T x + d &gt; 0\}$ 凸函数凸函数定义 $f:R^n \rightarrow R$为凸函数$\Leftrightarrow$$domf$为凸集，且$f(\theta x_1 + (1-\theta)x_2) \le \theta f(x_1) + (1-\theta)f(x_2)$，其中$\theta \in [0, 1]$ $f:R^n \rightarrow R$为凸函数$\Leftrightarrow$$domf$为凸集，且$\forall x \in dom f, \forall v, g(t) = f(x + tv)$为凸，$dom g = \{t | x + tv \in dom f\}$ $f:R^n \rightarrow R$为凸函数且$f$可微$\Leftrightarrow$$domf$为凸集，$\forall x,y \in dom f, f(y) \ge f(x) + \triangledown f^T(x)(y-x)$ $f:R^n \rightarrow R$为凸函数且$f$二阶可微$\Leftrightarrow$$domf$为凸集，$\forall x \in dom f,\triangledown ^2 f(x) \succeq 0$（二阶条件） 如果将上述前三个定义中的$\le$、$\ge$, 改为$&lt;$、$&gt;$，那么$f$就变成了严格凸函数，严格凸函数的二阶导数不能保证$\triangledown ^2 f(x) \succ 0$，但$\triangledown ^2 f(x) \succ 0$可以保证严格凸，但是二次函数$f$严格凸$\Leftrightarrow$Hession矩阵$\triangledown ^2 f(x) \in S^n_{++}$即$\triangledown ^2 f(x) \succeq 0$。 凸函数的一个重要性质$f(x) \ge f(x_0) + f’ (x_0) ^ T(x-x_0)$ 一些常见的凸函数 仿射函数：$f(x)=Ax+b, \triangledown_2f=0$，是凸函数也是凹函数 指数函数：$f(x) = e^{ax}, x \in R$ 幂函数：$f(x) = x^a, x \in R_{++}, a \ge 1\ or\ a \le 0$是凸函数，当$0 \le a \le 1$时，是凹函数 绝对值的幂函数：$f(x)= |x|^p, x \in R, x \ne 0, P \ge 1$ 对数函数：$f(x) = -log(x), x \in R_{++}$ 负熵：$f(x) = xlog(x), x \in R_{++}$ 范数：$p(x), x \in R^n$，满足$p(a)=|a|p(x), p(x+y) \le p(x) + p(y), p(x) = 0 \Leftrightarrow x = 0$ 极大值函数：$f(x) = max(x_1, x_2, …, x_n), x \in R^n$ 极大值函数的逼近-log sum up：$f(x) = log(e^{x_1} + e^{x_2} + … +e^{x_n}), x \in R^n$其中$max(x_1, x_2, …, x_n) \le f(x) \le max(x_1, x_2, …, x_n) + log(n)$ 二次型二次型是一种二次齐次函数的统称，可以表示为$f(x_1, x_2, …, x_n) = a_{11}x_1^2 + a_{22}x_2^2 + … + a_{nn}x_n^2 + 2a_{12}x_1x_2 + 2a_{13}x_1x_3 + … + + 2a_{n-1, n}x_1x_n$ 二次型可以通过一个对称矩阵表示$f(x_1, x_2, …, x_n)=X^T A X$，其中A因为是对称矩阵，有很多很好的分解方式，因此非常方便分析。 一些常用性质 $V^TXV \ge 0 \Leftrightarrow X \succeq 0$ $det(XY) = det(X)det(Y)$，$det$表示求行列式值。 $(a^Ta)(b^Tb) \ge (a^Tb)^2$ 保凸函数运算 $f_1, f_2, …, f_m$为凸，则$f = \sum\limits_{i=1}^m w_i f_i,\ \forall i,w_i \ge 0$为凸 若$f(x,y),\ \forall y \in A,f(x,y)$为凸，对于$\forall y \in A,\ w(y) \ge 0, g(x) = \int_{y \in A}w(y)f(x,y)dy$为凸 仿射映射$f:R^n \rightarrow R, A\in R^{n \times m}, b\in R^n,g(x) = f(Ax + b), dom g=\{x|Ax+b \in dom f\}$是凸函数。 对函数的仿射：$f_i: R^n \rightarrow R,i=1,…,m$为凸函数，$A \in R_+^n, b \in R, g(x)=A^T\begin{bmatrix}f_1(x)&amp; \cdots&amp;f_n(x)\end{bmatrix}^T + b$为凸函数 极大值操作：$f_1,f_2$为凸，则$f(x) = max(f_1(x), f_2(x)), dom f = domf_1 \bigcap domf_2$为凸。 无限个凸函数的极大值：$f(x,y),\forall y \in A$对于$x$为凸，则$g = \sup\limits_{y \in A}f(x,y)$为凸函数，其中$\sup\limits_{y\in A}$表示$y\in A$时的上确界（可以理解为最大值）。 函数的组合也可以保证函数凸性：$h:R^k \rightarrow R, g: R^n \rightarrow R^k,f=h \circ g, R^n \rightarrow R, domf = \{x|x\in dom g,g(x)\in dom h\}$，1、若$h$为凸，$\tilde{h}$不降，$g$为凸，则$f$为凸，2、若$h$为凸，$\tilde{h}$不增，$g$为凹，则$f$为凸，3、若$h$为凹，$\tilde{h}$不降，$g$为凹，则$f$为凹，4、若$h$为凹，$\tilde{h}$不增，$g$为凸，则$f$为凹，其中$\tilde{h}$为$h$的定义域扩展。 向量的最大$r$个分量之和：$f(x) = max\{x_{i1}+x_{i2}\cdots+x_{ir}|i1,i2,…,ir互不相等\}$是个凸函数。 实对称矩阵的最大特征值函数$f(X) = \lambda_{max}(X)$是个凸函数。 函数的透视$f:R^n \rightarrow R，g: R^n \times R^n_{++} \rightarrow R, g(u, v) = v^Tf(u \odot \frac{1}{v}), dom g = \{(u,v)|v \in R^n_{++}, u \odot \frac{1}{v} \in dom f\}$，$f$为凸$\Rightarrow g$为凸，$f$为凹$\Rightarrow g$为凹，且是联合凸和联合凹。 函数的共轭$f:R^n \rightarrow R, f^\star:R^n\rightarrow R, f^\star(y)= \sup\limits_{x \in domf}(y^Tx - f(x))$。若$f$可微，则$f^\star(y)=\sup\limits_{x \in domf}(y^Tx’ - f(x’)), f’(x’) = y$。对任意情况，$f^\star(y)$一定是凸函数。 凸集和凸函数的关系$\alpha$-sublevel set对于函数$f:R^n \rightarrow R$定义其$\alpha$-sublevel set为$C_\alpha=\{x\in domf|f(x)\le \alpha\}$ 对于函数$f:R^n \rightarrow R$定义其$\alpha$-suplevel set为$C_\alpha=\{x\in domf|f(x)\ge \alpha\}$ 凸函数所有的$\alpha$-sublevel set都是凸集。 但是函数的$\alpha$-sublevel set都是凸集，不能保证该函数是凸函数。 Quasi Convex（Concave） function（拟凸（凹）函数）$S_\alpha’ = \{x|x in dom f,f(x) \le \alpha\}$（$\alpha$-sublevel set）都是凸集，则这样的函数$f(x)$称为拟凸函数（unimodal function，单模态函数）。 $S_\alpha’ = \{x|x in dom f,f(x) \ge \alpha\}$（$\alpha$-suplevel set）都是凸集，则这样的函数$f(x)$称为拟凹函数。 $S_\alpha’ = \{x|x in dom f,f(x) = \alpha\}$都是凸集，则这样的函数$f(x)$称为拟线性函数。 类似于凸函数的第一个定义，拟凸函数也可以写成$f:R^n\rightarrow R, dom f$为凸，$\forall x,y \in dom f, \forall \theta \in [0,1]$，有$max(f(x), f(y)) \ge f(\theta x + (1-\theta)y)$ 类似于凸函数的第三个定义，拟凸函数也可以写成若$f(x)$为拟凸函数，$\forall x,y \in domf, f(y) \le f(x) \Rightarrow \triangledown^Tf(x)(y-x) \le 0$ 类似于凸函数的二阶条件，拟凸函数可以写成若$f(x)$为拟凸函数，$y^T\triangledown f(x) = 0 \Rightarrow y^T \triangledown^2f(x)y\ge 0$ 凸优化问题（Convex Problems）目标函数是凸函数且约束集合是凸集的问题叫做凸优化问题。 一般优化问题 \min f_0(x)\\ s.t. f_i(x) \le 0, i= 1, 2, ..., m\\ h_j(x) = 0, j= 1,2,...,n其中$x\in R^n$称为优化变量(Optimization variable)，$f_0$称为目标函数（损失函数）$f_i(x) \le 0$称为不等式约束，$h_j(x) = 0$称为等式约束。如果$m=n=0$则称为无约束问题。 优化问题的域：domain优化问题的定义域：$D = \bigcap\limits_{i=0}^m dom f_i \cap \bigcap\limits_{j=1}^n dom h_j$ 可行解集：feasible set可行解集$X_f = \{x|x \in D, f_i(x) \le 0,i=1,2,…,m,h_j(x) = 0, j= 1,2,…,n\}$ 问题的最优值：optimal value$p^\star = \inf\{f_0(x)|x \in X_f\}$，如果$X_f$是空集，则$p^\star = +\infty$ 最优解：optimal point/solution若$x^\star$可行，且$f_0(x^\star) = p^\star$ 最优解集$X_{opt} = \{x | x\in X_f, f_0(x)=p^\star\}$ $\epsilon$次优解集：$\epsilon$-suboptimal set$X_\epsilon = \{x | x\in X_f, f_0(x)) \le p^\star + \epsilon\}, \epsilon \ge 0$ 局部最优解：local optimal$\exists R, f_0(x^\star) = \inf\{f_0(z) | f_i(z) \le 0, h_j(z) =0, ||z-x||\le R\}$则$x^\star$是局部最优解 可行性优化问题：feasibility Problems$\min\limits_x c, s.t. f_i(X) \le 0, h_j(X) = 0$，其中$c$是任意一常数。 凸优化问题 \min f_0(x)\\ s.t. f_i(x) \le 0, i= 1, 2, ..., m\\ a_j^Tx = b_j, j= 1,2,...,n其中$f_0$、$f_i$是凸函数，其可行解集是个凸集、。 凸优化问题的局部最优解等于全局最优解（反证法）。 可微目标函数情况下的最优解凸函数在可微的情况下：$f_0(y) \ge f_0(x) + \triangledown f_0^T(x)(y-x)$ 其最优解$x^\star$满足$\triangledown f_0^T(x^\star)(y-x^\star) \ge 0, \forall y \in X_f$ 线性规划问题 \begin{aligned} \min\ &C^Tx +d\\ s.t.\ &Gx \le h\\ &Ax = b \end{aligned}其等式约束和不等式约束构成一个多面体集合。 线性规划的等价变换上面的线性变换可以等价于： \begin{aligned} \min &C^T x +d\\ s.t.\ &Gx + S = h\\ &Ax = b\\ &S \ge 0 \end{aligned}等价的判定条件：两个表达方式的可行解能够对应，且对应可行解的目标函数值相同。 线性分数规划 \begin{aligned} \min\ &f_0(x)\\ s.t.\ &Gx \le h\\ &Ax = b \end{aligned}其中$f_0(x) = \frac{C^Tx +d}{e^Tx + f}, domf = \{x | e^T x + f \ge 0\}$ 这个问题是一个拟凸问题。 如果该问题有可行解，则其有凸问题等价形式： \begin{aligned} \min\ &C^T y + dz\\ s.t.\ &Gy-hz \le 0\\ &Ay -bz = 0\\ &e^Ty+fz = 1\\ &z\ge 0 \end{aligned}两个问题等价的证明： 首先对于第一个问题任意可行的$x$，必须存在$y$和$z$对第二个问题可行，且$C^T y + dz = f_0(x)$，可以直接令$y = \frac{x}{e^Tx +f}, z=\frac{1}{e^Tx + f}$即证。 齐次对于第二个问题，任意可行的$y$和$z$，必须存在$x$对第一个问题可行，且$C^T y + dz = f_0(x)$，如果$z&gt;0$，则直接令$x=\frac{y}{z}$, 若$z=0$，那么对于第一个问题的一个可行解$x_0$，$\forall t\ge 0, x = x_0 + ty$也对第一个问题可行，令$\lim t \rightarrow +\infty$，则可以使两个目标函数值相等。 二次规划(QP) \begin{aligned} \min\ &\frac{1}{2} x^TPx + q^T x + r\\ s.t.\ & Gx \le h\\ &Ax = b \end{aligned}其中$P\in S_+^n$ 二次约束二次规划(QCQP) \begin{aligned} \min\ &\frac{1}{2} x^TPx + q^T x + r\\ s.t.\ & \frac{1}{2} x^TP_ix + q_i^T x + r_i \le 0,\ i=1,2,...,m\\ &Ax = b \end{aligned}其中$P\in S_+^n$，$P_i \in S_+^n, i =1,2,…,m$ 半正定规划（semi-Definite Programming） \begin{aligned} \min\ &tr(Cx)\\ s.t.\ & tr(A_ix) = b_i, i=1,2,...,p\\ & x \succeq 0 \end{aligned}其中$x\in S_+^n, C \in R^{n\times n}, A_i \in R^{n\times n}, b_i \in R$ 多目标优化问题 \begin{aligned} \min\ &f_0(x):R^n \rightarrow R^g\\ s.t.\ &f_i(x) \le 0, i= 1,2,...,m\\ &h_j(x) = 0, j= 1,2,...,p\\ \end{aligned}Pareto optimal front一个点满足“如果找到其他可行解，可以使得在某些指标上比这个点更优，那么这个可行解必然在其他指标上比这个点更差”，则这个点属于Pareto optimal front。 如果$f_0(x)$为凸，$f_i(x)$为凸，$h_j(x)$为仿射，则必可通过以下方式求得pareto optimal front中的一点： \begin{aligned} \min\ &\sum\limits_{i=1}^g \lambda_i f_{0i}(x), \lambda_i \ge 0\\ s.t.\ &f_i(x) \le 0, i = 1,2,...,m\\ &h_j(x) = 0, j=1,2,...,p \end{aligned}对偶性对于以下问题： \begin{aligned} \min\ &f_0(x)\\ s.t.\ &f_i(x) \le 0, i = 1,2,...,m\\ &h_j(x) = 0, j=1,2,...,p \end{aligned}其定义域$D = \{x| x \in \bigcap_{i=1}^m dom f_i \cap \bigcap_{j=1}^p dom h_j\}$ 拉格朗日函数其拉格朗日函数定义为：$L(x, \lambda, v) = f_0(x) + \sum\limits_{i=1}^m\lambda_if_i(x) + \sum\limits_{j=1}^pv_jh_j(x)$，其中$\lambda_i \ge 0$和$v_j$称为拉格朗日乘子。 拉格朗日对偶函数（对偶函数） g(\lambda, v) = \inf\limits_{x\in D}L(x, \lambda ,v) 对偶函数一定是个凹函数（因为函数对于$\lambda$和$v$是线性的）。 $\forall \lambda \ge 0, \forall v, g(\lambda,v) \le p^\star$,其中$p^\star$表示原问题的最优值。 对偶问题(Dual problem/lagrange Dual problem)对于原问题(Primal problem)（P）： \begin{aligned} \min\ &f_0(x)\\ s.t.\ &f_i(x) \le 0, i = 1,2,...,m\\ &h_j(x) = 0, j=1,2,...,p \end{aligned}其最优解记为$p^\star$ 其对偶问题（D）： \begin{aligned} \max\ &g(\lambda, v)\\ s.t.\ & \lambda \ge 0 \end{aligned}其中$g(\lambda, v)$是原问题的拉格朗日对偶函数，对偶问题的最优解记为$d^\star$ 有以下结论： $d^\star \le p^\star$（弱对偶）。 对偶问题的最优解$\lambda ^\star, v^\star$存在。 一个凸问题的对偶问题的对偶问题就是自身。 如果$d^\star = p^\star$，则称为强对偶。 对偶间隙：$p^\star - d^\star$ 相对内部（Relative Interior）去掉集合的边缘，让集合变成一个开集，集合$D$的相对内部可以表示为： $Relint\ D =\{x| x \in D, \exists r>0, B(x,r) \cap aff(D) \le D\}$ 其中$B(x,r)$是个球，$aff(D)$表示$D$的仿射包。 Slater’s Condition对偶间隙为零的充分条件，但是不必要。 如果凸问题： \begin{aligned} \min\ &f_0(x)\\ s.t.\ &f_i(x) \le 0, i=1,2,...,m\\ &Ax=b \end{aligned}其定义域为$D$，对$\forall i, f_i(x)$为凸，当$\exists x \in relint\ D$使得$Ax=b, f_i(x) &lt; 0, i =1,2,…,m$满足时，$d^\star = p^\star$ Weaken Slater’s Condition如果不等式约束为仿射（$D = dom f_0$），只要可行域非空，则必有$d^\star = p^\star$ 鞍点（Saddle Point）对于函数$L(x, \lambda)$，如果$\inf\limits_{x \in D} \sup\limits_{\lambda \ge 0} L(x, \lambda) = \sup\limits_{\lambda \ge 0} \inf\limits_{x \in D} L(x, \lambda)$，则称此时的$(x^\star, \lambda^\star)$为鞍点。 鞍点定理若$(\tilde{x}, \tilde{\lambda})$为$L(x, \lambda)$的鞍点$\Leftrightarrow$强对偶存在，且$(\tilde{x}, \tilde{\lambda})$为Primal与Dual的最优解。 KKT条件对于如下问题： \begin{aligned} \min\ &f_0(x)\\ s.t.\ &f_i(x) \le 0, i=1,2,...,m\\ &h_j(x) = 0, j=1,2,...,n \end{aligned}其拉格朗日函数：$L(x, \lambda, v) = f_0(x) + \sum\limits_{i=1}^m \lambda_i f_i(x) + \sum\limits_{j=1}^n v_j h_j(x)$ 其对偶函数：$g(\lambda, v) = \inf\limits_{x \in D}L(x, \lambda, v)$ 对偶问题：$\sup\limits_{\lambda, v} g(\lambda, v)$ 如果其满足对偶问题的最优解$d^\star$和原问题的最优解$p^\star$相等。即： \begin{aligned} f_0(x^\star) &= g(\lambda^\star, v^\star)\\ &=\inf\limits_{x \in D} (f_0(x) + \sum\limits_{i=1}^m \lambda^\star_i f_i(x) + \sum\limits_{j=1}^n v^\star_j h_j(x))\\ &\le f_0(x^\star) + \sum\limits_{i=1}^m \lambda^\star_i f_i(x^\star) + \sum\limits_{j=1}^n v^\star_j h_j(x^\star)\\ &\le f_0(x^\star) \end{aligned}这表示$f_0(x^\star) = f_0(x^\star) + \sum\limits_{i=1}^m \lambda^\star_i f_i(x^\star) + \sum\limits_{j=1}^n v^\star_j h_j(x^\star) \Rightarrow \sum\limits_{i=1}^m \lambda^\star_i f_i(x^\star) = 0$，而$f_i(x^\star) \le 0，\lambda_i \ge 0, h_j(x^\star) = 0$，因此可以得出$\lambda^\star_i f_i(x^\star) = 0$，这就是互补松弛条件。 又因为$x^\star = \mathop{\arg\inf}\limits_{x \in D} (f_0(x) + \sum\limits_{i=1}^m \lambda^\star_i f_i(x) + \sum\limits_{j=1}^n v^\star_j h_j(x)) = \mathop{\arg\inf}\limits_{x \in D} L(x, \lambda^\star, v^\star)$（这里是因为上面的推导中，两个$\le$可以改成$=$）,因此$x^\star$是该问题的全局最优解。 如果原问题中，所有函数都可微，则有： \frac{\partial L(x^\star, \lambda^\star, v^\star)}{\partial x^\star} = 0这就是稳定性条件。 因此只要满足$p^\star = d^\star$，且所有函数可微，则有如下结论： 互补松弛条件：$\lambda^\star_i f_i(x^\star) = 0$ 稳定性条件：$\frac{\partial L(x^\star, \lambda^\star, v^\star)}{\partial x^\star} = 0$ 原问题可行：$f_i(x) \le 0, i=1,2,…,m\ \ h_j(x) = 0, j=1,2,…,n$ 对偶问题可行：$\lambda_i \ge 0, i= 1,2,…,m$ 以上四个结论就是KKT条件。 在对偶间隙为0且各个函数可微的情况下，KKT条件仅是最优解的必要条件，满足KKT条件的解，不一定是最优解。 如果原问题为凸问题，各个函数可微，对偶间隙为0，则KKT条件是最优解的充分必要条件。 充分性证明：如果$(x^\star, \lambda^\star, v^\star)$满足KKT条件，则必有$(x^\star, \lambda^\star, v^\star)$为最优解。证明$g(\lambda ^\star, v^\star) = f_0(x^\star)$即可。 敏感性分析原问题$P$： \begin{aligned} \min\ &f_0(x)\\ s.t.\ &f_i(x) \le 0, i=1,2,...,m\\ &h_j(x) = 0,j=1,2,...,n \end{aligned}干扰问题： \begin{aligned} \min\ &f_0(x)\\ s.t.\ &f_i(x) \le u_i, i=1,2,...,m\\ &h_j(x) = w_i,j=1,2,...,n \end{aligned}将原问题的最优值$p^\star$看做一个函数$p^\star(u, w)$，原问题的最优值$p^\star = p^\star(0, 0)$ 有以下性质： 若原问题为凸问题，则$p^\star(u, w)$为关于$u$和$w$的凸函数。 若原问题为凸，对偶间隙为0，$\lambda^\star$和$v^\star$为对偶问题的最优解，则$p^\star(u,w) \le p^\star(0, 0) -\lambda^{\star T}u - v^{\star T}w$ 若原问题为凸，对偶间隙为0，且$p^\star(u,w)$在$(0, 0)$点可微，则$\lambda^\star_i = -\frac{\partial p^\star(0, 0)}{\partial u_i}, v^\star_j = -\frac{\partial p^\star(0, 0)}{\partial w_j}$, $p^\star(u,w) = p^\star(0, 0) -\lambda^{\star T}u - v^{\star T}w$ 强凸性强凸性表示：$\exists m &gt; 0, \forall x \in dom f, \triangledown^2f(x) \succeq mI$ 强凸性也等价于：$\exists m, \forall x,y \in dom f, f(y) \ge f(x) + \triangledown f^T(x)(y-x) + \frac{1}{2}m||y-x||^2$ 如果$f(x)$二阶可微且有强凸性，则$\min f(x)$的最优值$p^\star$满足$f(x) - p^\star \le \frac{1}{2m}||\triangledown f(x)||_2^2$ 无约束优化问题的求解算法梯度下降法（Gradient Descent）每次沿着负梯度方向更新参数。 梯度下降法的收敛性如果$\exists M, m,MI \succeq \triangledown_2f\succeq mI$，对于精确搜索的学习速率$\alpha$，可以证明$f(x^{k+1}) -p^\star \le (1 - \frac{m}{M})f(x^k - p^\star)$（利用强凸性的性质证明） 最速下降法（Steepest Descent）如果一阶可微，则可以找到一个方向$d = \mathop{\arg\min}\limits_v f(x+v) = \mathop{\arg\min}\limits_v \triangledown f^T(x)v,\ s.t.\ ||v||_k = 1$（这里使用了一阶泰勒展开），其中的k-范数不一定是2-范数，所以这个方向不一定是梯度，梯度下降法是最速下降法在范数取2-范数情况下的一个特例。 最速下降法的一些变种坐标轮换法（Coordinate Descent）每次选择一个基向量方向作为优化方向，但是需要在包含正值和负值的区间去搜索步长。 分块坐标轮换法（Block Coordinate Descent）每次选择一组基向量的线性组合方向（一个子空间）作为优化方向。 坐标轮换和分块坐标轮换适合于维度拆分之后损失函数比较简单的情况。 次梯度方法如果$f(x)$在某些点不可微，那么定义次梯度的概念$\frac{\partial f}{\partial x} = \theta\triangledown f(x)^+ + (1-\theta)\triangledown f(x)^-,\ \theta \in [0, 1]$，即使用梯度的左极限和右极限的凸组合。如果在当前点，对于某个$\theta$，次梯度可以为0，那么认为其到达极值点。 牛顿法（Newton’s Method）如果二阶可微，则使用二阶泰勒展开，找到一个方向$d = \mathop{\arg\min}\limits_v f(x+v) = \mathop{\arg\min}\limits_v \triangledown f^T(x)v + \frac{1}{2}v^T\triangledown_2 f^T(x)v$，这里不需要关于$v$的约束项，是因为如果是个凸函数，则有唯一解的$v = -(\triangledown_2 f^T(x))^{-1}\triangledown f(x)$（也称为牛顿方向）。 牛顿法在泰勒展开中的一次项接近于0的时候，就可以停止，即$\triangledown f^T(x)v = -\triangledown f^T(x)(\triangledown_2 f^T(x))^{-1}\triangledown f(x)$接近于0的时候就停止。 牛顿法的收敛速度如果$||\triangledown f(x)||_2$比较大，则泰勒展开的偏差较大，因此收敛速度比较慢，如果$||\triangledown f(x)||_2$比较小，则收敛速度比梯度下降快很多。 牛顿法的缺点在于求Hession矩阵可能非常难而且慢。 拟牛顿法（Quasi-Newton Method）不求二阶偏导，根据拟牛顿条件，找到一个对Hession矩阵逆矩阵的近似矩阵来进行方向选择，例如BFGS、L-BFGS、DFP等方法。 有约束优化问题的求解算法 \begin{aligned} \min\ &f(x)\\ s.t.\ &Ax = b \end{aligned}在使用KKT条件的过程中，如果$\triangledown f(x)$不是线性，那么存在求解困难。 首先变形原问题，使用迭代的方法，假设$x^k$满足约束$Ax^k = b$，那么下一次迭代问题变成： \begin{aligned} \min\limits_d\ &f(x^k + d)\\ s.t.\ &A(x^k +d) = b \Rightarrow Ad = 0 \end{aligned}进行二阶泰勒展开，去掉高阶项，原问题近似等价于： \begin{aligned} \min\limits_d\ &f(x^k) + \triangledown f^T(x^k)d + \frac{1}{2}d^T \triangledown_2 f(x^k)d\\ s.t.\ &Ad = 0 \end{aligned}拉格朗日法（Lagrangian Method）对于对偶间隙为零的问题，固定$v$去求$x$，然后固定$x$去求$v$，交替进行。 \begin{aligned} x^{k+1} &= x^k - \alpha^k(\triangledown f(x^k) + A^Tv^k)\\ v^{k+1} &= v^k + \alpha^k(Ax^k - b) \end{aligned}增广拉格朗日法（Augmented Lagragian Method） \begin{aligned} \min\limits_x\ &f(x) + \frac{C}{2}||Ax -b||^2_2\\ s.t.\ &Ax = b \end{aligned}这个约束问题的拉格朗日函数（增广拉格朗日函数）：$L_C(x,v) = f(x) + v^T(Ax-b)+\frac{C}{2}||Ax -b||^2_2$ 而且这个问题的原问题最优解和对偶问题最优解都和以下问题相同： \begin{aligned} \min\limits_x\ &f(x)\\ s.t.\ &Ax = b \end{aligned}一些性质 如果$v = v^\star$，则$\forall C &gt; 0$，$x^\star = \mathop{\arg\min}\limits_xL_C(x, v^\star)$ 若$C \rightarrow +\infty$，则$\forall v, x^\star =\mathop{\arg\min}\limits_xL_C(x, v)$ 如果有$x^k$，和$v^k$，首先更新$x^{k+1} = \mathop{\arg\min}\limits_x L_C(x, v^k)$，然后更新$v^{k+1} = v^k + C \triangledown_v L_C(x^{k+1}, v^k)$]]></content>
      <tags>
        <tag>凸优化</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[非标量求导术]]></title>
    <url>%2F2020%2F04%2F23%2F%E9%9D%9E%E6%A0%87%E9%87%8F%E6%B1%82%E5%AF%BC%E6%9C%AF%2F</url>
    <content type="text"><![CDATA[标量对标量求导$f(x)：R \rightarrow R, x \in R, df = f’(x)dx, f’(x) = \frac{\partial f}{\partial x} \in R$ 标量对向量求导$f(x)：R^n \rightarrow R, x \in R^n, df = (\frac{\partial f}{\partial x}) ^ T dx, \frac{\partial f}{\partial x} \in R^n$ 向量对向量求导$f(x)：R^m \rightarrow R^n, x \in R^m, df = (\frac{\partial f}{\partial x}) ^ T dx, \frac{\partial f}{\partial x} \in R^{m \times n}$，这样得到的结果是个矩阵，$f(x)$的每个元素对应一列，$x$的每个元素对应一行，即： \frac{\partial f}{\partial x} = \begin{bmatrix} \frac{\partial f(x)_1}{\partial x_1}&\cdots&\frac{\partial f(x)_n}{\partial x_1}\\ \vdots&\ddots&\vdots\\ \frac{\partial f(x)_1}{\partial x_m}&\cdots&\frac{\partial f(x)_n}{\partial x_m}\\ \end{bmatrix}标量对矩阵求导$f(X)：R^{m \times n} \rightarrow R, X \in R^{m \times n}, d f = tr((\frac{\partial f}{\partial X}) ^ T dX)$ 矩阵微分操作： $d(X \pm Y) = dX \pm dY$ $d(XY) = XdY + YdX$ $d(X^T) = (dX)^T$ $dtr(X)=tr(dX)$ $tr(AdX) = A^TdX$ $d(X^{-1})=-X^{-1}dXX^{-1}$，由$XX^{-1}=I$两侧微分可得。 $d|X| = tr(X ^ # d X)$, $X^#$为$X$的伴随矩阵，当$X$可逆时，$d|X| = tr(|X|X ^ {-1} d X)$ $d(X \odot Y) = X \odot dY + dX \odot Y$，其中$\odot$为逐元素乘法 $d\sigma(x)=\sigma’(X) \odot dX$，其中$\sigma$为逐元素函数 迹技巧 $a = tr(a)$，$a$为标量 $tr(A^T) = tr(A)$ $tr(A \pm B) = tr(A) \pm tr(B)$ $tr(A B) = tr(B A)$，其中$A$和$B^T$大小相同 $tr(A^TB \odot C) = tr((A \odot B)^T C)$，其中$A$、$B$、$C$尺寸相同 逐元素乘法的转换技巧 $X \odot Y = Y \odot X$ $X \odot Y = diag(X)Y, X,Y \in R^n$ $X \odot Y = vec^{-1}(diag(vec(X))vec(Y)), X,Y \in R^{m \times n}$ 矩阵对矩阵求导矩阵函数$F(X): R^{m \times n} \rightarrow R^{p \times q}$ 定义矩阵的向量化：$vec(X) = \begin{bmatrix}X_{1,1}&amp;X_{2,1}&amp;X_{3,1}&amp; \dots &amp;X_{m,n}\end{bmatrix}$，长度为$m \times n$。 矩阵对矩阵求导可以作为向量对向量的求导来处理： $vec(F)：R^{mn} \rightarrow R^{pq}, x \in R^{m \times n}, dvec(F) = (\frac{\partial vec(F)}{\partial vec(x)}) ^ T dvec(x), \frac{\partial vec(F)}{\partial vec(x)} \in R^{mn \times pq}$ 其实还有其他矩阵对矩阵的导数定义方式，这里选择这种定义，可以兼容微分运算，在求导时比较方便。 向量化的一些性质： $vec(A \pm B) = vec(A) \pm vec(B)$ $vec(AXB) = (B^T \bigotimes A)vec(X)$，其中$\bigotimes$表示kronecker积 $vec(A^T) = K_{mn} vec(A)$，其中$K_{mn} \in R^{mn \times mn}$是交换矩阵，作用是将按列优先的向量化变为按行优先的向量化，满足性质$K_{mn} = K_{mn}^T, K_{mn}K_{mn} = I$ $vec(A \odot X) = diag(vec(A))vec(X)$，其中$diag(vec(A)) \in R^{mn \times mn}$ $vec(ab^T) = b \bigotimes a$ kronecker积的性质 $(A \bigotimes B)^T = A^T \bigotimes B^T$ $(A \bigotimes B)(C \bigotimes D) = AC \bigotimes BD$ $K_{pm}(A \bigotimes B)K_{nq} = B \bigotimes A$]]></content>
      <tags>
        <tag>求导，矩阵，向量</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[《机器学习》读书笔记]]></title>
    <url>%2F2020%2F04%2F17%2F%E3%80%8A%E6%9C%BA%E5%99%A8%E5%AD%A6%E4%B9%A0%E3%80%8B%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[线性模型基于均方误差最小化来进行求解的方法称为最小二乘法用最小二乘法来优化线性回归线性回归的目标是学习函数$f(X) = Xw$，使得$f(X) \approx Y$，其中$X=\begin{bmatrix}x_1 &amp; 1\\ x_2 &amp; 1\\ \vdots &amp; \vdots \\ x_m &amp; 1 \end{bmatrix}$，$Y=\begin{bmatrix}y_1 \\ y_2 \\ \vdots \\ y_m\end{bmatrix}$，$w \in R^{(n+1) \times 1}$，$n$是数据特征维数。 如果用均方误差作为损失函数$L = (Xw-Y)^T (Xw-Y)$，那么问题可以描述为 \begin{aligned} &\arg \min_{w} (Xw-Y)^T (Xw-Y)\\ \end{aligned}直接求导： \frac{\partial L}{\partial w} = \frac{\partial (w^T X^T X w - Y^TXw -XwY - Y^TY)}{\partial w} = 2X^T(Xw-Y)\\令： \begin{aligned} &\frac{\partial L}{\partial w} = 0\\ &\Rightarrow w = (X^TX)^{-1}X^TY \end{aligned}即得到使用均方误差的线性回归问题的解。 对数几率回归（逻辑回归， Logistic Regression）对数几率回归即用线性回归去拟合对数几率$ln\frac{y}{1-y} = w^T x + b$ 对数几率回归也等价于$y=\frac{1}{1+e^{-z}}, z = w^T x + b$ 若将$y$视为后验概率，则$ln \frac{P(y=1|x)}{P(y=0|x)} = z, z=w^Tx + b$ 显然有$P(y=1|x) = \frac{e^z}{1 + e^z}, P(y=0|x)=\frac{1}{1 + e^z}$ 使用极大似然法求解对数几率回归：$\mathop{\arg\max}\limits_{w, b}\prod\limits_{i=1}^m P(y=y_i| x_i) \Rightarrow \mathop{\arg\max}\limits_{w, b}\sum\limits_{i=1}^m ln(P(y=y_i|x_i))$ 令$\beta = \begin{bmatrix}w ^ T&amp; b\end{bmatrix} ^ T \in R^{n+1}, Y=\begin{bmatrix}y_1&amp;y_2&amp;\dots&amp;y_m\end{bmatrix} ^ T, X=\begin{bmatrix} x_1&amp;x_2&amp;\dots&amp;x_m\\1&amp;1&amp;\dots&amp;1\end{bmatrix},x_i \in R^n, X \in R^{(n+1) \times m}$，其中$m$是数据量。 使用极大似然法求解对数几率回归可以重写为： \begin{aligned} &\mathop{\arg\max}\limits_{\beta} l(Z)\\ &Z = X^T \beta\\ &l(Z) = Y^Tln\frac{e^Z}{\mathbf{1} + e^Z} + (\mathbf{1}-Y)^Tln\frac{\mathbf{1}}{\mathbf{1}+e^Z}\\ &=Y^TZ - ln(\mathbf{1}+e^Z) \end{aligned}使用牛顿法，第$t$次更新为$\beta^{t+1} \leftarrow \beta ^ t - (\triangledown_2l)^{-1}\frac{\partial l}{\partial \beta}$ \begin{aligned} dl &= Y^TdZ - \mathbf{1}^T\frac{e^Z}{\mathbf{1}+e^Z} \odot dZ\\ &=Y^TdZ -\mathbf{1}^T \hat{P}_1 \odot dZ, \hat{P} = \begin{bmatrix} P(y=1|x_1) & P(y=1|x_2)& \dots & P(y=1|x_m)\end{bmatrix}^T\\ &=Y^TX^Td\beta - \mathbf{1}^T \hat{P}_1 \odot (X^Td\beta)\\ &=Y^TX^Td\beta - (\mathbf{1} \odot \hat{P}_1)^TX^Td\beta\\ &=(Y^T-\hat{P}_1^T)X^Td\beta \end{aligned}所以$\frac{\partial l}{\partial \beta} = X(Y-\hat{P}_1)$ \begin{aligned} d(\frac{\partial l}{\partial \beta}) &= d(X(Y-\hat{P}_1))\\ &=Xd\hat{P}_1\\ &=Xd\frac{e^Z}{\mathbf{1}+e^Z}\\ &=X(\frac{1}{1+e^Z}\odot\frac{e^Z}{1+e^Z} \odot dZ)\\ &=X(\hat{P}_0 \odot \hat{P}_1 \odot (X^Td\beta))\\ &=X diag(\hat{P}_0) diag(\hat{P}_1) X^Td\beta,\ diag(\hat{P}_0) = \begin{bmatrix} P(y=0|x_1)&\cdots&0\\ \vdots&\ddots&\vdots\\ 0&\cdots&P(y=0|x_m) \end{bmatrix} \end{aligned}所以$\frac{\partial^2 l}{\partial \beta \partial \beta^T} = \frac{\partial^2 l}{\partial \beta^T \partial \beta} = Xdiag(\hat{P}_1) diag(\hat{P}_0)X^T$ 即如果用牛顿法来求解极大似然对数几率回归，第$t$次更新为 \beta^{t+1} \leftarrow \beta ^ t - (Xdiag(\hat{P}_1) diag(\hat{P}_0)X^T)^{-1} X(Y-\hat{P}_1)线性判别分析线性判别分析也叫“Fisher判别分析”，其思想非常朴素：试图找到一个低维空间（直线），可以表示为$y=wx$(这个表达式中的$y$表示$x$投影到这个空间（直线）后和原点的距离)，使得当样本投影到该直线时不同类别样本的距离尽可能远，而相同类别的数据尽可能接近，预测时按照样本的投影位置对其进行分类。 以两类数据$x_1, x_2$为例，设$\mu_1,\mu_2,\Sigma_1,\Sigma_2$分别表示两类数据的均值和方差，则投影之后的均值和方差为$w\mu_1,w\mu_2,w^T\Sigma_1w,w^T\Sigma_2w$，因为直线是一维空间，所以这些均为实数，投影之后的类内距离可以用方差来衡量，这里使用$w^T\Sigma_1w + w^T\Sigma_2w$来度量投影之后的类内距离，而类间距离可以写成$||w\mu_2 - w\mu_1||_2^2$，同时考虑两种距离，给出希望最大化的目标函数如下。 \begin{aligned} J &= \frac{||w^T\mu_2 - w^T\mu_1||_2^2}{w^T\Sigma_1w + w^T\Sigma_2w}\\ &= \frac{w^T(\mu_2 - \mu_1)(\mu_2 - \mu_1)^Tw}{w^T(\Sigma_1 + \Sigma_2)w} \end{aligned}定义类内散度矩阵$S_w = \Sigma_1 + \Sigma_2$，类间散度矩阵$S_b = (\mu_2 - \mu_1)(\mu_2 - \mu_1)^T$，上面的优化目标可以简写为如下。 \begin{aligned} J = \frac{w^TS_bw}{w^TS_ww} \end{aligned}这个优化目标又称为$S_b$和$S_w$的广义瑞利商，注意到分子分母中都有$w$的二次项，因此和$w$大小无关，只和w方向有关，所以优化问题可以写成下式。 \begin{aligned} \min_w& \quad-w^TS_bw\\ s.t.& \quad w^TS_ww = 1 \end{aligned}用拉格朗日乘子法进行优化，求解$S_bw = \lambda S_ww$，因$S_bw$方向和$\mu_2 - \mu_1$相同，因此令$S_bw = \lambda(\mu_2 - \mu_1)$，代入求解，可以得到$w = S_w^{-1}(\mu_2 - \mu_1)$。 将数据进行降维，使得类内散度最小，类间散度最大，本质上是有监督的降维。 多分类可以将多分类问题拆解为二分类问题，拆解策略有三种：一对一（OvO）、一对其余（OvR）、多对多（MvM） 在MvM中，最常用的是纠错输出码（Error Correcting Output Codes，ECOC）,有$C_1C_2C_3…C_n$共$n$个类别，每个样本属于其中的一种，训练m个二分类器$f_1, f_2, …, f_m$，每个分类器将一些类作为正类，另一些类作为负类，这样对于某个类别的样本，理想情况是$m$个分类器对其进行预测的输出组成的0,1串，构成一种长度为$m$的固定的类别组合串，$n$个类就有$n$种组合，但在预测时，对一个样本预测得到的输出串，可能不在$n$个类的$n$种组合中，这时，计算预测输出串和每个类别组合串的距离（海明距离或者欧式距离），将样本判定为距离最小的那个类别组合串对应的类别。 类别不平衡解决办法主要有三种： 再缩放（再平衡），根据样本数量移动判定阈值或者缩放预测概率。 欠采样，将样本量过多的类别进行采样，减少该类别的样本数量，再拿去训练，但是这个方法容易丢失数据中的信息，最好是分成多个模型，每个模型使用该类别的一部分数据。 过采样，将样本量过少的类别样本进行重复，然后训练，但是这个方法容易严重过拟合，一个办法是用两个该类别样本进行插值，生成新的该类别样本。 决策树信息熵样本集合$D$中第$k$类样本所占比例为$p_k$，则信息熵定义为$Ent(D) = -\sum\limits_{k=1}^C p_k\log_2p_k$，其中$C$为类别个数。 信息熵增益假设离散属性$a$有$v$个取值：$a_1, a_2, …, a_v$，可以将当前数据集合分成$V$个子集：$D_1, D_2, …, D^V$，那么信息熵增益定义为$Gain(D,a)=Ent(D)-\sum\limits_{v=1}^V\frac{|D^v|}{|D|}Ent(D^v)$ 决策树构造过程，即每次选择一个信息熵增益最大的属性$a$，将数据划分为$V$个子集，然后在每个子集中进行同样的操作（不能选择已经使用过的属性），若无属性可用或者当前子集类别相同，则当前子集标记为类别数量最多的类别，停止继续划分。 增益率信息熵增益的定义导致其对数量较多的$D^v$更加敏感，因此又提出了增益率的概念：$Gain_ratio(D,a)=\frac{Gain(D,a)}{IV(a)}$，其中，$IV(a)=-\sum\limits_{v=1}^V \frac{|D^v|}{|D|} \log_2\frac{|D^v|}{|D|}$，称为属性$a$的固有值。 基尼指数基尼值定义为$Gini(D) = \sum\limits_{k=1}^C\sum\limits_{k’ \ne k}p_k p_{k’} = 1-\sum\limits_{k=1}^Cp_k^2$，其反映了在$D$中随机抽取两个样本，属于同一类别的概率。和信息熵增益类似，定义基尼指数为$Gini_index(D, a) = \sum\limits_{v=1}^V\frac{|D^v|}{|D|}Gini(D^v)$，构造决策树时的最优划分属性选择也可以使用能够使基尼指数最小的属性。 决策树的剪枝预剪枝在生成决策树的过程中，利用验证集验证当前划分是否能提升泛化效果，如果不能，则停止继续划分，直接将当前样本集作为决策树叶节点，并指定数量最多的类别做为叶节点类别。预剪枝训练时间短，但是由于其是贪心策略，容易引起欠拟合。 后剪枝在决策树生成完成之后，自底向上的对非叶节点进行考察，观察将该节点直接指定为叶节点是否能提升验证集泛化效果，如果能则将此节点的子树一起合并为一个叶节点。后剪枝的训练时间比预剪枝长很多，但是其欠拟合风险小，而且泛化性能往往优于预剪枝。 包含连续值的决策树如果连续属性$a$在$D$中出现$n$个取值，则将其从小到大排序为$\begin{bmatrix}a_1, a_2, … a_n\end{bmatrix}$，这样产生$n-1$个离散值$T_a = \{\frac{a_i + a_{i+1}}{2}|1 \le i \le n-1\}$则$Gain(D,a)= \max\limits_{t \in T_a}Gain(D, a, t)$，其中$Gain(D, a, t)$表示将$a$属性使用$t$划分为两部分，这样，连续值的决策树构造和离散值类似了，不过连续值属性在构造决策树的过程中可以使用多次。 属性缺失的处理令$\tilde{D}$是所有没有缺失属性a的样本集合，对于样本$x$，有样本权重$w_x$，定义如下参数。 \rho = \frac{\sum_{x\in \tilde{D}} w_x}{\sum_{x\in D}w_x}\\ \tilde{p}_k = \frac{\sum_{x\in \tilde{D}_k w_x}}{\sum_{x\in \tilde{D}}w_x}, (1\le k \le C)\\ \tilde{r}_v = \frac{\sum_{x\in \tilde{D}^v}w_x}{\sum_{x \in \tilde{D}} w_x}, (1 \le v \le V)显然，$\rho$表示属性无缺失样本所占比例，$\tilde{p}_k$表示属性无缺失样本中第$k$类所占比例，$\tilde{r}_v$表示属性无缺失样本中在属性$a$上取值$a^v$的样本比例。 由此推广信息增益为： \begin{aligned} Gain(D, a) &= \rho \times Gain(\tilde{D}, a)\\ &=\rho \times (Ent(\tilde{D}) - \sum\limits_{v=1}^V \tilde{r}_v Ent(\tilde{D}^v)) \end{aligned}其中： Ent(\tilde{D}) = -\sum\limits_{k=1}^C \tilde{p}_k log_2 \tilde{p}_k这样解决了最优划分的属性选择问题，在构造子树时，如果样本$x$在属性$a$上的取值已知，那么$x$划分到相应子节点，且权重保持为$w_x$，如果属性$a$未知，则将$s$划分入所有的子节点，且权重调整为$\tilde{r}_v w_x$。 多变量决策树叶节点不再针对某个属性，而是针对属性的线性组合进行划分。 神经网络感知机两层神经元，输入层（没有权重，直接前馈数据）和输出层，输出层是M-P神经元（阈值逻辑单元），感知机只能拟合线性可分的数据，否则其学习过程将变得震荡，难以收敛。 BP算法对于$l$层神经网络,输入$x \in R^n$，标签$y \in R^c$，第$i$层权重表示为$w_i \in R^{O_i \times I_i}, I_1 = n，O_l = c$，第$i$层偏移表示为$b_i \in R^{O_i}$，第$i$层激活函数表示为$\sigma_i$，这一般是个逐元素函数，第$i$层输入即第$i-1$层的输出，表示为$l_{i-1}$，其中$l_0 = x, z_i = w_i l_{i-1} + b_i, l_i = \sigma_i(z_i)$ loss函数记为$E(l_l, y)$，BP算法每次更新$w_i = w_i - \eta \frac{\partial E}{\partial w_i}$， $b_i = b_i - \eta \frac{\partial E}{\partial b_i}$，即让参数像梯度最小的方向前进。 首先定义$E$对$l_l$的偏导为$\frac{\partial E}{\partial l_l} = E’$，这个值由loss函数决定。因此 \begin{aligned} dE &= E'^Tdl_l\\ &=E'^T(\sigma_l'(z_l) \odot (dz_l))\\ &=E'^Tdiag(\sigma_l'(z_l))da_l\\ \Rightarrow \frac{\partial E}{\partial z_l} &= diag(\sigma_l'(z_l))E' \end{aligned}这里把$\frac{\partial E}{\partial z_l}$记作$\delta_l$ 因为： \begin{aligned} da_i &= w_idl_{i-1}\\ &= w_i(\sigma_i'(z_{i-1}) \odot (dz_{i-1}))\\ &=w_idiag(\sigma_{i-1}'(z_{i-1}))dz_{i-1}\\ \Rightarrow \frac{\partial z_i}{\partial z_{i-1}} &= diag(\sigma_{i-1}'(z_{i-1}))w_i^T\\ \end{aligned}所以定义: \begin{aligned} \delta_i &= \frac{\partial E}{\partial z_i},\ i=1,2,...,l-1\\ \Rightarrow \delta_{i-1} &= \frac{\partial z_i}{\partial z_{i-1}}\frac{\partial E}{\partial z_i},\ i=2,...,l\\ &= diag(\sigma_{i-1}'(z_{i-1}))w_i^T\delta_i\\ \end{aligned}现在再来考虑$E$对$w_{l-k}$的导数： \begin{aligned} dE &= \frac{\partial E}{\partial z_{l-k}}^Tdz_{l-k}\\ &= \delta_{l-k}^T(dw_{l-k}l_{l-k-1} + db_{l-k})\\ &= tr(\delta_{l-k}^Tdw_{l-k}l_{l-k-1} + \delta_{l-k}^Tdb_{l-k})\\ &= tr(l_{l-k-1}\delta_{l-k}^Tdw_{l-k} + \delta_{l-k}^Tdb_{l-k})\\ \Rightarrow \frac{\partial E}{\partial w_{l-k}} &= \delta_{l-k}l_{l-k-1}^T\\ \Rightarrow \frac{\partial E}{\partial b_{l-k}} &= \delta_{l-k} \end{aligned}这里的变换属于标量对矩阵求导$d f = tr((\frac{\partial f}{\partial X}) ^ T dX)$，且用到了迹的一个性质：$tr(A B) = tr(B A)$，其中$A$和$B^T$大小相同 全连接层的BP算法看起来很复杂，其实非常简单，只要使用以下几个等式即可求出任一层的权重和偏置的导数： \begin{aligned} \delta_l = \frac{\partial E}{\partial z_l} &= diag(\sigma_l'(z_l))E'\\ \delta_{i} = \frac{\partial E}{\partial z_i} &= diag(\sigma_i'(z_i))w_{i+1}^T\delta_{i+1},\ i=1,2,...,l-1\\ \frac{\partial E}{\partial w_i} &= \delta_il_{i-1}^T,\ i=1,2,...,l\\ \frac{\partial E}{\partial b_i} &= \delta_i,\ i=1,2,...,l \end{aligned}RBF（Radial Basis Function，径向基函数）网络RBF网络是指使用径向基函数作为隐层激活函数的单隐层前馈神经网络$\phi(x) = \sum\limits_{i=1}^q w_i\rho(x, c_i)$，常用的高斯径向基函数$\rho(x, c_i) = e^{-\beta_i||x-c_i||^2}$，其中$c_i,w_i$分别表示第$i$个神经元的中心和权重。 $c_i$可以使用随机采样或者聚类来获得，而其他参数$w_i, \beta_i$由BP算法等方法来获得。 SVM支持向量机的相关内容可以见我的另一篇文章SVM学习笔记，这里不再重新做笔记。 贝叶斯分类器条件风险条件风险$R(c_i|x) = \sum\limits_{j=1}^N \lambda_{ij}P(c_j|x)$其中$\lambda_{ij}$表示将$j$类样本分类为$i$时的损失。 机器学习的任务是寻找一个判定准则$h:x \rightarrow y$以最小化总体风险$\min\limits_{h} R(h)=E_x[R(h(x)|x)]$，即在每个样本上选择$h^\star(x) = \mathop{\arg\max}\limits_c R(c|x)$，这样的分类器$h(x)$被称为贝叶斯最优分类器。与之对应的总体风险$R(h^\star)$被称为贝叶斯风险，$1-R(h^\star)$是分类器能达到的最好性能，即通过机器学习能产生的模型精度理论上限。 如果$\lambda_{ij} = \begin{cases}1&amp;i \ne j\\0&amp;i = j\end{cases}$，那么条件风险将变成$R(c|x) = 1-P(c|x)$，于是最小化分类错误率的贝叶斯最优分类器变成$h^\star(x) = \mathop{\arg\max}\limits_c P(c|x)$即最大化后验概率。 生成式与判别式模型在使用贝叶斯分类器时，需要获取后验概率$P(c|x)$，但是非常难以直接获取，因此有两种方式： 第一种是直接对$P(c|x)$进行建模，称为判别式方法。 第二种是生成式模型，考虑$P(c|x) = \frac{P(x, c)}{P(x)}=\frac{P(c)P(x|c)}{p(x)}$，其中$P(c)$称为类先验概率，$P(x|c)$是样本$x$相对于类别$c$的类条件概率（似然），$P(c)$可以在样本量足够大时用各类样本出现的频率来估计（大数定律），但是$P(x|c)$非常难估计，因为这涉及到关于$x$的所有属性的联合概率，很难直接用样本出现的频率来进行估计。 极大似然估计再生成式模型中，估计$P(x|c)$的一种策略是首先确定其满足某种确定的概率分布形式，假设$P(x|c)$被参数向量$\theta_c$唯一确定，因此$P(x|c)$可以被记为$P(x|\theta_c)$，概率模型的训练过程就是参数估计的过程，参数估计有两种方案： 参数虽然未知，但是却是客观存在的固定值（频率主义学派） 参数也是一种未观察到的随机变量，其本身也满足一定的分布，因此需要先假设参数服从一个先验分布，然后基于观测数据来计算参数的后验分布（贝叶斯学派） 极大似然估计属于频率主义学派，将参数当成未知的固定值来处理。首先令$D_c$表示训练集$D$的第$c$类样本集合，并假设这些样本独立同分布，因此其似然可以表示为$P(D_c|\theta_c) = \prod\limits_{x \in D_c}P(x|\theta_c)$，极大化似然，就能找到参数$\theta_c$。 似然的表达式中有连乘，容易造成下溢，因此通常使用对数似然$\log P(D_c|\theta_c) = \sum\limits_{x \in D_c} \log P(x|\theta_c)$。 像极大似然法这种参数化的方法的准确性严重依赖于对$P(x|\theta_c)$分布的假设，在实际应用中需要利用应用任务的经验知识，才能得到比较好的分类器。 朴素贝叶斯分类器在生成式模型中，$P(c|x) = \frac{P(x, c)}{P(x)}=\frac{P(c)P(x|c)}{P(x)}$，这里估计$P(x|c)$的困难在于类条件概率$P(x|c)$是$x$所有属性的联合分布，难以从有限的训练样本中估计得到。 朴素贝叶斯分类器采用属性条件独立性假设：对所有已知类别，样本$x$的所有属性相互独立。 因此$P(c|x) = \frac{P(c)P(x|c)}{P(x)} = \frac{P(c)}{P(x)} \prod\limits_{i=1}^dP(x_i|c)$，其中$d$为样本的属性数量，$x_i$表示样本$x$的第$i$个属性值。 对于所有类别来说，$P(x)$相同，因此基于$\lambda_{ij} = \begin{cases}1&amp;i \ne j\\0&amp;i = j\end{cases}$，$h^\star(x) = \mathop{\arg\max}\limits_c P(c|x)$即最大化后验概率的朴素贝叶斯分类器就可以表达为$h_{nb}(x) = \mathop{\arg\max}\limits_c P(c)\prod\limits_{i=1}^dP(x_i|c)$ 对于类先验概率，可以从训练集中使用$P(c) = \frac{|D_c|}{|D|}$估计。 对于离散属性，估计$P(x_i|c)$的方式常用$P(x_i|c) = \frac{|D_{c,x_i}|}{|D_c|}$，其中$D_{c,x_i}$表示$D_c$中第$i$个属性取值为$x_i$的样本集合。 对于连续属性，则可以使用概率密度函数，假定$P(x_i|c)$服从某种分布，然后对其进行估计。 在离散属性的处理上，有个问题是：如果训练集中某种属性在类别$c$上没有出现，或者类别$c$在训练集上没有出现，则$P(c)\prod\limits_{i=1}^dP(x_i|c)$直接就为0了，因此需要进行修正平滑处理。 常用的是拉普拉斯修正，即将$P(c) = \frac{|D_c|}{|D|}$更改为$P(c) = \frac{|D_c|+1}{|D|+N}$，其中N表示类别个数, 将$P(x_i|c) = \frac{|D_{c,x_i}|}{|D_c|}$更改为$P(x_i|c) = \frac{|D_{c,x_i}| + 1}{|D_c| + N_i}$，其中$N_i$表示第$i$个属性的可取值个数。这样可以避免因训练集样本不充分而导致的概率估计为零的问题。 半朴素贝叶斯分类器由于属性条件独立性假设很难成立，因此尝试对条件独立性假设进行一定程度的放松。 例如独依赖估计（One-Dependent Estimator，ODE），即每个属性在类别之外最多依赖一个其他属性：$P(c|x)\propto P(c) \prod\limits_{i=1}^dP(x_i|c,pa_i)$，其中$pa_i$为属性$x_i$所依赖的属性，称为$x_i$的父属性。 问题的关键在于如何确定父属性，最直接的办法是假设所有属性都依赖于同一个父属性，称为超父（super-parent）由此形成了SPODE(Super-Parent ODE)方法。 集成学习构建并结合多个学习器来完成学习任务。一般是先产生一组个体学习器，然后使用某种策略将他们结合。 根据个体学习器的生成方式，集成学习方法大致可以分为两类： 个体学习器之间存在强依赖关系，必须串行生成，例如：Boosting。 个体学习器之间不存在强依赖关系，可以同时并行生成，例如：Bagging和随机森林（Random Forest） Boosting先训练一个基学习器，然后使用基学习器对训练样本分布进行调整，使得基学习器预测错误的样本在后续得到更多关注，然后进行下一个基学习器的训练，直到学习器数量达到指定数量。 Boosting中最著名的是AdaBoosting，可以理解为加性模型，即使用基学习器的线性组合$H(x) = \sum\limits_{t=1}^T\alpha_t h_t (x)$来最小化指数损失函数$l_{exp}(H|D) = E_{x \sim D}[e^{-f(x)H(x)}]$，其中$h_i$表示第$i$个基分类器，$f(x)$表示真实函数$f(x) = y$。 AdaBoosting只适用于二分类任务。 Bagging在原始数据中每次有放回的采样$m$个样本，组成一个子训练集，最终得到共$T$个子训练集，用每个子训练集训练一个基学习器，再将所有基学习器进行结合。 假设基学习器的计算复杂度为$O(m)$，Bagging的复杂度大致为$T(O(m) + O(s))$，其中$O(s)$为投票和采样的复杂度，一般非常小，而$T$是个比较小的常数，因此Bagging的训练复杂度和训练一个学习器的复杂程度同阶，非常高效。 Bagging和Boosting的区别随机森林（Random Forest，RF）以决策树为基学习器的Bagging集成基础上，引入随机属性选择。在构造决策树过程中，选择最优划分属性时，可以在当前可用属性的随机子集中进行选择。 集成学习的结合策略 平均法 投票法 学习法 stacking是学习法的一个例子：先用原始训练集训练出初级学习器，然后将初级学习器的输出作为输入特征，训练一个次级学习器，用于结合初级学习器输出得到最终输出。最好在初级学习器的验证集上对次级学习器进行训练。 多样性如果用$h_1,h_2,…,h_T$通过加权平均法来集成得到$H$来估计真实函数$f:R^d \rightarrow R$，则对样本$x$，定义学习器$h_i$的分歧为：$A(h_i|x) = (h(x_i) - H(x))^2$。集成的分歧表示为$\overline{A}(H|x) = \sum\limits_{i=1}^T w_i A(h_i|x)=\sum\limits_{i=1}^Tw_i(h_i(x) - H(x))^2$。分歧表示个体学习器在样本$x$上的不一致性。 而集成的泛化误差可以表示为$E=\overline{E} - \overline{A}$（$\overline{E} = \sum\limits_{i=1}^Tw_iE_i$，$\overline{A}=\sum_{i=1}^Tw_iA_i$，推导过程略过），这说明个体学习器误差越低，多样性越大，集成的效果越好。 增加个体学习器多样性的方法： 数据样本扰动：通常基于采样法，产生不同的数据子集。但是有些学习器对数据样本量的扰动不敏感（如线性学习器，支持向量机，朴素贝叶斯，k近邻，这些学习器称为稳定基学习器） 输入属性扰动：抽取属性子集来训练学习器。不适合属性较少的数据。 输出表示扰动：将样本的类别标记稍作改动，例如随机改变一些标记，或者进行编码调制，例如ECOC。 算法参数扰动：随机设置不同的参数，例如隐层神经元个数、初始连接权值等。 聚类原型聚类又称基于原型的聚类方法，此类算法假设聚类结构可以通过一组原型来刻画，通常先对原型进行初始化，然后对原型进行迭代更新求解。不同的原型表示、不同的求解方式就会产生不同的算法 k均值算法给定样本集$D=\{x_1,x_2,…,x_m\}$，聚类结果$C = \{C_1, C_2, …,C_k\}$，定义平方误差$E = \sum\limits_{i=1}^k \sum\limits_{x\in C_i} ||x-\mu_i||^2_2$，其中$\mu_i = \frac{1}{C_i}\sum\limits_{x \in C_i}x$表示簇$C_i$的均值向量。 k均值算法针对平方误差$E$进行优化，使用贪心策略，通过迭代优化方式来进行： 1、初始化$k$个初始均值向量$\mu_1, \mu_2, …, \mu_k$。 2、计算每个样本到$k$个均值向量的值，将每个样本划入最近的均值向量对应的簇中，得到一个划分。 3、使用划分好的簇计算新的均值向量。 4、如果没有均值向量被大幅更新或者达到了最大迭代次数，那么停止，否则从第2步继续循环。 学习向量量化（Learning Vector Quantization，LVQ）和k均值算法类似，但是学习向量量化在学习过程中还利用样本的监督信息来辅助聚类： 1、初始化原型向量$\{p_1, p_2, …, p_q\}$ 2、随机选取样本$x_j$，计算$x_j$和每个原型向量的距离，并找出距离最小对应的原型向量。 3、如果$x_j$的标签和原型向量的标签相同，则使用$p=p + \eta (x_j - p)$来对选出的原型向量进行更新，否则使用$p = p - \eta (x_j - p)$来对选出的原型向量进行更新 4、达到最大迭代轮数或者更新幅度很小则停止更新，否则从第2步继续循环。 其中$\eta \in (0, 1)$表示学习速率，在迭代停止之后，对于任意样本$x$，可以将其划分到与其距离最近的原型向量所代表的的簇中。 高斯混合聚类（Mixture-of-Gaussian）假设数据服从高斯混合分布$p_M(x) = \sum\limits_{i=1}^k \alpha_i p(x|\mu_i, \Sigma_i), \sum\limits_{i=1}^k \alpha_i=1$。 令随机变量$z_j,j\in \{1,2,…,k\}$表示样本$x_j$预测类别，其先验概率$p(z_j = i) = \alpha_i$，则$p_M(z_j = i|x_j) = \frac{\alpha_ip(x_i|\mu_i,\Sigma_i)}{\sum\limits_{l=1}^k \alpha_lp(x_j|\mu_l, \Sigma_l)}$ 当高斯混合分布已知时，即可将样本划分成$k$个簇，其标记为$\mathop{\arg\max}\limits_i p_M(z_j = i|x_j)$ 估计这样的高斯混合分布可以使用极大似然法，令对数似然$LL(p_M) = \sum\limits_{j=1}^m ln(\sum\limits_{i=1}^k \alpha_ip(x_i|\mu_i,\Sigma_i))$，使用EM算法即可求解。 密度聚类DBSCAN密度聚类算法的代表是DBSCAN算法，使用一组邻域参数$(\epsilon, MinPts)$来刻画样本分布的紧密程度。 $\epsilon$-邻域：$N_\epsilon(x_j) = \{x_i | x_i \in D, dist(x_j,x_i)\le \epsilon\}$，其中$D$表示全部数据集合。 核心对象：满足$|N_\epsilon(x_j)| \ge MinPts$的$x_j$称为核心对象，其中$|N_\epsilon(x_j)|$表示$x_j$的$\epsilon$-邻域中的样本数量。 密度直达：$x_j$是一个核心对象，且$x_i$在$x_j$的$\epsilon$-邻域中，则$x_i$由$x_j$密度直达。 密度可达：如果存在样本序列$p_1, p_2, …, p_n, p_1 = x_j, p_n = x_i$使得$p_{m+1}$由$p_m$密度直达，则称$x_i$由$x_j$密度可达。 密度相连：如果存在$x_k$使得$x_j$和$x_i$均可由$x_k$密度可达，则称$x_i$和$x_j$密度相连。 基于以上概念，DBSCAN将簇定义为由密度可达关系到处的最大密度相连样本集合。 DBSCAN的聚类过程如下： 1、找到所有的核心对象。 2、循环随机取一个还没有访问过的核心对象，将其所有密度相连的样本生成一个簇并标记为已访问，如果没有未访问的核心对象，则停止循环 3、仍未访问的样本被视为噪声样本。 层次聚类层次聚类方法试图在不同层次上对数据进行划分，形成树形的聚类结构。 AGNESAGNES是一种自底向上的层次聚类算法，首先将所有样本单独看做一个簇，然后每次迭代找到距离最近的两个簇进行合并，直到簇数量等于指定数量。 这里的关键是如何定义两个簇的距离，主要有三种方式，使用三种距离计算的AGNES分别被称为： 最大距离：单链接算法 最小距离：全链接算法 平均距离：均链接算法 降维与度量学习k近邻（k-Nearest Neighbor）学习k近邻方法没有训练过程，在给定测试样本时，直接使用训练样本中与其最靠近的$k$个样本，基于这$k$个样本的信息来对测试样本进行预测。 k近邻方法是懒惰学习（lazy learning）的一个代表，而那些在训练阶段就对样本进行学习处理的方法称为急切学习（eager learning） 低维嵌入大部分时候，观测到的数据是高维数据，但与学习任务密切相关的很可能是高维空间中的一个低维嵌入。 多维缩放（Multiple Dimensional Scaling，MDS）多维缩放的思路是找到一个低维空间，使得在这个低维空间中的欧氏距离和原始空间中的距离相等。 假如原始空间的维度为$d$，所有数据的距离矩阵$D\in R^{m \times m}$，其中$m$为样本数量，$d_{ij}$表示样本$x_i$和$x_j$的距离，降维后的数据$z \in R^{d’}$，所有样本表示为$Z\in R^{d’ \times m}$。 令$B = Z^T Z \in R^{m\times m}$是降维后的内积矩阵，有$b_{ij} = z_i^T z_j$ 则$d_{ij}^2 = ||z_i||^2 + ||z_j||^2 - 2z_i^T z_j = b_{ii} + b_{jj} - 2b_{ij}$ 令降维后的样本$Z$被中心化，即$\sum\limits_{i=1}^m z_i = \mathbf{0}$，可得到$\sum\limits_{i=1}^m b_{ij} = \sum\limits_{j=1}^m b_{ij} = 0$ 因此： \sum\limits_{i=1}^md_{ij}^2 = tr(B) + mb_{jj}\\ \sum\limits_{j=1}^md_{ij}^2 = tr(B) + mb_{ii}\\ \sum\limits_{i=1}^m\sum\limits_{j=1}^md_{ij}^2 = 2m\ tr(B)\\ tr(B) = \sum\limits_{i=1}^m b_{ii}则有： \begin{aligned} b_{ij} &= \frac{b_{ii} + b_{jj} - d_{ij}^2}{2}\\ &=\frac{1}{2m}(\sum\limits_{i=1}^md_{ij}^2) + \frac{1}{2m}(\sum\limits_{j=1}^md_{ij}^2) - \frac{1}{2m^2} \sum\limits_{i=1}^m\sum\limits_{j=1}^md_{ij}^2 - \frac{d_{ij}^2}{2} \end{aligned}这样就能根据原来的距离矩阵求出内积矩阵$B$，再对$B$进行特征分解得到$B=V \Lambda V^T$，其中$\Lambda = diag(\lambda_1, …, \lambda_d)$为特征值构成的对角阵，$\lambda_1 \ge \lambda_2\ge …\ge \lambda_d$，为了降维，我们可以取其中$d’$个非零值，构成对角矩阵$\Lambda_\star$与其对应的特征向量矩阵$V_\star$，则$Z = \Lambda_\star ^{\frac{1}{2}} V_\star^T \in R^{d’ \times m}$ 主成分分析（PCA）如果希望使用一个超平面来对数据进行表示，那么可以从两个方面去考虑： 最近重构性：样本点到这个超平面的距离都足够近 最大可分性：样本点在这个超平面上的投影尽可能分开 但是两个方面的考虑最终都会得到PCA的等价推导，即PCA既保证了最近重构性也保证了最大可分性。 假定数据$x_i \in R^d$已经进行过中心化，即$\sum_i x_i = \mathbf{0}$，现在使用一组标准正交基对$x_i$进行投影，得到$z_i = Wx_i, W = \begin{bmatrix}w_1^T\\ w_2^T\\ \vdots\\ w_{d’}^T \end{bmatrix}\in R^{d’ \times d}, w_i^T w_j = \begin{cases}1 &amp; i=j\\ 0&amp; i\ne j\end{cases}$，其中$z_{ij} = w_j^Tx_i$，如果使用$z_i$来还原$x_i$则得到$\hat{x}_i = \sum\limits_{j=1}^{d’}z_{ij}w_j = W^T z_i$。 如果从最近重构性来考虑，我们希望最小化$\sum\limits_{i=1}^m ||\hat{x}_i - x_i||^2_2$，即： \begin{aligned} &\min \sum\limits_{i=1}^m ||\hat{x}_i - x_i||^2_2\\ &=\min \sum\limits_{i=1}^m || \sum\limits_{j=1}^{d'}z_{ij}w_j - x_i||^2_2\\ &=\min \sum\limits_{i=1}^m || W^Tz_i - x_i||^2_2\\ &=\min \sum\limits_{i=1}^m (z_i^TWW^Tz_i - z_i^TWx_i - x^T_iW^Tz_i + x^T_i x_i)\\ &=\min \sum\limits_{i=1}^m (z_i^Tz_i - z_i^TWx_i - x^T_iW^Tz_i)\\ &=\min \sum\limits_{i=1}^m tr(z_i^Tz_i - z_i^TWx_i - x^T_iW^Tz_i)\\ &=\min \sum\limits_{i=1}^m tr(z_i^Tz_i - 2z_i^TWx_i)\\ &=\min \sum\limits_{i=1}^m -tr(z_i^T z_i)\\ &=\min -tr(Z^T Z),\ Z = \begin{bmatrix}z_1 &z_2 & \cdots &z_m\end{bmatrix} = WX,\ X = \begin{bmatrix}x_1 &x_2 & \cdots &x_m\end{bmatrix}\\ &=\min -tr(X^TW^TWX)\\ &=\min -tr(WXX^TW^T)\\ \end{aligned}因此我们需要解决的问题就是： \begin{aligned} \min\ & -tr(WXX^TW^T)\\ s.t.\ & WW^T = I_{d'} \end{aligned}另一方面，如果从最大可分性来考虑，我们希望最大化$z_i$之间的方差$\sum\limits_{i=1}^m (z_i - \frac{1}{m}\sum\limits_{j=1}^mz_j)^2$ \begin{aligned} &\max \sum\limits_{i=1}^m ||z_i - \frac{1}{m}\sum\limits_{j=1}^mz_j||_2^2\\ &=\max \sum\limits_{i=1}^m ||z_i - \frac{1}{m}\sum\limits_{j=1}^mWx_j||_2^2\\ &=\max \sum\limits_{i=1}^m ||z_i - \frac{1}{m}W\sum\limits_{j=1}^mx_j||_2^2\\ &=\max \sum\limits_{i=1}^m ||z_i||_2^2\\ &=\max \sum\limits_{i=1}^m (z^T_iz_i)\\ &=\max\ tr(Z^TZ)\\ &=\max\ tr(X^TW^TWX)\\ &=\min\ -tr(X^TW^TWX)\\ &=\min\ -tr(WXX^TW^T)\\ \end{aligned}因此我们需要解决的问题就是： \begin{aligned} \min\ & -tr(WXX^TW^T)\\ s.t.\ & WW^T = I_{d'}\\ \end{aligned}由此可见，在$\sum x_i = 0$的情况下，从两个方面得到的结果完全相同。 求解PCA可以使用拉格朗日法，首先得到拉格朗日函数$L(W) = -tr(X^TW^TWX) + \lambda (WW^T - I_{d’}), \lambda \ge 0$ \begin{aligned} dL(W) &= -tr(WXX^TdW^T + dWXX^TW^T) + \lambda tr(dW W^T + WdW^T)\\ &= -tr(2XX^TW^TdW - 2\lambda W^TdW)\\ \\ \frac{\partial dL(W)}{\partial W} &= 2\lambda W - 2WXX^T \end{aligned}令$\frac{\partial dL(W)}{\partial W} = 0$可得$\lambda W^T = XX^TW^T$，即求出协方差矩阵$XX^T$的特征向量即可构成$W^T$，在这个过程中，可以舍弃一部分特征向量，只取特征值最大的$d’$个特征向量，即可将数据维度从$d$维缩减到$d’$维。 如果将$X$进行奇异值分解，则有$X=D\Sigma V^T$，$XX^T = D \Sigma \Sigma^T D^T$，其中$D$是$X$的左奇异矩阵，也就是$XX^T$的特征矩阵，如果令$W=D^T$可以得到$Z=D^TX=D^TD\Sigma V^T = \Sigma V^T$，因此求出$X^TX$的特征矩阵也可以求出$Z$。 核化线性降维核主成分分析（KPCA）PCA是一种线性降维方式，在降维时假设从高维空间到低维空间的映射是线性的，其线性映射由$W$确定，且有$\lambda W^T = XX^TW^T = (\sum\limits_{i=1}^m x_i x^T_i)W^T$，假设在高维空间$\phi(x)$中进行PCA，则有$\lambda W^T = (\sum\limits_{i=1}^m \phi(x_i) \phi(x_i)^T)W^T$，有如下推导： \begin{aligned} \lambda W^T &= (\sum\limits_{i=1}^m \phi(x_i) \phi(x_i)^T)W^T\\ &= (\sum\limits_{i=1}^m \phi(x_i) \phi(x_i)^T)W^T\\ &= \sum\limits_{i=1}^m \phi(x_i)\phi(x_i)^TW^T\\ &= \sum\limits_{i=1}^m\phi(x_i)\alpha_i,\ \alpha_i = \phi(x_i)^TW^T\\ \end{aligned}由于$\phi$函数无法明确求出，因此引入核函数。 \begin{aligned} \lambda \phi(x_j)^TW^T &= \phi(x_j)^T\sum\limits_{i=1}^m\phi(x_i)\alpha_i\\ \lambda A&=KA\\ \end{aligned}其中$K$是核矩阵$K_{ij} = \phi(x_i)^T\phi(x_j)$，$A = \begin{bmatrix}\alpha_1, \alpha_2, …, \alpha_m\end{bmatrix}$ 则$z_{ij} = w_j^T \phi(x_i) = \sum\limits_{k=1}^m\alpha_{k,j}\phi(x_k)^T\phi(x_i)=\sum\limits_{k=1}^m\alpha_{k,j}K_{ki}$表示降维之后$x_i$对应向量的第$j$个分量。 其中W可以由$\phi(X)^T\phi(X)$的特征矩阵求出，但在计算$z$时，由于每个分量需要求和$\sum\limits_{k=1}^m\alpha_{k,j}$，计算量非常大。 流形学习流形是指在局部与欧式空间统配的空间，如果数据是嵌入在高维空间中的低维流形，则可以利用其局部与欧式空间同胚的性质，使用局部的欧式距离来计算数据样本之间的距离。 等度量映射（Isometric Mapping，Isomap）将低维嵌入流形上两点的距离定义为“测地线”距离，即两个样本沿着流形的最短距离，测地线距离的计算可以将邻近点之间进行连接，然后转换计算近邻连接图上两点之间的最短路径问题（Dijkstra算法或者Floyd算法）。得到距离矩阵之后，可以使用多维缩放算法（MDS）来进行降维。 邻近图的构建一般有两种做法，一个是指定最近的$k$个点作为邻近点，这样得到的邻近图称为$k$邻近图，另一个是指定距离阈值$\epsilon$，小于$\epsilon$的点被认为是邻近点，这样得到的邻近图称为$\epsilon$邻近图，两种方式各有优劣。 局部线性嵌入（Locally Linear Embedding，LLE）Isomap试图保持邻近样本之间的距离，而LLE试图保持邻域内样本的线性关系，假定样本点$x_i$可以通过$x_j,x_k,x_l$线性组合而得到，即$x_i = w_{ij}x_j+w_{ik}x_k+w_{il}x_l$ 首先对于每个样本$x_i$，找到其邻近下标集合$Q_i$，然后计算$Q_i$对$x_i$的线性重构系数： \begin{aligned} \min\limits_{w_1, w_2, ..., w_m}\ &\sum\limits_{i=1}^m ||x_i - \sum\limits_{j\in Q_i}w_{ij} x_j||_2^2\\ s.t.\ &\sum\limits_{j\in Q_i} w_{ij} = 1 \end{aligned}求得$w_{ij}$之后，$x_i$对应的低维空间坐标$z_i = \min\limits_{z_1, z_2, …, z_m} \sum\limits_{i=1}^m ||z_i - \sum\limits_{j\in Q_i} w_{ij} z_j||_2^2$。 令$Z = \begin{bmatrix}z_1 &amp; z_2 &amp; \cdots &amp; z_m\end{bmatrix} \in R^{d’ \times m}, W_{ij} = w_{ij}$ 则确定$W$之后，$Z$可以通过： \begin{aligned} \min\limits_Z\ &tr(Z(\mathbf{I} - W)^T(\mathbf{I} - W)Z^T)\\ s.t.\ &ZZ^T = \mathbf{I} \end{aligned}来求得，即对$(\mathbf{I} - W)^T(\mathbf{I} - W)$进行特征分解，取最小的$d’$个特征值对应的特征向量构成$Z^T$ 度量学习对高维数据的降维主要是希望找到一个合适的低维空间使得此空间中学习能比原始空间性能更好，度量学习的思路是尝试学习出一个距离度量。 对于两个$d$维样本$x_i$，$x_j$，其欧氏距离的平方$||x_i - x_j||^2_2 = dist_{ij,1} + dist_{ij,2} + \cdots + dist_{ij,d}$，其中$dist_{ij,k}$表示在第$k$维上的距离。 如果假定不同属性的重要性不同，则可以引入权重$w$，$||x_i - x_j||^2_2 = w_1 dist_{ij,1} + w_2 dist_{ij,2} + \cdots + w_d dist_{ij,d} = (x_i - x_j)^T W (x_i - x_j)$，其中$W = diag(w), w_i \ge 0$ 如果令$W$不再是一个对角矩阵，而是让其等于一个半正定对称矩阵$M$，则可以定义马氏距离$dist_{mah}^2(x_i, x_j) = (x_i - x_j)^TM(x_i - x_j) = ||x_i - x_j||^2_M$，则可以对这个$M$进行学习，得到满足要求的距离表达。 在近邻成分分析（Neighbourhood Component Analysis, NCA）中可以用对$M$进行训练，提高其分类正确率。 又例如根据一些领域知识已知某些样本相似（必连约束集合），另外一些样本不相似（勿连约束及合），则可以对$M$进行训练，使得必连约束集合中的样本距离尽可能小，而勿连约束集合中的样本的尽可能大。 不管以任何方式训练得到的$M$，都可以对M进行特征值分解，然后去掉一部分特征向量，得到降维矩阵，用于数据的降维。]]></content>
      <tags>
        <tag>机器学习</tag>
        <tag>读书笔记</tag>
        <tag>杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《Expectation-Maximization Attention Networks for Semantic Segmentation》]]></title>
    <url>%2F2020%2F01%2F03%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8AExpectation-Maximization-Attention-Networks-for-Semantic-Segmentation%E3%80%8B%2F</url>
    <content type="text"><![CDATA[主要工作基于高斯混合模型和EM优化算法，提出了一种自注意力机制，用一组基向量来对特征图进行重构，达到简化特征图以及去除噪声的目的。 主要思想将$C \times H \times W$大小的特征图看做$N \times C$的$N$个$C$维随机变量$X$，其中$x_i \in \mathbb{R}^{C}$表示像素$i$对应的特征向量。同构构造一组基向量$\mu \in \mathbb{R}^{K \times C}$以及一个隐变量$Z \in \mathbb{R}^{N \times K}$，其中$\mu_i \in \mathbb{R}^C$表示一个其中一个基向量，$z_i \in \mathbb{R}^K$表示第$i$个像素点在这组基向量下的坐标，文中最终把输入的特征图$X$进行了重构，得到$\hat{X} = Z \times \mu$。 具体实现通过高斯混合模型，对$x_n$建模得到$p(x_n) = \sum_{k=1}^K z_{nk}\mathcal{N}(x_n|\mu_k,\Sigma_k)$，再通过EM算法（可见我之前的文章EM算法学习笔记），求出$\mu$和$Z$，这里将EM算法，分解为$A_E$和$A_M$，对应EM算法的E步骤和M步骤，再加上重构步骤$A_R$，构成一种self-attention操作，称为Expectation-Maximization Attention（EMA）。 在实现过程中，EMA Unit的结构类似于带有bottleneck的Residual结构，只是将$3 \times 3$卷积换成了循环的$A_E$和$A_M$操作，整体结构如下。 在这个结构中，第一个$1 \times 1$卷积之后，不能有ReLU激活函数，作者解释说这是因为一旦使用ReLU激活，输入的范围将从$(-\infty, \infty)$变为$[0, \infty)$，导致EM算法得出的结果中，$\mu$将是一个半正定矩阵，其表达能力只相当于普通卷积操作的一半。 在$A_E$阶段，计算$Z^{(t)}=softmax(\lambda X(\mu^{(t-1)})^T)$，这里$\lambda$是控制$Z$分布的一个超参数，作者将$\lambda$设置为1。 在$A_M$阶段，计算$\mu^{(t)}_k=\frac{z^{(t)}_{nk}x_n}{\sum_{m=1}^Nz^{(t)}_{mk}}$ 在训练过程中，对于batch数据，$\mu$的值不是每个数据都去计算一次，在第一个mini-batch中，使用Kaiming初始化方法对$\mu^{(0)}$进行初始化（这里将矩阵乘法看做1*1的卷积），之后的mini-batch中，$\mu^{(0)}$不能简单的使用反向传播来更新，作者解释说是因为在不断的迭代过程中，容易引起梯度消失或者梯度爆炸等问题，这样更新会导致$\mu^{(0)}$不稳定，因此论文中提出，使用滑动平均对$\mu^{(0)}$进行学习，每个图像在EM迭代之后所得到的$\mu^{(T)}$将用于更新$\mu^{(0)}$：$\mu^{(0)} \leftarrow \alpha\mu^{(0)} + (1-\alpha)\mu^{(T)}$，而在推断过程中，$\mu^{(0)}$的值是固定的。 最后，由于$\mu^{(T)}$和$\mu^{(0)}$偏差不能太大，因此对于$\mu$还有一个L2Norm处理，将$\mu_k$的长度归一化。]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[EM算法学习笔记]]></title>
    <url>%2F2020%2F01%2F02%2FEM%E7%AE%97%E6%B3%95%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0%2F</url>
    <content type="text"><![CDATA[问题描述先看一个简单问题，例如现在有$m$个独立同分布样本$X=(x^{1}, x^{2}, \dots,x^{m})$，我们需要估计数据$X$的分布，那么我们只需要找到一个概率分布$P(x^{(i)}|\theta)$使得数据$X$的对数似然最大就行了，即只需要估计出参数$\theta=\mathop{\arg\max}_{\theta}L(\theta)=\mathop{\arg\max}_{\theta}\sum_{i=1}^{m}\log P(x^{(i)}|\theta)$ 接下来问题变得复杂一些，还有一个不可观测的隐含变量$Z$会影响到$X$的分布，这个时候为了估计$X$的分布，所进行的工作将变为找到$\theta, Z = \mathop{\arg\max}_{\theta, Z}L(\theta, Z) = \mathop{\arg\max}_{\theta, Z}\sum_{i=1}^{m}logP(x^{(i)}|\theta) = \mathop{\arg\max}_{\theta, Z}\sum_{i=1}^{m}\log \sum_{z^{(i)}}P(x^{(i)}, z^{(i)}|\theta)$这里$P(x^{(i)}|\theta)$其实是$P(x^{(i)}, z^{(i)}|\theta)$的边缘分布。 对于这个问题，直接对$Z$和$\theta$求导过于复杂，可能无法计算，因此需要EM算法来进行优化，得到一个可以接受的局部最优解。 EM算法EM算法分为两个重复的步骤，Expectation和Maximization Expectation首先，重写上式如下，这里多出了一个关于$Z$的概率分布$Q$，对于$z^{(i)}$的所有取值，可知$\sum_{z^{(i)}}Q(z^{(i)}) = 1$。 \begin{aligned} &\mathop{\arg\max}_{\theta, Z}\sum_{i=1}^{m}\log \sum_{z^{(i)}}P(x^{(i)}, z^{(i)}|\theta)\\ &= \mathop{\arg\max}_{\theta, Z} \sum_{i=1}^{m}\log \sum_{z^{(i)}} Q(z^{(i)}) \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})}\\ \end{aligned}由于$\sum_{z^{(i)}}Q(z^{(i)}) = 1$，因此$\log \sum_{z^{(i)}} Q(z^{(i)}) \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})} = \log E(\frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})})$，又由于$\log$为凹函数，因此$\log E(\frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})}) \ge E(\log \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})})$，即： \sum_{i=1}^{m}\log \sum_{z^{(i)}} Q(z^{(i)}) \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})} \ge \sum_{i=1}^{m} \sum_{z^{(i)}} Q(z^{(i)}) \log \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})}此时，假设$\theta$已经固定，那么要使得下面的等式成立 $\log E(\frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})}) = E(\log \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})})$ 我们需要让$\frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})}$等于一个常数$c$，因此对于所有的$i$，都满足 $c \times Q(z^{(i)}) = P(x^{(i)}, z^{(i)}|\theta)$ 两边同时求和，得到 $c \times \sum_{z} Q(z^{(i)}) = \sum_{z} P(x^{(i)}, z^{(i)}|\theta)$ 即： $c = \sum_{z} P(x^{(i)}, z^{(i)}|\theta)$ 因此： $Q(z^{(i)}) = \frac{P(x^{(i)}, z^{(i)}|\theta)}{\sum_{z} P(x^{(i)}, z^{(i)}|\theta)} = \frac{P(x^{(i)}, z^{(i)}|\theta)}{P(x^{(i)}|\theta)} = P(z^{(i)}|x^{(i)},\theta)$ 这样一来，确定了$Q(z^{(i)})$的值，等式可以成立，对数似然的下确界就被找到了，这个下确界即$\sum_{i=1}^{m} \sum_{z^{(i)}} Q(z^{(i)}) \log \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})}$，接下来在Maximization步骤，固定$Q(z^{(i)})$，通过$\theta$来最大化这个下确界，可以间接的达到优化对数似然的目的。 Maximization由于$Q(z^{(i)}) = P(z^{(i)}|x^{(i)},\theta^{old})$被固定为常量，不再和$\theta$相关，因此问题变为 \begin{aligned} &\mathop{\arg\max}_{\theta} \sum_{i=1}^{m} \sum_{z^{(i)}} Q(z^{(i)}) \log \frac{P(x^{(i)}, z^{(i)}|\theta)}{Q(z^{(i)})}\\ &=\mathop{\arg\max}_{\theta} \sum_{i=1}^{m} \sum_{z^{(i)}} Q(z^{(i)}) \log P(x^{(i)}, z^{(i)}|\theta) \end{aligned}总的来说，只需要首先初始化$\theta$，然后重复循环Expectation和Maximization两个步骤，不断更新$Q(z^{(i)})$和$\theta$，EM算法被证明最终可以收敛到一个局部最优解。]]></content>
      <tags>
        <tag>机器学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《ACNet——Strengthening the Kernel Skeletons for Powerful CNN via AsymmetricConvolution Blocks》]]></title>
    <url>%2F2019%2F12%2F31%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8AACNet%E2%80%94%E2%80%94Strengthening-the-Kernel-Skeletons-for-Powerful-CNN-via-AsymmetricConvolution-Blocks%E3%80%8B%2F</url>
    <content type="text"><![CDATA[主要工作提出了一种非对称卷积块（Asymmetric Convolution Block），使用一维的非对称卷积来加强方形卷积。 具体实现在训练过程中，将原始的$3 \times 3$卷积替换为一种非对称卷积块的结构，如下图所示。 在预测过程中，将ACB模块中的三个卷积核加起来，重新变成一个$3 \times 3$卷积，如下图所示。 对于预测过程，这样做并不会引起任何其他开销。但是实验结果表明，这样做可以提升模型的效果。 看起来这个ACB模块和直接训练一个$3 \times 3$卷积没有什么区别，但是一个细节的地方是，在实现ACB时，三个卷积分支在卷积层之后都会有一个BN层，在预测过程，混合卷积层权重时，BN层权重也需要进行混合，如下图所示。 那么这样分析之后，结果就很明显了，这就相当于为卷积核不同位置分配了可训练的权重。 我的实验结果我自己在最近做的乳腺肿块分割任务上，对我的baseline模型使用了ACB，发现dice score确实有1个百分点的提升。 我还尝试直接为卷积核构造一个空间权重参数，但是发现效果反而不如直接使用ACB，可能是因为ACB只为十字上的卷积核参数分配权重，算是一种约束条件，更加容易优化吧。]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《Deformable-Convolutional-Networks》]]></title>
    <url>%2F2019%2F09%2F03%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8ADeformable-Convolutional-Networks%E3%80%8B%2F</url>
    <content type="text"><![CDATA[提出的问题常规卷积层因为卷积核的固定几何结构，限制了模型的几何变换能力，而视觉任务中的一大难点就是如何适应目标的尺度缩放、变形等干扰，常规的卷积层在面对这类干扰较大的任务时，效果不好。 解决思想常规卷积可以分为两个步骤：采样和矩阵乘法，在采样阶段，可以加入一个自主学习的偏移，使得采样点组成的不再是固定的方形区域，从而达到可变形卷积的效果。 具体做法论文中提出了两个可变形模块：可变形卷积和可变形roi pooling。 可变形卷积使用$x$表示feature map，$w$表示卷积权重, $p_0$表示中心点位置坐标，假设卷积核大小为$3 \times 3$，那么采样区域相对于中心点的偏移区域$\mathcal{R} = \{(-1, -1), (-1, 0), …,(0, 1), (1, 1)\}$，因此传统的卷积可以写成如下： \begin{aligned} y(p_0) = \sum_{p_n \in \mathcal{R}}w(p_n)\cdot x(p_0 + p_n) \end{aligned}可变形卷积的操作可以写成如下，其中$\Delta p_n$是偏移量： \begin{aligned} y(p_0) = \sum_{p_n \in \mathcal{R}}w(p_n)\cdot x(p_0 + p_n + \Delta p_n) \end{aligned}令$p = p_0 + p_n + \Delta p_n$，$q$表示为图像上的所有位置，那么可以通过线性插值来计算$x(p)$：$x(p)=\sum_q G(q,p)\cdot x(q)$，其中$G(q,p)$表示线性插值核，$G(q,p)=g(q_x, p_x) \cdot g(q_y, p_y)$，$g(a,b) = max(0, 1-|a-b|)$ 在我的实验中，我将retinanet改为了一个带fpn的centernet（没有anchor，只用P2进行回归和分类输出），我在fpn的P2之后先叠加了三个可变形卷积层，然后再来分类和回归，发现训练根本不收敛，可能对于检测任务，可变形卷积层不是那么好用 由于我这边实验效果比较差，因此这篇文章后面的可变形ROI池化之类的内容就不往下读了。]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[“论文阅读《CornerNet-Detecting-Objects-as-Paired-Keypoints》”]]></title>
    <url>%2F2019%2F08%2F07%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8ACornerNet-Detecting-Objects-as-Paired-Keypoints%E3%80%8B%2F</url>
    <content type="text"><![CDATA[提出的问题单阶段目标检测大多基于anchor实现，但是用anchor存在两个问题： 为了保证所有ground truth都有匹配的anchor，不得不使用非常多的anchor，例如retinanet anchor个数100K, DSSD anchor个数40K，但是真正与ground truth iou较大的anchor非常少，导致了很严重的正负样本不平衡问题。 设置anchor时需要非常多的超参数，anchor的大小、长宽比、个数，使得找到合适的anchor设置非常困难 解决思想丢弃anchor，模型在回归目标位置时，不是去预测基于anchor的偏移量，而是使用两个feature map同时预测两个角点（左上、右下）即可，在feature map上的点，只有为目标角点的时候才进行激活预测其目标类别，只是这里又有三个问题： 两个feature map预测出来的角点，如何进行匹配，从而合并成一个目标？ 目标在两个角点之间，因此feature map上的角点位置缺少关于目标的信息，如何准确的进行分类？ feature map在经过下采样之后，feature map上的点的位置要映射回原图，这个时候存在位置精度的损失，可能造成预测位置的偏差。 具体做法针对第一个角点匹配的问题，这里引用了另外一篇论文的方法（《ssociative embedding: End-to-end learning forjoint detection and grouping》），在模型中，两个角点坐标的feature map都会额外预测一个一维的嵌入向量（embedding vector），希望当两个角点匹配时，两个角点位置预测的嵌入向量的距离尽可能接近，否则尽可能远离（这里嵌入向量的想法没看得太懂，可能还要看看原论文才能理解，除此之外计算量貌似有点大），因此定义了两个loss如下所示，其中$e_{t_k}$代表第k个目标左上角点的嵌入向量，$e_{b_k}$代表第k个目标右下角点的嵌入向量，$e_k = \frac{(e_{t_k} + e_{b_k})}{2}$，N代表目标的个数，$\Delta$在论文的所有实验中均为1。 \begin{aligned} L_{pull} &= \frac{1}{N}\sum_{k=1}^N[(e_{t_k} - e_k) ^ 2 + (e_{b_k} - e_k) ^ 2]\\ L_{push} &= \frac{1}{N(N - 1)}\sum_{k=1}^N\sum_{\begin{aligned} j = 1 \\ j \neq k \end{aligned}}max(0, \Delta - |e_k - e_j|) \end{aligned}针对第二个角点处没有目标信息的问题，论文中提出了Corner Pooling，如下图所示，在左上角角点预测过程中，进行pooling时，每个点水平向右和垂直向下搜索，找到最大的值，之后加起来作为这个点pooling之后的值，这样给出了当前点是否是角点的判断信息，并且引入了一个非常强的先验：一个点是否是左上角点，需要看其右边的信息和下面的信息来判断。而如果是在预测右下角点，那么Corner Pooling变为每个点水平向左和垂直向上搜索。 这里为了加快Corner Pooling的计算，如果是左上角点的Corner Pooling，可以从右下点开始计算，计算方式如下，$t_{ij}$和$l_{ij}$分别代表水平向右搜索和垂直向下搜索的结果，最终pooling结果$p_{ij} = t_{ij} + l_{ij}$。 \begin{aligned} t_{ij} &= \begin{cases} max(f_{t_{ij}}, t_{(i+1)j}) & if \quad i < H\\ f_{t_{Hj}} & otherwise \end{cases}\\ l_{ij} &= \begin{cases} max(f_{t_{ij}}, t_{i(j+1)}) & if \quad j < W\\ f_{t_{iW}} & otherwise \end{cases} \end{aligned}针对第三个问题，在模型中还预测两组偏移量，分别对左上角点和右下角点进行偏移，对于一组偏移量，其目标$o_k$计算如下，并使用SmoothL1Loss进行训练。 o_k = (\frac{x}{n} - \lfloor\frac{x}{n}\rfloor, \frac{y}{n} - \lfloor\frac{y}{n}\rfloor)最后要说的就是ground truth匹配问题，一个geound truth只匹配一个左上角点和一个右下角点，其余点都为负样本，但在训练时，在正样本周围画一个半径为$r$的圈，这里有个约束：半径$r$范围内的点，要保证可以和ground truth生成iou大于0.7的检测框，从而决定$r$的大小。 这个圈中除中心点之外，其余点在计算loss时，会降低其loss权重，作者定义了一个focal loss的变种，作为每个点的分类损失函数，如下所示，其中$C、H、W$分别表示类别数、图片高度、图片宽度，N表示图片上目标个数，$p_{cij}$表示位置$ij$处的$c$类预测概率，在论文所有实验中，$\alpha = 2$，$\beta = 4$，$y_{cij}$不是纯粹的标签，还编码了刚才说的loss函数权重信息，对于正样本点，$y_{cij} = 1$，对于正样本周围半径为$r$的圈内的点，$y_{cij} = 1 - e^{-\frac{x^2+y^2}{\sigma}}$(这里的$xy$是相对于中心点的坐标)，其中$\sigma = \frac{r}{3}$，而对于其余负样本点，$y_{cij} = 0$。 \begin{aligned} L_{det} = \frac{-1}{N}\sum_{c=1}^C\sum_{i=1}^H\sum_{j=1}^W\begin{cases} (1 - p_{cij})^\alpha log(p_{cij}) & if \quad y = 1\\ (1 - y_{cij})^\beta p_{cij}^\alpha log(1 - p_{cij}) & otherwise \end{cases} \end{aligned}存在的问题个人觉得这个模型的计算可能非常慢，首先是嵌入向量的匹配过程，我没有想到比较好的实现方式，暴力匹配的话应该非常慢，还有一个问题是Corner Pooling的计算，这里如果需要按顺序计算，那么GPU并行加速提升不大。]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《Reducing-the-Dimensionality-of-Data-with-Neural-Networks》]]></title>
    <url>%2F2019%2F08%2F01%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8AReducing-the-Dimensionality-of-Data-with-Neural-Networks%E3%80%8B%2F</url>
    <content type="text"><![CDATA[提出的问题使用多层神经网络进行数据降维编码和解码的时候，使用梯度下降法的效果非常依赖权重的初始化，难以训练。 解决思想定义一种两层的神经网络模型（注意，只有一层权重，但是有两个偏置变量），称为限制玻尔兹曼机（RBM）,多个RBM串联组成一个自动编/解码器，训练时，每个RBM单独训练（每个玻尔兹曼机的目标都是使编码解码之后的输出和原始输入尽可能相同，具体模型结构图如下所示。 实现细节定义RBM的能量函数$E(v,h) = - \sum_{i \in pixels}b_i v_i - \sum_{j \in features}b_j h_j - \sum_{i,j}v_iw_{ij}h_j$，其中$v$表示可见单元(visible unit)，$h$表示隐藏单元(hidden unit)，$i$表示可见单元编号，$j$表示隐藏单元编号，$w_{ij}$表示权重矩阵中的相应值，$b_i$表示可见层的偏置，$b_j$表示隐藏层的偏置，训练每个RBM就是最小化其能量函数。]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《Improving-neural-networks-by-preventingco-adaptation-of-feature-detectors》]]></title>
    <url>%2F2019%2F07%2F31%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8AImproving-neural-networks-by-preventingco-adaptation-of-feature-detectors%E3%80%8B%2F</url>
    <content type="text"><![CDATA[提出的问题在小数据集上使用非常大的神经网络模型训练，往往会出现过拟合的情况。 解决思想随机忽视掉一半的特征检测器，即drop rate为0.5的Dropout，作者解释这样做的原因是减少特征之间的相互依赖性，并展示了在CNN中使用和不使用Dropout两种情况所提取到的特征图，如下所示，作者解释说:“ The features that dropout learns are simpler and looklike strokes, whereas the ones learned by standard backpropagation are difficult to interpret”。 我觉得这个解释很勉强，关于Dropout的有效性，另外一篇论文《Dropout: a simple way to prevent neural networks from overfitting》有更多的描述，其中提到了Dropout可以让feature激活值变得更加稀疏,如下所示,我认为这是Dropout抑制过拟合的一个原因 实现细节 MNIST:使用全连接神经网络,在输入层使用drop rate为0.2的Dropout，在隐藏层使用drop rate为0.5的Dropout,学习速率指数衰减，初始学习率为10，不使用权重衰减，但是使用权重裁剪策略，将权重的“squared length”限制在15以内。 CIFAR-10:使用CNN模型，在最后的卷积层之后加入一个局部全连接层，作为一个不共享权重的卷积层，并在这个层上使用Dropout。 CIFAR-10:使用CNN模型，在最后的两个全连接层上进行Dropout。]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《Multi-Task-Learning-Using-Uncertainty-to-Weigh-Lossesfor-Scene-Geometry-and-Semantics》]]></title>
    <url>%2F2019%2F07%2F29%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8AMulti-Task-Learning-Using-Uncertainty-to-Weigh-Lossesfor-Scene-Geometry-and-Semantics%E3%80%8B%2F</url>
    <content type="text"><![CDATA[提出的问题在多任务学习的过程中，不同的损失函数权重设置非常困难。 解决思想使用同方差不确定性来对多任务损失函数进行组合。 具体方法$f^{W}(x)$代表输入x经过参数为$W$的神经网络之后得到的输出，那么对于回归任务，可以定义其高斯似然概率：$p(y|f^{W}(x)) = \mathcal{N}(f^{W}(x), \sigma^2)$,对于分类任务，因为常用Softmax来进行输出的处理，所以定义其似然概率为$p(y|f^{W}(x)) = Softmax(\frac{1}{\sigma^2}f^{W}(x))$，这里的$\frac{1}{\sigma^2}$不会影响Softmax的结果。 如果一个模型有多个任务，可以用最大似然法来定义其损失函数，假设模型存在一个回归任务和一个分类任务，其对数似然定义如下： \begin{aligned} log(p(y_1,y_2=c|f^{W}(x))) &= log(p(y_1|f^{W}(x))) + log(p(y_2=c|f^{W}(x), \sigma_2^2))\\ &= log(\mathcal{N}(f^{W}(x), \sigma_1^2)) + log(Softmax(y_2=c; f^{W}(x)))\\ &= log(\frac{1}{\sqrt{2\pi}\sigma} e^{-\frac{(x - f^{W}(x))^2}{2\sigma_1^2}}) + log(\frac{e^{\frac{1}{\sigma ^ 2}f_c^W(x)}}{\sum_{\hat{c}}e^{\frac{1}{\sigma_2 ^ 2}f_{\hat{c}}^W(x)}})\\ &= -\frac{(x - f^{W}(x))^2}{2\sigma_1^2} - log\sigma_1 - log\sqrt{2\pi} + log(\frac{e^{\frac{1}{\sigma ^ 2}f_c^W(x)}}{\sum_{\hat{c}}e^{\frac{1}{\sigma_2 ^ 2}f_{\hat{c}}^W(x)}})\\ \end{aligned}当$\sigma_2$接近1时，$\frac{1}{\sigma_2}\sum_{\hat{c}}e^{\frac{1}{\sigma_2 ^ 2}f_{\hat{c}}^W(x)} \approx (\sum_{\hat{c}}e^{f_{\hat{c}}^W(x)})^\frac{1}{\sigma_2 ^ 2}$，上式可以写为： \begin{aligned} &\approx -\frac{(x - f^{W}(x))^2}{2\sigma_1^2} - log\sigma_1 - log\sqrt{2\pi} + log(\frac{e^{\frac{1}{\sigma ^ 2}f_c^W(x)}}{\sigma_2(\sum_{\hat{c}}e^{f_{\hat{c}}^W(x)})^\frac{1}{\sigma_2 ^ 2}})\\ &= -\frac{(x - f^{W}(x))^2}{2\sigma_1^2} - log\sigma_1 - log\sqrt{2\pi} + \frac{1}{\sigma_2 ^ 2}log(\frac{e^{f_c^W(x)}}{\sum_{\hat{c}}e^{f_{\hat{c}}^W(x)}}) - log\sigma_2\\ &= \frac{1}{\sigma_1 ^ 2}L_1(W) + \frac{1}{\sigma_2 ^ 2}L_2(W) - log\sigma_1 - log\sigma_2 - log\sqrt{2\pi} \end{aligned}因此，在模型中设置两个可训练权重$\sigma_1$和$\sigma_2$，使用$-\frac{1}{\sigma_1 ^ 2}L_1(W) - \frac{1}{\sigma_2 ^ 2}L_2(W) + log\sigma_1 + log\sigma_2$作为多任务损失函数，即可自动学习模型的损失的权重。 一些问题 上式的简化是说在$\sigma_2$接近1的时候有用，但是模型学出来的权重很可能不在1附近，或者可以将$\sigma_2$固定为1？ 从论文贴出的结果看，组合多任务学习的效果比单任务的效果要差？结果图如下]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《Learning-a-Discriminative-Feature-Network-for-Semantic-Segmentation》]]></title>
    <url>%2F2019%2F07%2F26%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8ALearning-a-Discriminative-Feature-Network-for-Semantic-Segmentation%E3%80%8B%2F</url>
    <content type="text"><![CDATA[提出的问题 类内不一致问题：被分配到同一个标签的一块区域，像素之间表现不同 类间模糊问题：不同标签的邻近区域，像素之间表现相似 解决思想从宏观角度考虑语义分割问题，将语义分割问题当做将一致的语义标签分配给事物类别的任务，将一个类别的像素当成一个整体去考虑，使用一种Discriminative Feature Network(DFN)，由Smooth Network和Border Network组成 Smooth Network用于解决类内不一致问题，目的是学习针对类内不一致性的鲁棒特征，主要考虑全局上下文信息和多尺度的特征，因此Smooth Network基于U型结构来获取多尺度上下文信息，并使用Channel Attention Block(CAB)逐步利用高级特征来选择低级特征。 Boder Network用于解决类间模糊问题，通过整合语义边界损失来使模型发现能增大类间距离的更具描述性的特征。 整体结构如下图所示： 一些问题原文：”Accordingto our observation, the different stages have different recognition abilities resulting in diverse consistency manifestation.”，这里作者是如何观察到的，还有这里的”consistency”是指什么？]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[学习笔记-2019-07]]></title>
    <url>%2F2019%2F07%2F04%2F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-2019-07%2F</url>
    <content type="text"><![CDATA[2019-07-04几种回归的概念线性回归很简单，就是$y=W^Tx + b$，而广义线性回归则可以用一个单调可微的函数$g(\centerdot)$写成$y=g^{-1}(W^Tx + b)$或者$g(y) = W^Tx + b$,对数回归$ln(y) = W^Tx + b$就是广义线性回归的一个例子。 如果要使用回归来解决分类问题，理想的情况是使用单位阶跃函数将回归值映射为类别标签，但单位阶跃函数不连续，因此使用对数几率函数来完成这个映射，对数几率函数即$\frac{1}{1 + e^{-z}}$，是sigmoid函数的一种。 这里顺便介绍下几率的概念：若一个事件发生的概率为$y$，则这个事件不发生的概率为$1-y$，两者的比值$\frac{y}{1-y}$被称为几率，反应的是事件发生的相对可能性，对几率取对数得到对数几率$log\frac{y}{1-y}$即为logit，逻辑回归(logistic regression)也可称为对数几率回归(logit regressoion)，就是用线性回归去逼近对数几率，如下： \begin{aligned} y &= \frac{1}{1 + e^{-z}}\\ 代入线&性回归得到的z：\\ y &= \frac{1}{1 + e^{-(W^Tx + b)}}\\ log\frac{y}{1-y} &= W^Tx + b \end{aligned}线性判别分析(Linear Discriminant Analysis, LDA)线性判别分析也叫“Fisher判别分析”，其思想非常朴素：试图找到一条过原点的直线，可以表示为$y=w^Tx$(这个表达式中的$y$表示$x$投影到这条直线后和原点的距离)，使得当样本投影到该直线时不同类别样本的距离尽可能远，而相同类别的数据尽可能接近，预测时按照样本的投影位置对其进行分类。 以两类数据$x_1, x_2$为例，设$\mu_1,\mu_2,\Sigma_1,\Sigma_2$分别表示两类数据的均值和方差，则投影之后的均值和方差为$w\mu_1,w\mu_2,w^T\Sigma_1w,w^T\Sigma_2w$，因为直线是一维空间，所以这些均为实数，投影之后的类内距离可以用方差来衡量，这里使用$w^T\Sigma_1w + w^T\Sigma_2w$来度量投影之后的类内距离，而类间距离可以写成$||w\mu_2 - w\mu_1||_2^2$，同时考虑两种距离，给出希望最大化的目标函数如下。 \begin{aligned} J &= \frac{||w^T\mu_2 - w^T\mu_1||_2^2}{w^T\Sigma_1w + w^T\Sigma_2w}\\ &= \frac{w^T(\mu_2 - \mu_1)(\mu_2 - \mu_1)^Tw}{w^T(\Sigma_1 + \Sigma_2)w} \end{aligned}定义类内散度矩阵$S_w = \Sigma_1 + \Sigma_2$，类间散度矩阵$S_b = (\mu_2 - \mu_1)(\mu_2 - \mu_1)^T$，上面的优化目标可以简写为如下。 \begin{aligned} J = \frac{w^TS_bw}{w^TS_ww} \end{aligned}这个优化目标又称为$S_b$和$S_w$的广义瑞利商，注意到分子分母中都有$w$的二次项，因此和$w$大小无关，只和w方向有关，所以优化问题可以写成下式。 \begin{aligned} \min_w& \quad-w^TS_bw\\ s.t.& \quad w^TS_ww = 1 \end{aligned}用拉格朗日乘子法进行优化，求解$S_bw = \lambda S_ww$，因$S_bw$方向和$\mu_2 - \mu_1$相同，因此令$S_bw = \lambda(\mu_2 - \mu_1)$，代入求解，可以得到$w = S_w^{-1}(\mu_2 - \mu_1)$。]]></content>
      <tags>
        <tag>学习笔记，杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[学习笔记-2019-06]]></title>
    <url>%2F2019%2F06%2F20%2F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-2019-06%2F</url>
    <content type="text"><![CDATA[2019-06-20课程学习在弱监督学习中的应用在网上看到了关于CVPR2017 WebVision图片分类竞赛冠军的技术分享，大致整理其主要思路。 这个比赛的数据非常多，但是标签可能存在问题，要求是在存在问题标签的数据中进行模型训练。 这里主要使用了一种课程学习的思想，先让模型学习简单的样本，然后再让其学习较困难的样本，循序渐进，得到最终的训练模型。 难点在于没有先验知识的情况下定义数据集或者图片的难易程度。 其基本思想是在同一个类别的图像的特征空间中，越密集的地方的图像特征越相似，表示图片类别越可能正确，根据这个思想将图片分类成不同的难易程度，分步，使用不同权重对待不同数据，进行模型训练。 这里先介绍一种样本分析方法density-distance：对于样本$P_i$，其特征$f(P_i)$，计算所有样本之间的特征距离矩阵：$D_{ij} = {||f(P_i) - f(P_j)||} ^ 2$，定义样本密度：$\rho_i = \sum_i X(D_{ij} - d_c) \quad X(d) = \begin{cases} 1 &amp; d &lt; 0 \\ 0 &amp; other \end{cases}$，其中$d_c$是一个超参数， 计算每个样本的密度距离值：$\delta_i = \begin{cases}min_{j:\rho_j &gt; \rho_i}(D_{ij}) &amp; if \quad \exists j s.t. \rho_j &gt; \rho_i \\ max(D_{ij}) &amp; otherwise \end{cases}$，将$\rho_i$和$\delta_i$相乘，值越大的说明可能在聚类中心，数据标签越可能正确。 对数据进行分类的具体实现步骤如下： 设计课程 使用所有数据($P_i$)训练一个特征提取模型：$f$ 对所有样本的进行特征提取：$f(P_i)$ 对每个类别的图片分别进行density-distance分析 按照$\rho_i \times \delta_i$结果划分出三个子类，分别代表难易度不同的图片，作为先后训练的数据 课程训练 首先使用第一类最简单的数据进行训练 为第二类数据设置权重0.5，继续训练 为第三类数据设置权重0.5，继续训练 2019-06-29BCE、CE两个分类损失与激活函数的关系网上都说BCE用于二分类，CE用于多分类，我认为这里有些问题可以讨论，下面的例子基于tensorflow和keras。 BCE和CE是常用的分类损失函数，计算公式如下。 \begin{aligned} BCE(x)_i &= -(y_i log(f(x)_i) + (1 - y_i) log(1 - f(x)_i)) \\ BCE_{loss}(x) &= \frac{\sum_{i=0}^{C}BCE(x)_i}{C}\\ CE_{loss}(x) &= CE(x) = \sum_{i=0}^{C}-y_i log(f(x)_i) \end{aligned}从上面可以看出，CE是个标量，直接作为loss函数使用，而BCE计算出的是一个向量，在keras的实现中，BCE损失最终需要对每个类别求平均才能作为loss函数使用。 而对于一个batch的数据，在keras实现中，BCE和CE都是直接对每个样本求平均。 \begin{aligned} BCE_{final} &= \frac{\sum_{b=1}^{N} BCE_{loss}(x^{(b)}}{N}\\ CE_{final} &= \frac{\sum_{b=1}^{N} CE_{loss}(x^{(b)}}{N} \end{aligned}对于一般的分类任务，最终都是一个全连接层变为输出向量$O$，之后再经过sigmoid或者softmax变为预测概率向量。 \begin{aligned} O_{sigmoid} = [\frac{1}{1 + e^{-O_1}} \quad \frac{1}{1 + e^{-O_2}} \quad ... \quad \frac{1}{1 + e^{-O_c}}]\\ O_{softmax} = [\frac{O_1}{\sum_{j = 1}^c O_j} \quad \frac{O_2}{\sum_{j = 1}^c O_j} \quad ... \quad \frac{O_c}{\sum_{j = 1}^c O_j}] \end{aligned}这里要注意的是，如果输出的损失函数是用的sigmoid，那么输出向量中，每个元素是相互独立的，如果这个时候使用CE作为损失函数，CE只计算了$y_i$为1的地方的损失，对于$y_i$为0的地方不考虑，所以可能导致模型训练出现问题。 tensorflow作为后端的keras定义的两种损失函数见下方代码，一般使用时要注意from_logits这个参数，如果为False，则binary_crossentropy默认模型的输出是$O_{sigmoid}$而categorical_crossentropy默认模型的输出的是$O_{softmax}$BCE会尝试将$O_{sigmoid}$还原成$O$，然后调用tf.nn.sigmoid_cross_entropy_with_logits这个方法，而对于CE,因为softmax无法还原，因此keras直接自己写了个损失函数，否则会直接调用softmax_cross_entropy_with_logits这个方法。 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566def binary_crossentropy(target, output, from_logits=False): """Binary crossentropy between an output tensor and a target tensor. # Arguments target: A tensor with the same shape as `output`. output: A tensor. from_logits: Whether `output` is expected to be a logits tensor. By default, we consider that `output` encodes a probability distribution. # Returns A tensor. """ # Note: tf.nn.sigmoid_cross_entropy_with_logits # expects logits, Keras expects probabilities. if not from_logits: # transform back to logits _epsilon = _to_tensor(epsilon(), output.dtype.base_dtype) output = tf.clip_by_value(output, _epsilon, 1 - _epsilon) output = tf.log(output / (1 - output)) return tf.nn.sigmoid_cross_entropy_with_logits(labels=target, logits=output)def categorical_crossentropy(target, output, from_logits=False, axis=-1): """Categorical crossentropy between an output tensor and a target tensor. # Arguments target: A tensor of the same shape as `output`. output: A tensor resulting from a softmax (unless `from_logits` is True, in which case `output` is expected to be the logits). from_logits: Boolean, whether `output` is the result of a softmax, or is a tensor of logits. axis: Int specifying the channels axis. `axis=-1` corresponds to data format `channels_last`, and `axis=1` corresponds to data format `channels_first`. # Returns Output tensor. # Raises ValueError: if `axis` is neither -1 nor one of the axes of `output`. """ output_dimensions = list(range(len(output.get_shape()))) if axis != -1 and axis not in output_dimensions: raise ValueError( '&#123;&#125;&#123;&#125;&#123;&#125;'.format( 'Unexpected channels axis &#123;&#125;. '.format(axis), 'Expected to be -1 or one of the axes of `output`, ', 'which has &#123;&#125; dimensions.'.format(len(output.get_shape())))) # Note: tf.nn.softmax_cross_entropy_with_logits # expects logits, Keras expects probabilities. if not from_logits: # scale preds so that the class probas of each sample sum to 1 output /= tf.reduce_sum(output, axis, True) # manual computation of crossentropy _epsilon = _to_tensor(epsilon(), output.dtype.base_dtype) output = tf.clip_by_value(output, _epsilon, 1. - _epsilon) return - tf.reduce_sum(target * tf.log(output), axis) else: return tf.nn.softmax_cross_entropy_with_logits(labels=target, logits=output) 最后，总结一下就是：BCE loss不是不能用于多分类, 但CE loss不适合单输出的分类, BCE loss最好用sigmoid激活函数，而CE loss最好用softmax函数。]]></content>
      <tags>
        <tag>学习笔记，杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文阅读《CondenseNet: An Efficient DenseNet using Learned Group Convolutions》]]></title>
    <url>%2F2019%2F06%2F04%2F%E8%AE%BA%E6%96%87%E9%98%85%E8%AF%BB%E3%80%8ACondenseNet-An-Efficient-DenseNet-using-Learned-Group-Convolutions%E3%80%8B%2F</url>
    <content type="text"><![CDATA[主要工作本文主要贡献是提出了一种比ShuffleNets还要高效很多的CondenseNets。 主要思想该论文认为普通的卷积模型:layer-by-layer的连接模式，模型需要在每一层都去重复最前面层的特征，因此DenseNet的短连接所引导的特征重用对这个问题有一定的缓解，但论文中猜想对于DenseNet这样的密集短连接，会引入一些冗余的浅层特征，因此论文提出了一种方法来减少这种冗余，并形成了一种高效的模型结构。 主要方法将一层中的输出特征分成多组（分组也是学习得到），并在训练过程中按照分组逐渐移除组中与一些不重要的输入特征之间的连接。 效率对比在ImageNet数据上进行图像分类实验， CondenseNets相比于同样正确率的DenseNets模型，计算量减少到了DenseNets的$\frac{1}{10}$，相比于相近top-1错误率的MobileNet，CondenseNets计算量为MobileNet的$\frac{1}{2}$。 具体实现如图，在Condensing阶段训练过程中，使用sparsity inducing regularization重复训练模型， 并丢弃权重较低的特征（在丢弃时，保证同一个Group的卷积核有相同的稀疏连接，方便在测试阶段使用一个标准的group convolution实现计算），在optimization阶段，固定分组，并训练卷积核。图中解释可能有误，图上说是的每个Condensing stage 移除$(C-1)/C$比例的连接，但后面又说每个Condensing stage之后移除$1/C$比例的连接，我觉得应该是后面一种说法正确 特征凝聚原则(Condensation Criterion)普通的卷积核是四维的$O \times R \times W \times H$，其中$O$代表输出的特征个数，$R$代表输入的特征个数，$W$代表卷积核宽度，$H$代表卷积核高度，如果简化为$1 \times 1$卷积，则卷积核可以看成一个$O \times R$的矩阵$F$, 对于某个分组$G$, 其卷积权重表示为$F^G$，$F^g_{ij}$则表示分组$g$中第$j$个输入到第$i$个输出的权重。 在筛选的过程中，第$j$输入特征的重要程度由$\sum_{i=1}^{O/G}|F^g_{ij}|$来衡量，丢弃的过程可以简单描述为：对于矩阵$F^g$，如果其某一列的$L_1$正则项较小，则把这一列的全都置0。 为了激励同一组的卷积倾向于同样的一组输入特征，作者没有使用逐元素的$L_1$正则化，而是使用其提出的一种group-lasso正则化，其正则化项计算如下： \sum_{g=1}^G \sum_{j=1}^R \sqrt{\sum_{i=1}^{O/G}{F^g_{ij}}^2}这样的的正则化项由于其值由每个$F^g$中的每一列的最大值来主导，因此会倾向于把$F^g$中某一列整体变小，满足要求。 特征凝聚因子(Condensation Factor)一组输出所连接到的输入个数比例不一定是$\frac{1}{G}$，这里定义特征凝聚因子$C$，允许每一组输出连接到$\lfloor\frac{R}{C}\rfloor$个输入特征。 特征凝聚过程(Condensation Procedure)在上面的Learn Group Convolution的学习和预测示意图中，在经过$C - 1$个Condensing stage之后，只有$1 - (C - 1) \times \frac{1}{C}=\frac{1}{C}$比例的输入特征保留下来。 epoch设置：Condensing stage $C-1$的训练epoch设置为$\frac{M}{2(C-1)}$,其中M表示总共的训练epoch数。 学习速率：使用余弦状学习速率，初始值为0.1,300个epoch下降到0，如下图所示。 上图在150代的时候，训练loss跳跃上升，论文解释说是因为在最后一个Condensing stage，去掉了当前一半的连接导致。 特征选择和重排(Index Layer)在经过训练丢弃一些卷积核权重之后，层间的连接非常杂乱，为了高效的在硬件上实现组卷积操作，增加了Index Layer用于特征选择和重排。(暂时没有看到有关Index Layer的具体实现方法的描述) 模型结构设计(Architecture Design) 普通的DenseNet结构在每个dense block之后特征个数增加$k$(常数)个，作者在一篇论文中看到在DenseNet中更深的层更多的依赖深层特征而非浅层特征，因此作者决定将k改为指数增长，加大深层特征所占的比例，所以设置$k=2^{m-1}k_0$，其中$m$表示层的编号，$k_0$是一个常数。 为了提高特征重用率，作者将原始输入（或者原始输入对应大小的平均池化下采样）连接到了所有的层。 在CIFAR-10数据上进行的对比实验特征凝聚因子对比实验 在DenseNet50的基础上，对比了不同模型权重丢弃程度(特征凝聚因子)下的测试集错误率，如下图，蓝色表示原始模型，黄色表示其他的模型剪枝方法，红色表示作者提出的凝聚模型，三个凝聚模型的分组个数$G$都设置为4，这里将特征凝聚因子$C$作为横坐标，表示使用不同的特征凝聚因子的效果，其它的模型剪枝方法最终也是保留$\frac{1}{C}$比例的连接作为对比 分组个数实验 在四个对比实验中，特征凝聚因子$C$使用固定值：8。 特征凝聚因子效率对比 这里作者说所有实验固定分组个数$G=4$，然后观察不同的凝聚因子$C$下的测试集错误率和FLOPs的关系。这里没搞清楚，按理说$G$和$C$一旦确定，那么FLOPs就是固定的，为什么这里对于每个$C$可以变化FLOPs？模型不同？暂时没有看到相应的描述]]></content>
      <tags>
        <tag>论文阅读</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[编译安装emacs记录]]></title>
    <url>%2F2019%2F05%2F08%2F%E7%BC%96%E8%AF%91%E5%AE%89%E8%A3%85emacs%E8%AE%B0%E5%BD%95%2F</url>
    <content type="text"><![CDATA[最近需要实现一些tensorflow OP，所以先找一个ubuntu下的C++ IDE，决定用emacs，下面是安装过程。 文件下载使用镜像站: http://mirrors.kernel.org/gnu/emacs/，下载26.2版本 安装流程按照安装说明，首先解包之后运行 ./configure --prefix=/usr/local 遭遇无情报错： 三个package没有，因此尝试安装 sudo apt-get install libxpm-dev sudo apt-get install libdif-dev sudo apt-get install gnutls-bin sudo apt-get install libgnutls-dev 安装完后，configure就没有问题了,configure执行完后，按照常规操作，开始make，但是这里又遇到无情报错： locate 找了一下libpcre，发现就在Anaconda目录下面就有，因此make之前设置一下动态链接库搜索路径 ecport LD_LIBRARY_PATH=/home/lty/env/Anaconda/lib:$LD_LIBRARY_PATH 重新make就成功了，make完成之后先检查编译出来的可执行文件是否可用，使用命令： src/emacs -Q 成功打开emacs，常规操作，没有什么问题，之后开始安装依旧是常规的 make install,]]></content>
      <tags>
        <tag>环境搭建</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[refineDet模型结构要点]]></title>
    <url>%2F2019%2F04%2F15%2FrefineDet%E6%A8%A1%E5%9E%8B%E7%BB%93%E6%9E%84%E8%A6%81%E7%82%B9%2F</url>
    <content type="text"><![CDATA[因为需要实现keras版本的refineDet模型，因此考虑从keras-retinanet代码上进行修改，这里记录refineDet模型结构中的一些关键实现方式。 为了获取更高层的信息，在backbone网络中，加深了一层，对于ResNet-101，添加了一个额外的residual block. 在anchor大小选择方面，根据《S3FD: Single Shot Scale-invariant Face Detector》、《Understanding the Effective Receptive Field inDeep Convolutional Neural Networks》这两篇论文，anchor大小的选择应该要比理论感受小很多，使用有效感受野来估计每一个检测层的anchor大小，大约使用检测层stride的4倍作为anchor大小，并在anchor基础大小之上使用三种比例0.5,1.0,2.0 anchor匹配时，首先对于每个ground truth，分别匹配一个jaccard overlap最大的anchor，之后再将剩下的每一个anchor分配给jaccard overlap大于0.5的ground truth。 计算loss时，对anchor进行hard negative mining，保证negative anchor和positive anchor的比例是3：1。]]></content>
      <categories>
        <category>工程实践</category>
      </categories>
      <tags>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[学习笔记-2019-04]]></title>
    <url>%2F2019%2F04%2F06%2F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-2019-04%2F</url>
    <content type="text"><![CDATA[2019-04-06对于随机批梯度下降理解 对于batch size：从n个训练数据对真实数据分布进行估计，其标准差可以写为$\sigma/\sqrt{n}$，因此加大batch size对模型的学习效果的提升不是线性的。 对于数据：随机批梯度下降，可以减少计算的冗余度，因为很多数据其求出的梯度基本相同，最坏的情况就是训练集中每个数据都是相同的，如果使用原始的梯度下降算法，对于整个训练集进行梯度计算，最后得到的梯度和根据单个样本计算的梯度相同，存在大量计算冗余，随机批梯度下降对这种问题有一定缓解作用。 小的batch size：小的batch size 可以起到正则化的作用，有可能是因为小的batch size在估计梯度的过程中，引入了一定地噪声，从而可以使得模型的泛化误差减小，但是小的batch size 需要更小的学习速率和更多的学习步数。 对于优化算法：不同的优化算法对于batch size有不一样的要求，因为不同的优化算法对采样误差的敏感程度不同，可能一些优化算法需要从样本中提取的信息很难从少量的样本中估计出来，因此需要设置batch size为一个较大值才会有较好效果。仅仅直接使用梯度$g$的优化算法往往有较好的鲁棒性，并一般可以适应100这种小的batch size。 病态矩阵与条件数理解对于矩阵方程如下： \begin{aligned} A x = b \end{aligned}若对于一个较小的样本观察误差$\Delta b$，使得$A(x+\Delta x)=b + \Delta b$，$\Delta x$如果比$\Delta b$大很多，则称矩阵A是一个病态矩阵，一个典型的病态矩阵如下： \begin{bmatrix} 1000 &1000\\ 0 &0.001 \end{bmatrix}其中两个列向量的相关性非常大，夹角非常小，表示的特征太过相似，若$b$是第一个列向量方向的单位向量,则求出的解为$[n, 0]^T$，若$b$稍微偏差一点，变到了第二个列向量的方向，则求出来的解变为$[0\ 1]^T$，两次解的差距非常大，仅仅因为$b$上有非常小的偏移。 对于某种矩阵范数$||A||$以及某种向量范数$||b||$，若: A(x+\Delta x)=b + \Delta b则 A\Delta x = \Delta b即 \Delta x = A^{-1}\Delta b则有 ||\Delta x|| \le ||A^{-1}||\cdot||\Delta b||同时有 ||A|| \cdot ||x|| \ge ||b||因此 \frac{||\Delta x||}{||A|| \cdot ||x||} \le \frac{||A^{-1}||\cdot||\Delta b||}{||b||} \frac{||\Delta x||}{||x||} \le (||A^{-1}||\cdot||A||)\frac{||\Delta b||}{||b||}这里将$||A^{-1}||\cdot||A||$称为矩阵A的条件数，对于不同的范数，条件数各有不同，但都反映了矩阵A的病态程度，条件数越大，矩阵呈现更加明显的病态特征。 2019-04-11SGD中的Momentum方法momentum方法主要针对两个问题： 参数空间存在条件数较大的病态Hessian矩阵。 随机梯度下降对梯度的估计存在偏差。 v = \alpha v - \epsilon \nabla_\theta(\frac{1}{m}\sum_{i=1}^{m}L(f(x^{(i)};\theta), y^{(i)}))\\ \theta \leftarrow \theta + v如果每次梯度都是$g$，那么最终的$v$会趋近于$\frac{\epsilon g}{1 - \alpha}$ SGD中的Nesterov Momentum方法 v = \alpha v - \epsilon \nabla_\theta(\frac{1}{m}\sum_{i=1}^{m}L(f(x^{(i)};\theta + \alpha v), y^{(i)}))\\ \theta \leftarrow \theta + v网络权重随机初始化的原因如果两个计算单元具有相同的输入和激活函数，如果初始化相同，那么这两个计算单元在优化过程中很可能一直同步，最终出现冗余单元的情况，即两个计算单元计算的是相同的函数，重复计算没有意义。 2019-04-12AdaGrad优化算法Global learning rate $\epsilon$ Initial parameter $\theta$ Small constant $\delta$ Initial variable $r = 0$ batch size $m$ $A \odot B$: hadamard product of $A$ and $B$ 每次计算迭代过程如下： \begin{aligned} g &\leftarrow \frac{1}{m} \nabla_\theta\sum_iL(f(x^{(i)}; \theta), y^{(i)})\\ r &\leftarrow r + g \odot g\\ \Delta \theta &\leftarrow -\frac{\epsilon}{\delta + \sqrt{r}} \odot g\\ \theta &\leftarrow \theta + \Delta \theta \end{aligned}此算法主要用于凸函数的优化问题，在梯度较小的地方可以加大参数移动速度，但是我觉得这个算法不好，因为对梯度的方向进行了更改，而且$r$项一直增大，会使得学习过程很快趋于停滞。 RMSProp优化算法Global learning rate $\epsilon$ decay rate $\rho$ Initial parameter $\theta$ Small constant $\delta$ Initial variable $r = 0$ batch size $m$ $A \odot B$: hadamard product of $A$ and $B$ 每次计算迭代过程如下： \begin{aligned} g &\leftarrow \frac{1}{m} \nabla_\theta\sum_iL(f(x^{(i)}; \theta), y^{(i)})\\ r &\leftarrow \rho r + (1 - \rho)g \odot g\\ \Delta \theta &\leftarrow -\frac{\epsilon}{\sqrt{\delta + r}} \odot g\\ \theta &\leftarrow \theta + \Delta \theta \end{aligned}在AdaGrad算法的基础上，通过衰减参数$\rho$可以避免学习过程趋于停滞。 带有Nesterov Momentum 的RMSProp优化算法Global learning rate $\epsilon$ decay rate $\rho$ momentum coefficent $\alpha$ Initial parameter $\theta$ Small constant $\delta$ initial velocity $v$ Initial variable $r = 0$ batch size $m$ $A \odot B$: hadamard product of $A$ and $B$ 每次计算迭代过程如下： \begin{aligned} \tilde{\theta} &\leftarrow \theta + \alpha v\\ g &\leftarrow \frac{1}{m} \nabla_{\tilde{\theta}}\sum_iL(f(x^{(i)}; \tilde{\theta}), y^{(i)})\\ r &\leftarrow \rho r + (1 - \rho)g \odot g\\ v &\leftarrow \alpha v -\frac{\epsilon}{\sqrt{r}} \odot g\\ \theta &\leftarrow \theta + v \end{aligned}这里计算了Nesterov动量项以应对可能的梯度估计偏差和参数空间病态Hessian矩阵问题。 Adam优化算法Step size $\epsilon$ Exponential decay rates for moment estimates $\rho_1$ $\rho_2$ Small constant $\delta$ Initial parameter $\theta$ Initialize 1st and 2nd moment variables $s=0$ $r=0$ Initialize time step $t=0$ $A \odot B$: hadamard product of $A$ and $B$ 每次计算迭代过程如下: \begin{aligned} g &\leftarrow \frac{1}{m} \nabla_\theta\sum_iL(f(x^{(i)}; \theta), y^{(i)})\\ t &\leftarrow t + 1\\ s &\leftarrow \rho_1 s + (1-\rho_1) g\\ r &\leftarrow \rho_2 r + (1-\rho_2) g \odot g\\ \hat{s} &\leftarrow \frac{s}{1-\rho_1^t}\\ \hat{r} &\leftarrow \frac{r}{1 - \rho_2^t}\\ \Delta_\theta &\leftarrow -\epsilon \frac{\hat{s}}{\sqrt{\hat{r}} + \delta}\\ \theta &\leftarrow \theta + \Delta_\theta \end{aligned}牛顿法首先通过二阶估计来表达损失函数： \begin{aligned} \hat{J}(\theta) &\approx J(\theta_0) + (\theta - \theta_0)^T\nabla_\theta J(\theta_0) + \frac{1}{2}(\theta - \theta_0)^TH(\theta - \theta_0)\\ \end{aligned}其一阶导数： \begin{aligned} \frac{\partial \hat{J}}{\partial \theta} &= (\nabla_\theta J(\theta_0))^T + \frac{1}{2}(H + H^T)(\theta - \theta_0)\\ &= \nabla_\theta J(\theta_0) + H(\theta - \theta_0) \end{aligned}对于凸函数，当一阶导数为0时，有可以直接得到最优解： \nabla_\theta J(\theta_0) + H(\theta - \theta_0) = 0\\ \theta = \theta_0 - H^{-1}\nabla_\theta J(\theta_0)对于非凸函数，牛顿法不能有效的收敛。 2019-04-27论文：Feature Selective Anchor-Free Module for Single-Shot Object Detection这篇论文对retinanet进行了改进，在原始的anchor-based分支上，新增了一个anchor-free分支。 目前一阶段的目标检测大部分都是基于先验框进行，就是对于所谓的anchor(锚点)，事先定义好anchor box的大小、比例、个数，模型的回归输出作为基于anchor box的偏移，训练时将目标检测框映射分配到anchor box上，指定回归输出的优化目标，而inference过程是将和先验anchor box一起计算得到最终的predict box。 anchor-free分支不需要计算先验框，不需要定义anchor box的大小、比例、个数等超参数，而是直接将目标检测框映射到feature map上，并定义有效范围和忽略范围，feature map上在有效范围内的点作为正样本，在忽略范围外的点作为负样本进行训练，训练时，对于多尺度feature map的选择则根据每种大小的feature map的损失函数来计算，训练时只选择损失函数最小的feature map层进行训练。 论文FCOS: Fully Convolutional One-Stage Object Detection这篇论文也致力于目标检测的模型去anchor化工作，将anchor-based的预测修改为anchor-free的方式，但是Feature Selective Anchor-Free Module for Single-Shot Object Detection论文中不同的是，这里的anchor-free分支不需要计算有效范围和忽略范围，而是直接将目标框在feature map上的投影覆盖的所有点作为正样本点，其余的直接作为负样本点，这样可以提升检测的召回率，但是必然提升检测的假阳性率，因此论文中针对这个问题，提出了center-ness层，在训练过程中，让这一层来预测当前像素点到gt中心的距离度量，这一层的训练目标被定义为如下： \begin{aligned} centerness^* = \sqrt{\frac{\min(l^*, r^*)}{\max(l^*, r^*)}\frac{\min(t^*, b^*)}{\max(t^*, b^*)}} \end{aligned}其中$t^\ b^\ l^\ r^$四个值的定义如下图，这里截取的整张图都在同一个目标框的范围内，这里计算的就是两个轴交点的$centerness^*$值： 距离目标框中心越近，$centerness^$值越大，在进行inference的时候，$centerness^$值将作为feature map上每个点的预测权重，用于抑制和目标框偏移较大的点的预测值。]]></content>
      <tags>
        <tag>学习笔记，杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[学习笔记-2019-03]]></title>
    <url>%2F2019%2F03%2F06%2F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-2019-03%2F</url>
    <content type="text"><![CDATA[2019-03-06RetinaNet论文阅读论文中提出，One-Stage的检测方案，由于候选框类别极度不平衡，导致训练不好，损失精度，因此提出使用focal loss来平衡损失函数的方法。 focal loss 定义如下： \begin{aligned} p_t &= \begin{cases} p &if \quad y = 1\\ \\ 1 - p &otherwise \end{cases}\\ \\ FL(p_t) &= -\alpha_t(1 - p_t)^{\gamma}\log(p_t) \end{aligned}其中$\alpha_t$的值可以根据不同类别手动设置。 论文中使用的五种基础大小的feature map进行anchor分解，每种大小又分别又三种比例，三种缩放。]]></content>
      <tags>
        <tag>学习笔记，杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[学习笔记_2019-02]]></title>
    <url>%2F2019%2F02%2F13%2F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-2019-02%2F</url>
    <content type="text"><![CDATA[2019-02-13支持向量机首先复习了数学上的一个小知识，如果有一个超平面可以表达为如下线性方程： \begin{aligned} w^Tx+b = 0 \end{aligned}则空间中任意一点$x_0$到该超平面的距离可以表示为如下： \begin{aligned} r = \frac{|w^Tx_0+b|}{||w||} \end{aligned}在分类问题的样本空间中，令$x_i$和$y_i$分别表示某个样本和其对应的标签，支持向量机的基本思想是求出一个满足如下条件的超平面： \begin{cases} w^Tx_i + b \ge 1 & y_i = 1\\ w^Tx_i + b \le 1 & y_i = -1 \end{cases}示意图如下,这里z展示的样本的特征空间为2维，图中被圈出来的满足上式等号条件一个正样本和两个负样本被称为支持向量，$\gamma=\frac{2}{||w||}$称为间隔。 找分类超平面的问题就可以转化为如下： \begin{aligned} &\max_{w,b}\frac{2}{||w||}\\ &s.t.\quad y_i(w^Tx_i + b) \ge 1, i = 1,2,...,m\\ 等价为\\ &\min_{w,b}\frac{||w||^2}{2}\\ &s.t.\quad y_i(w^Tx_i + b) \ge 1, i = 1,2,...,m \end{aligned}这里可以使用不等式约束下的拉格朗日乘子法转换为对偶问题进行求解。 支持向量机的基本型就是希望找到一个间隔最大的超平面，但是这里有个问题，在原始的特征空间中，样本不一定线性可分，因此引入一个函数，将原始特征空间映射到一个高维的特征空间，使得在这个高维空间中线性可分，这样即可将任何分类问题转换成线性可分的形式，但是这样也有一个问题，设映射后的特征向量为$\phi(x)$，求解超平面的过程中，会遇到${\phi(x)}^T\phi(x)$的情况，因为$\phi(x)$的维数可以为无穷，所以这里的计算无法进行，因此需要设想一个这样的函数：$k(x_i,x_j)={\phi(x_i)}^T\phi(x_j)$使得不需要通过$\phi(x)$来计算${\phi(x)}^T\phi(x)$，而是直接通过$k(x_i,x_j)$来表示，这个函数即称为核函数。 核函数可以用一个核矩阵来表示如下，任何一个对称的半正定矩阵，都可以作为核矩阵。 k=\begin{bmatrix} k(x_1,x_1) &\cdots &k(x_1,x_j) &\cdots &k(x_1,x_n)\\ \vdots &\ddots &\vdots &\ddots &\vdots\\ k(x_j,x_1) &\cdots &k(x_j,x_j) &\cdots &k(x_j,x_n)\\ \vdots &\ddots &\vdots &\ddots &\vdots\\ k(x_n,x_1) &\cdots &k(x_n,x_j) &\cdots &k(x_n,x_n)\\ \end{bmatrix}选择一个核函数，就隐式的确定了映射的特征空间，因此核函数的选择非常重要。 但是在实际使用中，选出一个合适的特征空间恰好让这个样本线性可分非常不容易，因此将优化目标重写为： \min_{w,b}\frac{1}{2}{||w||}^2+C\sum_{i=1}^ml(y_i(w^Tx_i+b) - 1)这里的$C$为一个设定好的常数，决定模型的偏好，$l$为一种损失函数。这样的支持向量机被称为软间隔支持向量机。 2019-02-19对拉格朗日乘子法的理解前置知识：梯度的方向是当前等值平面的法线方向。约束函数和目标函数相切时，约束函数和目标函数的梯度方向相同。 如下图所示，其中$f(x,y)$是目标函数，$f(x,y) = d_1,f(x,y) = d_2,f(x,y) = d_3$都是目标函数的等值线，$g(x,y) = c$是约束条件，在约束条件和等值线相切的地方，有$\nabla f = \lambda \nabla g$,这里假设$d_1 &lt; d_2 &lt; d_3$且所求问题为 \min f(x,y) \\ s.t. g(x,y)=c那么其最优解一定出现在相切的地方，即只要求如下联立方程即可得到最优解的可能位置。 \begin{cases} \nabla f = \lambda \nabla g \\ g(x,y) = 0 \end{cases}但是一般看到的拉格朗日乘子法是下面的形式： \begin{aligned} &首先定义F(x,y,\lambda) = f(x,y) + \lambda g(x,y)\\ &之后求解\nabla F = \mathbf{0} \end{aligned}其实是一样的，展开$\nabla F$即可，如下： \begin{aligned} \begin{pmatrix} \frac{\partial F}{\partial x}\\ \\ \frac{\partial F}{\partial y}\\ \\ \frac{\partial F}{\partial \lambda}\\ \end{pmatrix} &= \begin{pmatrix} 0\\ \\ 0\\ \\ 0\\ \end{pmatrix}\\ &即\\ \nabla f &= -\lambda \nabla g \\ \frac{\partial F}{\partial \lambda} &= g(x,y)\\ &= 0\\ \end{aligned} 上面只是单个约束时且约束为等式的情况，如果是多个等式约束，则可以写成 $F(x,y,\lambda_1, \lambda_2,…,\lambda_m) = f(x,y) + \lambda_i g_i(x,y);i = 1, 2, …, m$ 同理求解如下： \begin{cases} \nabla f = \lambda_i \nabla g_i &i=1,2,...m\\ g_i(x,y) = 0&i=1,2,...m \end{cases}也可以写成 \begin{aligned} \begin{pmatrix} \frac{\partial F}{\partial x}\\ \\ \frac{\partial F}{\partial y}\\ \\ \frac{\partial F}{\partial \lambda_1}\\ \vdots\\ \frac{\partial F}{\partial \lambda_m}\\ \end{pmatrix} = \begin{pmatrix} 0\\ \\ 0\\ \\ 0\\ \vdots\\ 0\\ \end{pmatrix} \end{aligned}如果是不等式约束，如下描述： \begin{aligned} \min f(x,y)\\ s.t.\quad h(x,y) \le 0 \end{aligned}可以分成两种情况讨论，第一种情况是内部解，第二种情况是边界解，如果最优解在约束条件内部，那么约束条件不起作用， 退化为无约束问题，$\nabla f = 0 且 \lambda = 0$，如果在边界上，则满足$h(x,y)=0$因此不论哪种情况，$\lambda h(x,y) = 0$恒成立。 这里依旧定义一个如下形式的函数： \begin{aligned} F(x,y,\mu) = f(x,y) + \mu h(x,y) \end{aligned}因此可以提出KKT条件的基本形式： \begin{aligned} \begin{cases} \begin{pmatrix} \frac{\partial F}{\partial x}\\ \\ \frac{\partial F}{\partial y} \end{pmatrix} &= \begin{pmatrix} 0\\ \\ 0 \end{pmatrix}\\ h(x,y) &\le 0\\ \lambda &\ge 0 \\ \lambda h(x,y) &= 0 \end{cases} \end{aligned}这里的$\lambda \ge 0$是因为要求的是$\min f(x,y)$，如果要求的是$\max f(x,y)$,则使用$\lambda \le 0$ 对于多个等式约束和多个不等式约束的情况： \begin{aligned} &\min f(\mathbf{x})\\ s.t.\quad &g_j(\mathbf{x}) = 0, j=1,2,...m\\ s.t.\quad &h_k(\mathbf{x}) \le 0, k=1,2,...n \end{aligned}首先定义拉格朗日函数： L(\mathbf{x},\lambda_1,\lambda_2,...,\lambda_m,\mu_1,\mu_2,...,\mu_n) = f(\mathbf{x}) + \sum_{j=1}^m \lambda_j g_j(\mathbf{x}) + \sum_{k=1}^n \mu_k h_k(\mathbf{x})关于对偶问题的分析略过，结论是如果满足KKT条件，则原问题的最优解和对偶问题最优解相同，计算KKT条件，即可得到候选解，然后对比不同的候选解，即可得出最优解，这里直接给出完整的KKT条件： \begin{cases} \nabla_{\mathbf{x}}L = \mathbf{0}\\ g_j(\mathbf{x})=0 \quad j=1,2,...,m\\ h_k(\mathbf{x})\le 0 \quad k=1,2,...,n\\ \mu_k \ge 0 \quad k=1,2,...,n\\ \mu_k h_k(\mathbf{x})=0 \quad k=1,2,...,n \end{cases}求解KKT条件，即可找到约束优化问题的解。 2019-02-20git的常用操作复习查看仓库当前状态 git status 查看修改内容 git diff &lt;file&gt; 查看最近的提交 git log 恢复最近提交 git reset --[soft|mixed|hard] HEAD~ soft:仅仅修改HEAD指向 mixed:在修改HEAD之后，修改暂存区内容 hard:在修改暂存区内容之后，修改工作区内容 HEAD~:等效于HEAD^，代表HEAD的上一次提交，可以使用HEAD~100代表HEAD之前100次提交 HEAD~也可替换为commit id 如果指定恢复文件，则只从最后一次提交恢复暂存区文件。 git reset file 等价于 git reset --mixed HEAD file 查看版本库历史和commit id，可以用 git log 修改最后一次commit的信息 git commit --amend 查看版本库所有的改动，可以用 git reflog 创建和切换分支 git branch &lt;branch_name&gt; #创建分支 git chechout &lt;branch_name&gt; #切换分支 git checkout -b &lt;branch_name&gt; #切换分支，不存在则创建 将当前的暂存区改动压栈并隐藏 git stash 查看当前的栈区 git stash list 从栈顶恢复 git stash apply # 只恢复不删除栈内容 git stash pop # 恢复并删除栈内容 查看当前分支 git branch 合并分支 git merge &lt;branch_name&gt; #合并指定分支到当前分支 如果合并出现冲突，修改冲突文件之后，可以重新add、commit进行提交 删除分支 git branch -d &lt;branch_name&gt; 关联远程分支和本地分支 git branch --set-upstream &lt;branch-name&gt; &lt;origin/branch-name&gt; 之后就可以从远程分支pull或者push到远程分支了 关联远程服务器 git remote add &lt;remote-name&gt; &lt;remote-url&gt; 创建远程分支 git push &lt;remote-name&gt; &lt;local-branch-name&gt;:&lt;remote-branch-name&gt; 删除远程分支 git push &lt;remote-name&gt; :&lt;remote-branch-name&gt; 2019-02-21对l2正则化的理解普通的损失函数可以写成如下： J(w;X,y)在权重参数上加上l2正则化项之后，损失函数如下： \begin{aligned} &\tilde{J} = \frac{\alpha}{2}w^Tw + J(w;X,y)\\ &对w的求导：\\ &\nabla_w\tilde{J} = \alpha w + \nabla_w J(w;X,y) \end{aligned}每次的参数更新可以写成如下： \begin{aligned} &w \leftarrow w - \epsilon(\alpha w + \nabla_w J(w;X,y))\\ &等价于：\\ &w \leftarrow (1 - \epsilon\alpha) w + \epsilon\nabla_w J(w;X,y) \end{aligned}为了分析l2正则化的作用，假定$J(w ^ \ast ; X , y)$是损失函数的极小值。$w^\ast$是使损失函数取得极小值的参数，先简单的对损失函数进行二次近似如下，这里没有一次项是因为在函数极小值的地方，一次导数应该为0。 \begin{aligned} J(w;X,y) = J(w^\ast;X,y) + (w-w^\ast)^TH(w-w^\ast) \end{aligned}对加上l2正则化项的损失函数近似形式如下： \begin{aligned} \hat{J}(w;X,y) = J(w^\ast;X,y) + (w-w^\ast)^TH(w-w^\ast) + \alpha\frac{1}{2}w^Tw \end{aligned}对损失函数进行求导如下。 \begin{aligned} \nabla_w \hat{J}(w;X,y) = \alpha w + H(w - w^\ast) \end{aligned}当$\nabla_w\hat{J}(w;X,y) = 0$时，有如下推导： \begin{aligned} \alpha w + H(w - w^\ast) = 0\\ (H + \alpha I)w = Hw^\ast\\ w = (H + \alpha I)^{-1}Hw^\ast \end{aligned}可见当$\alpha \rightarrow 0$时，$w \rightarrow w^\ast$，进一步，因为$H$是实对称矩阵，因此必定可以正交对角化，$H=Q \Lambda Q^T$，因此进一步推导如下： \begin{aligned} w &= (Q \Lambda Q^T+ \alpha I)^{-1}Q \Lambda Q^Tw^\ast\\ &=[Q (\Lambda+ \alpha I) Q^T]^{-1}Q \Lambda Q^Tw^\ast\\ &=Q (\Lambda+ \alpha I)^{-1}\Lambda Q^Tw^\ast \end{aligned}其中，假设$\Lambda$如下： \begin{bmatrix} &\lambda_1 &0 &0 &\cdots &0\\ &0 &\lambda_2 &0 &\cdots &0\\ &0 &0 &\lambda_3 &\cdots &0\\ &\vdots &\vdots &\vdots &\ddots &\vdots\\ &0 &0 &0 &\cdots &\lambda_n\\ \end{bmatrix}则$(\Lambda+ \alpha I)^{-1}\Lambda$如下： \begin{bmatrix} &\frac{\lambda_1}{\lambda_1 + \alpha} &0 &0 &\cdots &0\\ &0 &\frac{\lambda_2}{\lambda_2 + \alpha} &0 &\cdots &0\\ &0 &0 &\frac{\lambda_3}{\lambda_3 + \alpha} &\cdots &0\\ &\vdots &\vdots &\vdots &\ddots &\vdots\\ &0 &0 &0 &\cdots &\frac{\lambda_n}{\lambda_n + \alpha}\\ \end{bmatrix}这就相当于在原损失函数极小值点的Hession矩阵$H$的特征向量方向上，将$w^\ast$进行了缩放，而且特征值$\lambda_i$越小的方向，$\alpha$对其影响越大，缩小得越大，即加入l2正则化项之后，模型参数倾向于对损失函数影响显著的方向偏移。 以最小二乘线性回归为例，其损失函数如下： (Xw-y)^T(Xw-y)如果加上l2正则化，则损失函数变成如下形式： (Xw-y)^T(Xw-y) + \frac{1}{2}\alpha w^Tw那么线性回归的解就从： w = (X^TX)^{-1}X^Ty变成了： w = (X^TX + \alpha I)^{-1}X^Ty其中$X$可以写成如下,其中$x_{ij}$表示第i个样本$x_i$的第j维： X=\begin{bmatrix} &x_{11} &x_{12} &\cdots &x_{1m}\\ &x_{21} &x_{22} &\cdots &x_{2m}\\ &\vdots &\vdots &\ddots &\vdots\\ &x_{n1} &x_{n1} &\cdots &x_{nm}\\ \end{bmatrix}则$X^TX$可以表示如下： X^TX=\begin{bmatrix} &\sum_i^nx_{i1}x_{i1} &\sum_i^nx_{i1}x_{i2} &\cdots &\sum_i^nx_{i1}x_{im}\\ &\sum_i^nx_{i2}x_{i1} &\sum_i^nx_{i2}x_{i2} &\cdots &\sum_i^nx_{i2}x_{im}\\ &\vdots &\vdots &\ddots &\vdots\\ &\sum_i^nx_{im}x_{i1} &\sum_i^nx_{im}x_{i2} &\cdots &\sum_i^nx_{im}x_{im}\\ \end{bmatrix}$X^TX$同样可以正交对角化，$X^TX = Q \Lambda Q^T$，这里的$\Lambda$对角线上的值是$X$奇异值的平方，之后的推导和上面相同，可见，加入l2正则化项之后，模型参数倾向于对损失函数影响显著的方向偏移，从而忽略掉数据中的一些干扰，增强模型泛化能力。 记录几个简单的矩阵求导公式 \frac{\partial \beta^T X}{\partial X} = \beta\\ \quad\\ \frac{\partial X^T X}{\partial X} = X\\ \quad\\ \frac{\partial X^T A X}{\partial X} = (A+A^T)X\\2019-02-25使用tensorflow后端的keras中的session和graph这两天在进行将keras模型集成到项目系统的工作，这个系统需要加载不同的模型和训练不同的模型，而且顺序不固定，多个模型可能同时存在，一些在训练，一些在预测，因此直接加载模型，使用keras创建的session和graph的时候，遇到了很多错误，最后的解决方案是为模型手动创建session，并在加载或者使用模型之前使用Keras.backend.set_session方法设置当前使用的session。 还有一个问题是模型使用完成之后，已经使用del model的形式删除，但是windows任务管理器上显存依旧占用，尝试过Keras.backend.clear_session的方式，显存依旧占用，但是其它模型也可以继续跑，不知道是不是tensorflow自动分配了显存的原因。 python multiprocessing的问题前两天遇到一个问题，在使用multiprocessing进行多进程任务时，发现所调用的函数所在的文件，多次执行import操作，导致已经被修改的import变量出现不一致的问题，现在还没有找到解决办法，只能把多进程改为了单进程执行。 2019-02-26对l1正则化的理解进行l1正则化之后的损失函数如下： \tilde{J} = \alpha ||w||_1 + J(w;X,y)对其进行求导： \nabla_w\tilde{J} = \alpha sign(w) + \nabla_wJ(w;X,y)从这里可以看出l1正则化项对导数的影响是一个固定值，和l2有很大区别。 使用在l2正则化分析中的损失函数近似方法，将原本的损失函数二次近似为$\hat{J}(w)$，其导数如下： \nabla_w\hat{J} = H(w - w^\ast)加上正则化项之后，其损失函数的二次近似可以表示为： \hat{J}(w;X,y) = J(w^\ast;X,y) + \sum_i[\frac{1}{2}H_{i,i}(w_i-w_i^\ast)^2 + \alpha|w_i|]这里将Hessian矩阵简化成了对角阵，这个类似对数据进行PCA之类的操作，将数据之间的相关性去掉了，因此Hessian矩阵变成对角阵，这样分析要简单一些。 如果对$w_i$进行求导，如下： \nabla_{w_i}\hat{J}(w;X,y) = H_{i,i}(w_i - w_i^\ast) + \alpha sign(w_i)可以看出，加了l1正则化之后最优解变成了如下： w_i = sign(w_i^\ast)max\{|w_i^\ast| - \frac{\alpha}{H_{i,i}}, 0\}描述成图像大概如下： 可以看出，l1使得权重变得更加稀疏，这在特征选择方面非常有用。 2019-02-27数据增强之噪声1.直接在数据上添加噪声普遍来说，在数据上添加噪声比简单的收缩权重更加有效。 2.在权重上添加噪声类似贝叶斯方法中将权重参数也作为一个随机变量的做法，在权重上添加噪声类似于在损失函数上加上预测结果对权重的导数的l2正则化项:$\eta E_{p(x,y)}[||\nabla_W \hat{y}(x)||^2]$，其中$\hat{y}$表示模型的输出。这会使得模型偏好于一个对于参数来说更加平缓的区域。 3.在标签上添加噪声对于二分类问题，可以使用$\epsilon$和$1 - \epsilon$来代替0、1标签，对于$k$类多分类问题，则可以使用$\frac{\epsilon}{k}$ 和 $1 - \frac{k - 1}{k}\epsilon$来代替，这对交叉熵损失函数可能没什么区别，因为交叉熵损失函数中为了避免$log0$，已经使用了这种标签平滑，但是对于最大似然学习并在最后一层使用softmax的算法，标签平滑可能会很有用，因为使用softmax之后，输出不可能为绝对的1和0，因此模型会尝试学习越来越大的权重，让输出更加极端，容易导致数值溢出和模型不稳定的问题，在标签上添加噪声可以解决这个问题，虽然通过权重衰减策略也可以解决这个问题，但是盲目的权重衰减可能影响到分类正确性，而在标签上添加噪声没有这个问题。]]></content>
      <tags>
        <tag>学习笔记，杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[学习笔记_2019-01]]></title>
    <url>%2F2019%2F02%2F13%2F%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0-2019-01%2F</url>
    <content type="text"><![CDATA[2019-01-20学习了如何在hexo的markdown中使用数学公式首先修改next的配置文件如下： mathjax: enable: true per_page: true 由于开启了pre_page，因此首先需要在markdown页面中使用： mathjax: true 写法和latex中的公式差不多，如： 1$\sum_&#123;i=0&#125;^&#123;n&#125;x_i$ 显示效果：$\sum_{i=0}^{n}x_i$ 但是在使用中碰到了数学公式渲染不正确的问题，网上说是hexo默认的渲染包有问题，因此需要修改。首先在博客根目录使用以下命令，卸载原来的渲染包，安装新的。 npm uninstall hexo-renderer-marked --save npm install hexo-renderer-kramed --save 这里需要注意的是一定要在博客根目录下打开控制台，才会安装到博客的node_modules目录中，否则不起作用。 之后还需要修改kramed的rule文件 修改node_modules/kramed/lib/rules/inline.js 第11行: escape: /^\\([\\`*{}\[\]()#$+\-.!_&gt;])/, 替换为: escape: /^\\([`*\[\]()#$+\-.!_&gt;])/, 第20行: em: /^\b_((?:__|[\s\S])+?)_\b|^\*((?:\*\*|[\s\S])+?)\*(?!\*)/, 替换为: em: /^\*((?:\*\*|[\s\S])+?)\*(?!\*)/, 修改之后，一切正常。语法与latex公式基本相同，详细参考latex数学公式 《Deep learning》读书笔记极大似然与对数极大似然与KL散度极大似然估计表示如下： \begin{aligned} \theta_{ML} & = \mathop{\arg\max}_\theta p_{model}(\mathbb{X}; \theta)\\ & = \mathop{\arg\max}_\theta \prod_{i=1}^m p_{model}(x^{(i)};\theta) \end{aligned}由于这样的连乘容易造成下溢，因此可以替换为对数极大似然估计： \begin{aligned} \theta_{ML} & = \mathop{\arg\max}_\theta\sum_{i=1}^m\log p_{model}(x^{(i)};\theta) \end{aligned}将训练数据看做一种经验分布$\hat p_{data}$，且因整体缩放不会影响$\mathop{\arg\max}$操作，因此后面的项可以用期望表示，对数似然估计可以用下面的式子表达： \begin{aligned} \theta_{ML} & = \mathop{\arg\max}_\theta \mathbb{E}_{\mathbf{x}\sim \hat p_{data}}\log p_{model}(x^{(i)};\theta) \end{aligned}这其实就相当于最小化KL散度，KL散度的定义如下： \begin{aligned} D_{KL}(\hat p_{data} \| p_{model}) & = \mathbb{E}_{\mathbf{x} \sim \hat p_{data}}[\log \hat p_{data}(x) - \log p_{model}(x)] \end{aligned}其中有最后一项的期望:$-\mathbb{E}_{\mathbf{x} \sim \hat p_{data}} \log p_{model}(x)$即是负的对数似然。 2019-01-21在vscode预览markdown时渲染数学公式只需要安装’Markdown+Math’这个插件就OK了。 尝试解析keras保存的参数hd5文件经过尝试，发现keras保存的参数文件结构如下：最上层有两个键:’optimizer_weights’和’model_weights’，其中’optimizer_weights’是优化器参数，这里不关心，第二个键有关于模型权重的信息。 ‘model_weights’包含attrs属性，其下又会有三个键:’layer_names’,’backend’,’keras_version’。重要的是其中的’layer_names’,这个下面需要包含所有层名，字节数组的形式。 ‘model_weights’下所有层名作为键值，每个键值都有attrs属性，attrs属性下有键值’weight_names’，包括所有的权重参数名，字节数组形式。 2019-01-22空洞卷积(也叫膨胀卷积,Dilated Convolution)空洞卷积的数学定义如下：如果$F:\mathbb{Z}^2\rightarrow\mathbb{R}$是一个离散函数，定义一个变量域$\Omega_r = [-r, r]^2 \cap\mathbb{Z}^2$再定义一个大小为$(2r+1)^2$的离散卷积$k:\Omega_r\rightarrow \mathbb{R}$,那么卷积操作可以表示为： \begin{aligned} (F \ast k)(p) = \sum_{s+t=p}F(s)k(t) \end{aligned}空洞卷积可以表示为： \begin{aligned} (F \ast_l k)(p) = \sum_{s+lt=p}F(s)k(t) \end{aligned}可见，当$l$为1时，空洞卷积就是普通的卷积。 空洞卷积可以增加感受野，空洞卷积感受野示意图如下，其中(a)图为普通卷积产生的感受野示意,记为$F1$，$3 \times 3$的普通卷积感受野和卷积核大小相同，(b)图为在(a)中的$F1$基础上进行$l$等于2的空洞卷积操作，结果记为$F2$，其感受野变为$7 \times 7$，(c)图为在(b)中$F2$的基础上进行$l$等于4的空洞卷积，其感受野计算为$(4 \ast 2 + 1) \times (4 \ast 2 + 1) = (9 \times 9)$，注意这里的感受野计算是基于逐层卷积的结果，很多博客中没有说明，我看了原文才知道。 2019-01-23DenseNet论文阅读论文地址：Densely Connected Convolutional Networks DenseNet模型结构如下。 其中$1 \times 1$卷积层称为bottleneck层，用于减少通道个数，DenseBlock由BN-ReLU-Conv($1 \times 1$)-BN-ReLU-Conv($3 \times 3$)这样的结构重复而组成，如果一个DenseBlock中每一个$3 \times 3$的卷积输出通道个数是$k$，那么作者建议设置的bottleneck层输出通道个数为$4k$，使用了bottleneck层的DenseNet称为DenseNet-B。 之后的TransitionLayer会进一步压缩模型的通道个数，其输出通道个数为$\theta m$，其中m为DenseBlock的输出通道数。而$0 &lt; \theta \le 1$，如果$0 &lt; \theta &lt; 1$那么称为DenseNet-C。 作者的实验中，最前面的一个卷积层输出通道数为$2k$，设置$\theta=0.5$并且使用了bottleneck层，因此称其模型为DenseNet-BC。训练过程中，使用SGD，初始学习率为0.1在30代和60代的时候分别除以10，训练batch_size：256，一共训练90代。 论文中解释说Densenet的提出是希望解决深层网络带来的梯度消失和梯度爆炸问题，并提对深度学习模型提出了一种新的解释：传统的前向传播模型就像是一种具有状态的算法，每一层读取其前一层的状态(输入)并对其进行处理，修改并保存了认为需要保存的状态，之后传到下一层，而Resnet通过相加处理，显式的保存了前一层的状态，Densenet通过通道连接，不仅保存了前一层的状态，而且还可以加以区分，虽然连接更密集，但是Densenet的模型可以参数相比于Resnet少，因为Densenet在DenseBlock中每一层的卷积核个数可以很少，通过$k$来指定。 ROI PoolingROI Pooling可以根据提供的区域位置信息，将特征图上的位置pooling到一个固定大小的输出。 以一个输出为$2 \times 2$的ROI Pooling为例。 输入为一张特征图。 由区域建议网络给出区域位置。 将建议区域划分为$2 \times 2$的区域。 在各个区域内进行Pooling操作(这里是Max Pooling)，得到最终输出。 卷积层输出的尺寸计算$n_{out} = [\dfrac{n_{in} + 2p - k}{s}] + 1$ 其中$n_{out}$表示输出的特征图的大小，$n_{in}$表示输入的特征图的大小，$p$表示padding大小，$k$表示卷积核大小，$s$表示stride大小。 Inception NetInception使用了NIN(Network in Network)的思想，网络中的一层不再是单一的卷积，而是几种大小的卷积分支或者几个不同深度的分支进行同时计算，最后在通道维度连接到一起。 Inception在V1版本中(也就是GoogLeNet)使用了$5 \times 5$和$7 \times 7$大小的卷积核，以适应不同尺度的目标识别。 Inception V2版本将V1中的$5 \times 5$和$7 \times 7$卷积拆开成了小卷积的堆叠，减少了计算量的同时，增加了层数。在卷积层之后使用了BN层，添加了辅助分类层，论文中说，辅助分类层在最开始的训练过程中看不出效果，在模型收敛的时候才显现出效果。 Inception V3将V2中的辅助分类层也加上了BN。 Inception V4修改了之前inception模型中最前面的卷积部分(进入Inception Block之前)，将其中的下采样变成了两个分支，一个是步长为2的卷积，一个是池化，最终再Concatenate。 inception v2/v3论文中还提到了设计模型的一些经验原则，我的理解如下： 1、慎用非常窄的瓶颈层，前馈神经网络中的瓶颈层会减少传播的信息量，如果瓶颈层非常窄，会导致有用信息的丢失，特征图尺寸应该从输入到输出逐渐减小，直到用来完成当前的任务(识别、分类等)。 2、增加卷积层的卷积核个数可以丰富特征的组合，让特征图通道之间耦合程度更低，使网络加速收敛。 3、网络开始的几层特征关联性很高，对其进行降维导致的信息损失较小，降维甚至可以加速学习。 4、平衡网络的宽度和深度。优化网络性能可以认为是平衡每个阶段(层)的卷积核数目和网络深度。同时增加宽度和深度能够提升网络性能。 2019-01-25 ~ 2019-01-26Pandas库的排序问题注意到了一个非常坑的地方，关于Pandas库的，例如以下代码。 1234567import pandas as pddict1 = &#123;'c': [1, 4, 5, 2, 3]&#125;dict2 = &#123;'c': [5, 4, 1, 2, 3]&#125;df1 = pd.DataFrame(dict1)df2 = pd.DataFrame(dict2)df1['c'] += df2['c']print(list(df1['c'])) 代码中创建了两个DataFrame，然后进行列求和。这个输出如下，一切正常。 c 0 6 1 8 2 6 3 4 4 6 但是，如果首先对两个DataFrame排序，如下： 123456789101112import pandas as pddict1 = &#123;'c': [1, 4, 5, 2, 3]&#125;dict2 = &#123;'c': [5, 4, 1, 2, 3]&#125;df1 = pd.DataFrame(dict1)df2 = pd.DataFrame(dict2)df1 = df1.sort_values(by='c')df2 = df2.sort_values(by='c')print(df1)print(df2)df1['c'] += df2['c']print(list(df1['c'])) 这个时候输出就很奇怪了，如下。 c 0 1 3 2 4 3 1 4 2 5 c 2 1 3 2 4 3 1 4 0 5 [6, 4, 6, 8, 6] 首先是打印的两个DataFrame，常规操作没有任何问题，之后进行了两列的求和，但是求和结果的顺序看不懂了，按照代码里的意思，我希望得到的结果是 [2, 4, 6, 8, 10] 但是输出的结果是，乍一看，好像还挺顺口！！！ [6, 4, 6, 8, 6] 这个结果，既不是排序之后相加，也不是原顺序相加(注意这个输出和不排序版本的输出也有差别)。 最后找到了正确解释：先按照对应的index相加，之后再按照df1的index顺序进行输出。 全连接层的反向传播算法之前看过深度神经网络(Deep Neural Network)反向传播的推导，但是没怎么用，现在感觉快忘光了，再来详细的推导一遍。首先假设损失函数: loss = J(a^L, y)其中$a^L$为第$L$层的输出值，且有 \begin{aligned} a^L & = \sigma(z^L)\\ z^L & = W^L a^{L-1} + b^L\\ z^L & = W^L \sigma(z^{L-1}) + b^L \end{aligned}那么损失函数对第$L$层的权重和偏置的偏导为： \begin{aligned} \frac{\partial J}{\partial W^L} & = \frac{\partial J}{\partial z^L}\frac{\partial z^L}{\partial W^L}\\ \frac{\partial J}{\partial b^L} & = \frac{\partial J}{\partial z^L}\frac{\partial z^L}{\partial b^L} \end{aligned}首先要计算的是$\frac{\partial J}{\partial z^L}$，这一项与损失函数和最后一层的激活函数有关，这里不具体讨论，直接计算即可，并将其结果记为$\delta^L$，之后的$\frac{\partial z^L}{\partial W^L}$项和$\frac{\partial z^L}{\partial b^L}$的计算非常简单，最后计算出的两个偏导结果分别为$\delta^L(a^{L-1})^T$和$\delta^L$。 到这里，第$L$层的偏导就计算完了，那么$L-1$层同理可以如下计算。 \begin{aligned} \frac{\partial J}{\partial W^{L-1}} & = \frac{\partial J}{\partial z^L}\frac{\partial z^L}{\partial z^{L-1}}\frac{\partial z^{L-1}}{\partial W^{L-1}}\\ & = \delta^L \frac{\partial z^L}{\partial z^{L-1}}(a^{L-2})^T\\ \frac{\partial J}{\partial b^{L-1}} & = \frac{\partial J}{\partial z^L}\frac{\partial z^L}{\partial z^{L-1}}\frac{\partial z^{L-1}}{\partial b^{L-1}}\\ & = \delta^L \frac{\partial z^L}{\partial z^{L-1}} \end{aligned}推广开来，若一共有$L$层，为方便表达，定义任意层的$\delta^l$ \delta^l = \begin{cases} \frac{\partial J}{\partial z^l}&l=L\\ \\ \delta^{l+1} \frac{\partial z^l}{\partial z^{l-1}}&l < L \end{cases}则$L-n$层的计算如下： \begin{aligned} \frac{\partial J}{\partial W^{L-n}} & = \delta^{L-n} (a^{L-n-1})^T\\ \frac{\partial J}{\partial b^{L-n}} & = \delta^{L-n}\\ \end{aligned}卷积层的反向传播首先要明确数学上的离散卷积和卷积网络中的卷积操作(网上有人也称作互相关)有区别。 对于数学中的二维卷积$Z = K*B$，若$K$的宽度和高度分别为$W_K, H_K$，若$B$的宽度和高度分别为$W_B, H_B$，那么其表达式可以写为: \begin{aligned} Z &= K \ast B\\ Z_{s,t} &= \sum_{h=0}^{H_K-1}\sum_{w=0}^{W_K-1}K_{h,w}B_{s-h,t-w} \end{aligned}而同样条件下，卷积网络卷积操作表示如下： \begin{aligned} Z &= K \ast_{Conv} B\\ Z_{s,t} &= \sum_{h=0}^{H_K-1}\sum_{w=0}^{W_K-1}K_{h,w}B_{s+h,t+w} \end{aligned}数学中的卷积$\ast$可以看做是把$\ast_{Conv}$中的卷积核$K$旋转180度后再进行$\ast_{Conv}$操作。 一般的卷积网络中，卷积层的操作可以表示如下 \begin{aligned} z^l_{s,t} &= b^l + \sum_{h=0}^{H_K-1}\sum_{w=0}^{W_K-1}K^l_{h,w}a^{l-1}_{s+h,t+w}\\ a^l &= \sigma(z^l) \end{aligned}其中$a^l$表示第$l$层输出的特征图，是一个三维张量，$W_K$和$H_K$意义同上，$K^l$表示第$l$层的卷积核，二维卷积的卷积核是一个四维张量，前面两维表示位置，后两维是一个用于映射前一层特征向量到下一层特征向量的矩阵。 如果不考虑卷积核的最后两维和特征图的最后一维，第$l$层的卷积核的偏导表示如下，其中$S$和$T$分别为第$z^l$层的高和宽。 \begin{aligned} \frac{\partial J}{\partial K^l} &= \frac{\partial J}{\partial z^l}\frac{\partial z^l}{\partial K^l}\\ \end{aligned}写成逐像素计算的形式，如下: \begin{aligned} \frac{\partial J}{\partial K^l_{h,w}} &= \sum_{s=0}^{S}\sum_{t=0}^{T}\frac{\partial J}{\partial z^l_{s,t}}\frac{\partial z^l_{s,t}}{\partial K^l_{h,w}} \end{aligned}其中的第二项如下： \begin{aligned} \frac{\partial z^l_{s,t}}{\partial K^l_{h,w}} &= \frac{b^l + \sum_{h^{'}=0}^{H_K-1}\sum_{w^{'}=0}^{W_K-1}K^l_{h^{'},w^{'}}a^{l-1}_{s+h^{'},t+w^{'}}}{\partial K^l_{h,w}}\\ &=a^{l-1}_{s+h,t+w} \end{aligned}则有： \begin{aligned} \frac{\partial J}{\partial K^l_{h,w}} &= \sum_{s=0}^{S}\sum_{t=0}^{T}\frac{\partial J}{\partial z^l_{s,t}}a^{l-1}_{s+h,t+w} \end{aligned}要进一步计算，为了简化，这里和DNN中的反向传播相同，先给出$\delta^l$定义和逐层计算规则如下： \begin{aligned} \delta^l &= \frac{\partial J}{\partial z^l}\\ \delta^l_{s,t} &= \frac{\partial J}{\partial z^l_{s,t}} \end{aligned}因此可以将上面的偏导等式写成如下表示： \begin{aligned} \frac{\partial J}{\partial K^l_{h,w}} &= \sum_{s=0}^{S}\sum_{t=0}^{T}\delta^l_{s,t}a^{l-1}_{s+h,t+w}\\ &=\delta^l\ast_{Conv}a^{l-1} \end{aligned}这里的卷积操作是上面提到的卷积层的卷积(互相关)。 最后的问题就是，如何计算$\delta^l$，推导如下： \begin{aligned} \delta^l &= \frac{\partial J}{\partial z^l}\\ \delta^{l-1} &= \frac{\partial J}{\partial z^l}\frac{\partial z^l}{\partial z^{l-1}}\\ &=\delta^l\frac{\partial z^l}{\partial z^{l-1}}\\ \delta^{l-1}_{s,t} &= \frac{\partial J}{\partial z^l_{s,t}}\\ &=\sum_{s^{'} = 0}^{S^{'}}\sum_{t^{'} = 0}^{T^{'}}\frac{\partial J}{\partial z^l_{s{'}, t^{'}}}\frac{\partial z^l_{s{'}, t^{'}}}{\partial z^{l-1}_{s,t}}\\ &=\sum_{s^{'} = 0}^{S^{'}}\sum_{t^{'} = 0}^{T^{'}}\delta^l_{s^{'},t^{'}}\frac{\partial z^l_{s{'}, t^{'}}}{\partial z^{l-1}_{s,t}}\\ z^l_{s^{'},t^{'}} &= b^l + \sum_{h=0}^{H_K-1}\sum_{w=0}^{W_K-1}K^l_{h,w}a^{l-1}_{s^{'}+h,t^{'}+w}\\ &=b^l + \sum_{h=0}^{H_K-1}\sum_{w=0}^{W_K-1}K^l_{h,w}\sigma(z^{l-1}_{s^{'}+h,t^{'}+w})\\ \frac{\partial z^l_{s{'}, t^{'}}}{\partial z^{l-1}_{s,t}} &= K^l_{s-s^{'},t-t^{'}}\sigma^{'}(z^l_{s, t})\\ \delta^{l-1}_{s,t} &= \sum_{s^{'} = 0}^{S^{'}}\sum_{t^{'} = 0}^{T^{'}}\delta^l_{s^{'},t^{'}}(K^l_{s-s^{'},t-t^{'}}\sigma^{'}(z^l_{s, t}))\\ &=\sigma^{'}(z^l_{s, t})(\delta^l\ast K^l) \end{aligned}如果考虑卷积核的最后两维的话，卷积操作中的乘法应该是向量乘法，这里不详细讨论。 池化层的反向传播池化层的反向传播算法非常简单，因为池化层没有可学习的参数，所以只需要传播$\delta^l$，以便于前面的层计算梯度。 2019-01-27无偏性估计值的均值等于被估计的随机变量:$E(\hat{\alpha}) = \alpha$ 渐进无偏性渐进无偏性相比于无偏性的要求要弱一些，是指在样本数趋于无穷大的时候，估计值的期望等于被估计值:$\lim_{n\rightarrow\infty}E(\hat{\alpha}) = \alpha$ 依概率收敛$\lim_{n\rightarrow\infty}\mathbb{P}(|X-X_n| \ge\epsilon) = 0$ 条件极大似然与均方误差首先回顾下极大似然估计：极大似然估计用于估计数据的分布参数，其对数似然形式的定义为：$\sum_i\log P(x^{(i)};\theta)$。 而在线性回归问题中，给定一个输入$x$，要预测一个$y$。 即需要求出一个函数$\hat{y}(x;w)$，其参数为$w$，给定输入$x$，输出预测值。 假设训练集中样本为$x^{(i)}$，符合独立同分布(i.i.d.)条件，观测标签为$y^{(i)}$。 最小二乘法的思想是直接对$\hat{y}(x;w)$建模并学习参数$w$。如果使用最小二乘法，学习的过程可以表示如下： \begin{aligned} \mathop{\arg\min}_w\sum_{i}{||y^{(i)} - \hat{y}(x^{(i)};w)||}_2 \end{aligned}从似然估计的角度，可以对$P(y|x)$建模，借助高斯分布，可以做如下定义： \begin{aligned} P(y|x) = \mathcal{N}(y;\hat{y}(x;w),\sigma^2) \end{aligned}这里使用$\hat{y}(x;w)$作为均值，方差则考虑了观测标签中的噪声。 因此在线性回归问题中，使用条件极大似然的方法，给出条件对数似然的形式定义如下： \begin{aligned} \sum_i\log P(y^{(i)}|x^{(i)};w) \end{aligned}可以进一步写成： \begin{aligned} \sum_i\log \mathcal{N}(y;\hat{y}(x;w),\sigma^2) \end{aligned}学习的过程可以表示成如下： \begin{aligned} \mathop{\arg\max}_{w}\sum_i\log \mathcal{N}(y^{(i)};\hat{y}(x^{(i)};w),\sigma^2)\\ \end{aligned}展开表示： \begin{aligned} &\mathop{\arg\max}_{w} \sum_i\log (\frac{1}{\sqrt{2\pi}\sigma} e^{ - \frac{(y^{(i)} - \hat{y}(x^{(i)};w))^2}{2\sigma^2}})\\ &=\mathop{\arg\max}_{w}\sum_i(-\frac{1}{2}\log2\pi - \log \sigma - \frac{(y^{(i)} - \hat{y}(x^{(i)};w))^2}{2\sigma^2}) \end{aligned}可见这里和最小二乘估计相同，依旧需要小化$\sum_i{(y^{(i)}-\hat{y}(x^{(i)};w))}^2$ 因此条件似然估计和最小二乘估计其实最终得到的结果是相同的。 hexo的一个问题在latex公式中，如果出现如下的写法，会导致报错(虽然在vscode中预览渲染没有问题)1&#123;(...)&#125;^2 报错如下，说是表达式后面需要逗号，明显是hexo解析有问题，暂时没有解决方案，只能换一种写法，将大括号去掉(不影响公式形式)。 INFO Start processing FATAL Something&#39;s wrong. Maybe you can find the solution here: http://hexo.io/docs/troubleshooting.html Template render error: (unknown path) [Line 201, Column 78]parseAggregate: expected comma after expression 2019-01-28贝叶斯统计方法和频率统计方法的理解贝叶斯统计的视角将参数也看做一个随机变量，而频率统计的视角是将参数看做一个固定量。]]></content>
      <tags>
        <tag>学习笔记，杂项</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[论文翻译之《Perceptual Losses for Real-Time Style Transferand Super-Resolution》]]></title>
    <url>%2F2018%2F05%2F08%2F%E8%AE%BA%E6%96%87%E7%BF%BB%E8%AF%91%E4%B9%8B%E3%80%8APerceptual-Losses-for-Real-Time-Style-Transferand-Super-Resolution%E3%80%8B%2F</url>
    <content type="text"><![CDATA[在实时风格转换和超分辨率上使用感知损失函数作者：Justin Johnson, Alexandre Alahi, and Li Fei-Fei摘要本文研究图像转换问题，即将一个输入图像转化到一个输出图像，最近在这个问题上使用的典型方法是训练一个使用在输出和目标图像上逐像素计算损失函数（per-pixel loss）的卷积神经网络。有研究表明高质量的图像可以通过基于预训练网络提取的高级特征定义和优化感知损失函数（perceptual loss）来生成。我们结合这些方法的有点，并且提出使用干事损失函数来训练前向网络来做图像风格转换任务，我们展示一个由Gatys等人提出的训练于解决优化问题的前向网络来做图像风格转换器的结果，相比于这个方法，我们的方法可以得到质量大致相同的图像，但是我们的速度比这个方法大约快三个数量级。我们还在单图像超分辨率问题上进行的实验，将逐像素计算的损失函数替换为感知损失函数，我们得到了视觉上令人满意的效果。 关键词：风格转换器，超分辨率，深度学习 简介许多经典问题可以被构筑为图像风格转换任务，即一个系统接收一些输入图像，并且将其转换到输出图像，图像处理的例子包括去躁、超分辨率、着色，这些问题中，输入图像是一个低质量图像（噪声多、分辨率低或者灰度图），而输出是一个高质量彩色图像。机器视觉问题中的例子包括语义分隔和深度估计（depth estimation）,这里的输入时彩色图像，输出图像编码了场景中的语义或者集合信息。一个解决图像转换任务的方法是用有监督方法训练一个前向卷积神经网络，使用逐像素计算的损失函数来测量输出和目标图像之间的距离，例如被Dong等人用于超分辨率问题[1]，被Cheng等人用于着色问题[2,3]，被Long等人用于分割问题[4]，被Eigen等人用于深度和表面正则预测（depth and surface normal prediction）[5,6]，这些方法在测试阶段非常高效，仅仅需要一个前向的通过一个训练好的网络。 然而，使用这些方法逐像素计算的损失函数不能捕获输出图像和目标图像感知上的差异。例如，考虑两个在对方基础上平移一个像素的独立图像，虽然他们在感知上相似，但是在他们之间按照逐像素计算的损失函数来度量差距的话，会显示两个图像非常不同。 最近的研究显示使用感知损失函数生成的高质量图像不是基于图像像素之间的差异，而是基于预训练的卷积神经网络提取的图像的高级特征之间的差异。图像通过最小化一个损失函数来生成。这种策略已经被Mahendran等人用于特征反演[7]，被Simonyan和Yosinski等人用于特征可视化[8][9],被Gatys等人用于上下文分析和风格转换[10][11][12]，这些方法生成了高质量的图像，但是在用于解决一个优化问题的时候很慢。 在本文中我们结合了两种方法的优点，我们训练一个用于图像转换任务的前向转换网络，但是不仅只是在低级的像素信息上使用逐像素的损失函数，我们在预训练的损失网络提取出的高级特征上使用感知损失函数。在训练过程中，感知损失函数在测试实时转换网络运行时在度量图像相似度的效果上比逐像素损失函数有更好的鲁棒性。 我们在两个任务上进行了实验：风格转换和单图像超分辨率，两者本质上是病态的。在风格转换任务上，这里没有唯一正确的输出，而在超分辨率图像上，一个低分辨率输入可以生成很多高分辨率输出图像。l两个任务都需要在输入图像上进行语意推理。在风格转换任务中，虽然在纹理和颜色上有很大变化，但输出必须在语义上和输入相似。在超分辨率任务上，需要从视觉模糊的低分辨率图片上推断出细节。原则上，训练好的用于这两个任务的一个大容量的神经网络可以隐式的学习到推断相关的语义；然而实际上我们不需要从头学习，使用感知损失函数允许语义转换的知识从损失网络传递到转换网络。 在图像风格转换优化问题[11]上；我们的结果在使用目标函数值衡量的质量上和[11]相似，但是快了三个数量级。对于超分辨率问题，我们发现使用感知损失函数替代逐像素计算的损失给出了在放大4倍和8倍的超分辨率问题上的令人满意的结果。 相关工作 前馈的图像转换 在最近几年中，很多不同的图像转换任务使用逐像素计算的损失函数来训练。 语义分隔方法[4,6,14-17]使用一个逐像素的分类损失，通过全卷积的方式在输入图像上运行网络来产生密集的场景标签，最近的深度和表层正态估计（normal estimation）方法[6,19]都是相似的，依靠使用一个前向的用逐像素的回归[5,6]或者分类损失函数[19]训练的前向卷积网络。一些方法在使用逐像素损失函数之外还使用图像梯度的惩罚[6]，将复发层和网络的其余部分一起训练来构建CRF推断[17]。或者使用一个CRF损失层[8]来加强输出中的局部一致性。 我们的风格转换网络的架构来源于[4]和[16]，使用网络内下采样来减少特征映射的空间范围，然后进行网络内上采样来产生最终的输出图像。 感知优化 最近大量的论文已经着手使用感知目标来优化图像的生成，依赖于从一个卷积网络中提取出的高级特征。图像可以根据最大化分类预测值[8,9]或者为了理解函数编码而是用个别特征[9]的方向来生成。相似的优化技术可以被用于生成高度混淆的图片[20,21]。 Mahendran和Vedaldi[7]使用最小化一个特征重构损失反转卷积网络的特征来理解不同网络层保留的图像信息;相似的方法更早被用于反转局部二值描述[22.23]以及HOG特征[24] Dosovitskiy和Brox的研究[25]和我们非常相关，他们训练了一个前向神经网络来反转卷积特征，迅速的估计一个由[7]提出的优化问题的解。然而，他们的前向网络使用的是逐像素计算的重构损失，而我们直接优化了[7]中的特征重构损失。 风格转换 Gatys等人[11]实现了艺术风格的转换，将一个图片的内容通过最小化[7]中的特征重构损失函数以及一个同样基于预训练的卷积网络提取的特征的风格重构损失来实现和另一个图片的风格结合；在这之前，有人用类似的方法来做纹理合成[10]。他们在这个问题上得到了非常好的结果，但是由于优化问题的每一步都需要在预训练的网络上前向和反向传播共两次，他们的方法需要非常大的计算量。为了减少这个计算负担，我们训练了一个前向网络来迅速估计他们的优化问题的解。论文[26,27]和我们同时提出了前向方法来快速转换图像风格。 图像超分辨率 图像差分辨率问题是产生了许多技术的一个经典问题。Yang等人[28]在广泛采用卷积神经网络之前，对之前的技术做了广泛的评估。他们将超分辨率技术分组为基于预测的技术（biliner，bicubic，Lanczos，[29]）,基于边界的方法[30,31]，统计方法[32-34]，贴片型方法[30,35-41]，以及稀疏字典方法[42,43]。最近论文[1]在但图像超分辨率问题上使用了一个使用逐像素欧几里得损失的三层卷积神经网络实现了非常好的效果。其他近期的方法包括[44-46]。 方法如fig.2所示，我们的系统包括两个部分：一个图像转换网络以及一个损失网络 三个月过去了，发现翻译这篇论文没有啥意义，因此终止于此。 引用请注明出处。]]></content>
      <tags>
        <tag>论文翻译</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[在win10上搭建python3和tensorflow-gpu环境]]></title>
    <url>%2F2018%2F03%2F10%2F%E5%9C%A8win10%E4%B8%8A%E6%90%AD%E5%BB%BApython3%E5%92%8Ctensorflow-gpu%E7%8E%AF%E5%A2%83%2F</url>
    <content type="text"><![CDATA[安装Anaconda2和Anaconda3因为需要用到一些python2的代码，因此决定同时安装Anaconda2和3，搭建python2和3共存的环境。首先安装Anaconda3作为主要环境，安装文件可以在Anaconda官网上下载，我把它安装在C盘下的Anaconda目录下，一路上没有什么特别需要注意的，两个选项都选中就好了，安装完后在Anaconda3安装目录下的envs文件夹下新建py2目录，如下图：安装好后就可以正常使用python2和Python3的环境了，并且可以非常简单的切换。只需在命令行输入如下命令即可切换到python2环境,这个py2就是上面在Anaconda3安装目录下的envs文件夹下的py2目录名。 activate py2 然后用 deactivate py2 就可以切换回python3环境 安装cuda在cuda官网选择下载版本，我选择cuda9.0版本，我的电脑适合版本如下：然后开始下载，1.4G，需要一定时间。下载完成后直接安装，我直接使用的精简安装，更改目录选在D盘下，然而并没有什么效果，还是装到了C盘，但是C盘下面还是会有一个文件夹，如图：安装完成后还需要下载cudnn，这个也在官网可以找到，需要注册并填写问卷才能下载。下载相应版本，之后解压出来，发现有几个文件夹和Cuda安装目录下的文件夹同名，那么直接把文件夹下的内容拷贝到Cuda安装目录中的对应文件夹下就好了。还要注意的是把下面的路径加入到path环境变量中： C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v8.0\lib\x64 cuda的安装基本上就结束了。命令行使用 nvcc --version 即可查看cuda版本如下： 安装tensorflow-gpu这个是最简单的一步，只需要在命令行下使用如下命令即可完成: pip install tensorflow-gpu 等待即可 这个时候遇到了一个bug，说是pip安装一些包的时候有编码问题，google了一下，按照这里的办法，修改了pip的init.py文件，完美解决。 测试环境在交互模式下，引入tensorflow然后创建一个session，可以发现找到了一颗GPU（我电脑上就一个），说明环境搭建成功，接下来就可以开始愉快的玩耍了。 原创文章，引用请注明出处。]]></content>
      <categories>
        <category>环境搭建</category>
      </categories>
      <tags>
        <tag>tensorflow-gpu</tag>
        <tag>python3</tag>
        <tag>win10</tag>
      </tags>
  </entry>
</search>
